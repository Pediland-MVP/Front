// packages/ui/src/automation-builder/types/product.enum.ts
// Duplicated (not moved) from apps/dashboard/src/types/product.enum.ts — leaf enum needed
// by product.ts below.
export enum ProductFieldTypeEnum {
  TEXT = 'text',
  SELECT = 'select',
  TEXTAREA = 'textarea',
}
