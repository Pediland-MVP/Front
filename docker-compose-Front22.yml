networks:
  app_networks:
    external: true

services:

  front22:
    build:
      context: /home/cicd/Front
      dockerfile: Dockerfile
    container_name: front22
    networks:
      - app_networks
    ports:
    - "51002:3000"
    restart: always
