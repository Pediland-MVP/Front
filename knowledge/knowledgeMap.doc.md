# Front Knowledge Map

This document indexes all documentation files inside the `Front/knowledge` directory.

| File Name / Folder | Path | Description |
|---|---|---|
| `front-back-relations.md` | `Front/knowledge/front-back-relations.md` | Documents the relationships and API dependencies between the Front and the Back projects. |
| `2026-08-19-userDetailsMobileScroll.update.md` | `Front/knowledge/updates/2026-08-19-userDetailsMobileScroll.update.md` | Summary of the admin `/users/[id]` mobile scroll fix: `(main)/layout.tsx` forced `overflow-hidden` on `LayoutPage` at all breakpoints, which (via the flex automatic-minimum-size rule) clipped page content below `md:` instead of letting `SidebarInset`'s own mobile scroll container reach it. Fixed to `md:overflow-hidden`. |
| `knowledgeMap.doc.md` | `Front/knowledge/knowledgeMap.doc.md` | Indexes and describes all documentation files in this folder. |
| `updates/` | `Front/knowledge/updates/` | Folder containing date-stamped markdown updates for individual features and bug fixes. |
| `2026-07-12-subscriptionDisplayPolish.update.md` | `Front/knowledge/updates/2026-07-12-subscriptionDisplayPolish.update.md` | Summary of subscription validity merging and reserved subscription UI polish. |
| `2026-07-12-userDetailsCardSubscriptionInfo.update.md` | `Front/knowledge/updates/2026-07-12-userDetailsCardSubscriptionInfo.update.md` | Summary of removing the top-level credit/renewal row and displaying remaining subscription days next to Instagram profiles. |
| `2026-07-13-mobileExportFormsResponsiveness.update.md` | `Front/knowledge/updates/2026-07-13-mobileExportFormsResponsiveness.update.md` | Summary of the shared Drawer/DialogStyled scroll fix and the date-picker font-size fix (iOS Safari zoom-on-focus) across the Sessions/Orders/Contacts excel-export forms and the Contacts edit dialog. |
| `2026-07-13-hideSubscriptionFromWebView.update.md` | `Front/knowledge/updates/2026-07-13-hideSubscriptionFromWebView.update.md` | Summary of detecting the Android WebView via the UA `wv` marker and hiding credit-type subscription info plus all navigation to `/settings/subscription` in that context. |
| `2026-07-13-installSection.update.md` | `Front/knowledge/updates/2026-07-13-installSection.update.md` | Summary of the new `/install` page (Android store links / iOS PWA-install steps by device) and its sidebar entry. |
| `2026-07-13-automationCopy.update.md` | `Front/knowledge/updates/2026-07-13-automationCopy.update.md` | Summary of the "Copy" button on automation cards that pre-fills `/automations/add` from an existing automation's full content (including Instagram accounts), always creating a new, independent automation. |
| `2026-07-13-accountSessions.update.md` | `Front/knowledge/updates/2026-07-13-accountSessions.update.md` | Summary of the new `/settings/account-session-management` ("نشست‌های فعال حساب") page that lists the user's active login sessions (`GET /auth/sessions`), pins the current device, and terminates sessions older than 5 days (`DELETE /auth/sessions/:id`). |
| `2026-07-14-sidebarNavPendingSpinner.update.md` | `Front/knowledge/updates/2026-07-14-sidebarNavPendingSpinner.update.md` | Summary of swapping the sidebar/bottom-nav icon and dashboard home stat-card icons for a spinning `CircleNotchIcon` while a clicked link's target route is pending, via Next.js's `useLinkStatus` hook. |
| `2026-07-14-workspaceCategories.update.md` | `Front/knowledge/updates/2026-07-14-workspaceCategories.update.md` | Summary of the workspace categories feature: admin CRUD page + category column/filter on the admin workspaces page, and the dashboard's required category on workspace creation/onboarding plus the forced-category guard for legacy workspaces. |
| `2026-07-14-bannerCarousel.update.md` | `Front/knowledge/updates/2026-07-14-bannerCarousel.update.md` | Summary of the banner carousel feature: admin CRUD page for banners with category multi-select, and the dashboard carousel component that fetches active banners, shuffles them, and auto-advances every 5s. |
| `workspace-categories/` (admin) | `Front/apps/admin/src/app/(main)/workspace-categories/` | Admin CRUD page for the workspace-category taxonomy: `page.tsx` renders `client-page.tsx`, which drives a paginated/searchable `/workspace-categories` SWR query (matching the `workspaces` page's list style) and passes data to `workspace-categories-table.tsx` (search input, add/edit dialog with `nameFa`/`nameEn` fields via `react-hook-form` + `zod`, delete with confirm) and `columns.tsx` (`useWorkspaceCategoryColumns` — `nameFa`, `nameEn`, `workspaceCount`, `createDate`, row actions). |
| `banners/` (admin) | `Front/apps/admin/src/app/(main)/banners/` | Admin CRUD page for banners: `page.tsx` renders `client-page.tsx`, which drives a paginated/searchable `/banners` SWR query and passes data to `banners-table.tsx` (search input, add/edit dialog with title/description bilingual fields, buttons field array, and category multi-select via `react-hook-form` + `zod`, delete with confirm) and `columns.tsx` (`useBannerColumns` — `title`, `category`, `isActive`, `order`, `createDate`, row actions). |
| `workspaces/{columns,workspace-table,client-page}.tsx` (admin, category addition) | `Front/apps/admin/src/app/(main)/workspaces/` | The existing admin workspaces list gained a `category` column (`columns.tsx`, shows `category.nameFa` or a "no category" placeholder) and a category filter `Select` (`workspace-table.tsx`, sourced from `/workspace-categories?limit=100`); `client-page.tsx` holds the `categoryId` filter state and appends it to the `/workspaces` query string. |
| `useWorkspaceCategories.ts` | `Front/apps/dashboard/src/hooks/useWorkspaceCategories.ts` | SWR hook wrapping `GET /workspace-categories` (`{ id, nameEn, nameFa }[]`), shared by the "new workspace" creation dialog, the onboarding form, and `WorkspaceCategoryGuard` — the single source of the category list on the dashboard. |
| `WorkspaceCategoryGuard.tsx` | `Front/apps/dashboard/src/components/Console/WorkspaceCategoryGuard.tsx` | Non-cancelable dialog (no close button, `Escape`/outside-click suppressed) mounted in `(Console)/layout.tsx` next to `WorkspaceSessionGuard`. Renders only when the active workspace has no `category` **and** the current user is its `ownerId` (non-owners are never blocked); submits `PATCH /workspaces/:id` with the chosen `categoryId` and revalidates `useWorkspaces()`. |
| `useActiveBanners.ts` | `Front/apps/dashboard/src/hooks/useActiveBanners.ts` | SWR hook wrapping `GET /banners/active` (`{ id, titleEn, titleFa, descriptionEn, descriptionFa, color, buttons: [{ id, textEn, textFa, url, isExternal }] }[]` — no `categoryId(s)`/`isActive`, by design: the endpoint never reveals why a banner was selected), shared by the dashboard home carousel component. Handles error states and provides fallback empty array. |
| `DashboardBannerCarousel.tsx` | `Front/apps/dashboard/src/components/Console/Dashboard/DashboardBannerCarousel.tsx` | Carousel component that renders active banners fetched from `useActiveBanners`, using `StableCarousel` (with `StableCarouselPrevious`/`StableCarouselNext` nav buttons) with shuffle-once-per-load and auto-advance every 5s. Each banner displays bilingual title/description, background color, and clickable buttons (internal page links or external URLs). Replaces the deleted `DashboardHelpBanner.tsx` in the dashboard home. |
| `2026-07-14-freeAutomationQuotaWarningDialog.update.md` | `Front/knowledge/updates/2026-07-14-freeAutomationQuotaWarningDialog.update.md` | Summary of the `AutomationForm` confirmation dialog that warns before the automation that would push a selected Instagram page over its free automation quota, using new `automationCount`/`freeAutomationLimit`/`freeAutomationQuotaExceeded` fields on `GET /instagram/accounts` (paired Back branch `feat/free-automation-quota`). |
| `2026-07-14-templateSystemFrontend.update.md` | `Front/knowledge/updates/2026-07-14-templateSystemFrontend.update.md` | Summary of the Template System feature: the shared `packages/ui/src/automation-builder` extraction (`AutomationBuilder` with `mode='automation'\|'template'`), the admin `/templates` CRUD pages, the dashboard Create-Automation-from-template picker, `?templateId=` prefill, and the in-form "Template" content-type insert. |
| `2026-07-16-delayContentSelectbox.update.md` | `Front/knowledge/updates/2026-07-16-delayContentSelectbox.update.md` | Summary of the shared 23-hour delay budget feature: `DelayContent.tsx`'s free-text magnitude input replaced by a budget-aware `<Select>`, `Contents.tsx` blocking a new DELAY item once the budget is used up (`DelayBudgetExhaustedDialog`), and an admin `TemplateForm.tsx` submit-time 23h-sum guard added for parity with the dashboard's existing check. Frontend-only, no backend changes. |
| `2026-07-17-automationTextareaAutogrow.update.md` | `Front/knowledge/updates/2026-07-17-automationTextareaAutogrow.update.md` | Summary of the auto-growing textareas in the automation contents editor (4-row floor, 12-row cap, JS scrollHeight measurement instead of CSS field-sizing). |
| `2026-07-17-instagramPostButton.update.md` | `Front/knowledge/updates/2026-07-17-instagramPostButton.update.md` | Summary of the "Instagram Post" button type: reuses the "پست خاص" select-post dialog (generalized to a headless/controlled mode) to fill a URL button's link from a picked post's permalink, with the same single-Instagram-account guard, no backend changes. |
| `automation-builder/` (`@befroosh/ui`) | `Front/packages/ui/src/automation-builder/` | Shared automation-authoring builder, extracted from the dashboard's `AutomationForm` so both the dashboard (real automations) and the admin templates UI render the same form. `AutomationBuilder.tsx` owns the `react-hook-form` instance (`AutomationFormSchema` from `schemas/automationForm.ts`) and takes a `mode: 'automation' \| 'template'` prop (`'template'` renders only Conditions/Triggers/Contents, skipping JustFollowers/comment-trigger — templates have no workspace/live-automation context), an `apiClient: { upload, get }` (`types/apiClient.ts`) the caller injects (dashboard: `apps/dashboard/src/lib/automationApiClient.ts`; admin: `apps/admin/src/lib/templateAutomationApiClient.ts`), plus `initialValue`/`onSubmit`/`beforeSubmit`/`onInvalid`/`headerSlot`/`helpSlots`/`hasInstagram`/`isPromotion`/`commentRepliesSlot`. `Contents/` holds every content-type editor plus `remapTemplateContents.ts` (strips server ids/timestamps/back-refs and workspace-scoped product/Instagram-post refs, re-derives `delayUnit`/vitrin image fields/button `postbackPayloadType`, so a template's `contents[]` can be safely appended into another automation/template). `TemplatePicker/` (`TemplatePicker.tsx` + `TemplateCard.tsx`) is a shared search+grid (Dialog/Drawer) reused by the dashboard's create-automation flow and the in-form "Template" content-type insert (`Contents.tsx`, hidden in `mode="template"` and in reminder-mode Contents trees). `utils/delayBudget.ts` is a pure helper computing the shared 23-hour DELAY-item budget math (`remainingDelayBudgetMs`, `delayUnitOptionsCount`) consumed by `DelayContent.tsx` (magnitude `<Select>` option range) and `Contents.tsx` (blocking a new DELAY item once the budget is exhausted) — see `2026-07-16-delayContentSelectbox.update.md`. **Updated 2026-08-02:** `QuestionContent.tsx`'s `validationType` dropdown (`types/validationType.enum.ts`) gained an `Image` option — restricts the lead's DM reply to Instagram image attachments (paired Back change validates `message.attachments`, not `message.text`). See `2026-08-02-questionImageValidation.update.md`. **Updated 2026-08-02 (same day, PostalCode):** the same dropdown gained a `PostalCode` option (Iranian postal code regex validation, paired Back change, no attachments involved — a plain text validator like `Mobile`/`NationalCode`). See `2026-08-02-questionPostalCodeValidation.update.md`. **Updated 2026-08-03 (BEF-164):** `DelayContent.tsx`'s number/unit `Select` fields swapped to render number-first (was reading "hour 1" in the RTL UI instead of "1 hour"); `MediaUploader.tsx`'s audio `acceptedFormats` gained `audio/mp3`. See `2026-08-03-bef164AutomationDelayAndMp3Fix.update.md`. |
| `templates/` (admin) | `Front/apps/admin/src/app/(main)/templates/` | Admin CRUD pages for templates, mirroring the `workspace-categories` admin pattern: `page.tsx`/`client-page.tsx` drive a paginated/searchable `/templates` SWR query into `templates-table.tsx` (thumbnail, title, description, categories/"applies to all" badge, row actions) + `columns.tsx`. `add/page.tsx` and `[id]/page.tsx` both render `TemplateForm.tsx`, which wraps `AutomationBuilder mode="template"` with a `headerSlot` holding a separate `metaForm` (title, description, "applies to all categories" switch + category multi-select from `/workspace-categories`, thumbnail upload via `POST /templates/:id/upload-image`) merged into one payload at submit (`POST`/`PATCH /templates[/:id]`). Editing prefills from `GET /templates/:id`'s raw `ContentCycle` graph via `transformTemplateToFormValues`. |
| Dashboard Create-Automation-from-template | `Front/apps/dashboard/src/components/Automations/CreateAutomationTemplateDialog.tsx`, `Front/apps/dashboard/src/app/(Console)/automations/add/page.tsx`, `Front/apps/dashboard/src/components/Automations/AutomationForm.tsx` | As of `2026-07-18-automationSkipTemplatePicker.update.md`, `automations/page.tsx`'s "create automation" button no longer opens `CreateAutomationTemplateDialog` — it (and the draft-flow "create new" branch) navigates straight to plain `/automations/add`, same as the dialog's old "start from scratch" button and the dashboard quick-action link. `CreateAutomationTemplateDialog.tsx` (listing `GET /templates?search=` via the shared `TemplatePicker`, selecting one navigates to `/automations/add?templateId=<id>`) and the in-form "Template" content-type insert (`Contents.tsx`) still use `TemplatePicker` — only the create-automation entry point stopped opening this dialog. The `add/page.tsx` route reads and UUID-validates `?templateId=` alongside the existing `?copyFrom=`; `AutomationForm.tsx` accepts a `templateId` prop and — only for a brand-new automation (no `id`/`copyFromId`) — fetches `GET /templates/:id` and feeds it through the same `initialValue` transform as `copyFromId`, silently (no toast, since nothing is persisted yet). |
| `2026-07-17-httpsLinkNormalization.update.md` | `Front/knowledge/updates/2026-07-17-httpsLinkNormalization.update.md` | Summary of the submit-time zod transforms that upgrade user-entered `http://` links to `https://` (and add a scheme to bare-domain button urls) across the shared automation form (including message fields from Task 2b) and the dashboard product form. |
| `2026-07-18-subscriptionBuyButtonVisibility.update.md` | `Front/knowledge/updates/2026-07-18-subscriptionBuyButtonVisibility.update.md` | Summary of fixing the invisible "خرید اشتراک" button on `/settings/subscription` — `bg-violet-650` in `ChoosePlan.tsx` isn't a real Tailwind class, so no background CSS was generated; changed to `bg-violet-600`. |
| `2026-07-18-buttonTemplateUiSizing.update.md` | `Front/knowledge/updates/2026-07-18-buttonTemplateUiSizing.update.md` | Summary of the sizing/alignment cleanup of the shared button-row editor (`ContentButtonsItem.tsx`) used by `buttonTemplate`/`text`/`question`/`vitrin` content buttons — fixed type-select width jumping, type+title not sharing a row, asymmetric card padding from a `-mt-2` hack, inconsistent per-card top padding, and a mismatched icon-button height next to the URL input. No functional/behavior changes. |
| `2026-07-18-automationSkipTemplatePicker.update.md` | `Front/knowledge/updates/2026-07-18-automationSkipTemplatePicker.update.md` | Summary of removing the template-picker step from the "create automation" flow — clicking the button on `automations/page.tsx` now navigates straight to `/automations/add` instead of opening `CreateAutomationTemplateDialog`, matching the dialog's old "start from scratch" button. `TemplatePicker`/`CreateAutomationTemplateDialog` left in place for other uses. |
| `2026-07-18-vitrinOptionalTitleDescription.update.md` | `Front/knowledge/updates/2026-07-18-vitrinOptionalTitleDescription.update.md` | Summary of making vitrin (generic template) `title`/`description` optional in `VitrinItemSchema` to match the backend DTO relaxation, plus updated "(اختیاری)" placeholders and a 5-row description `Textarea`. |
| `2026-07-18-autoConsentQuickReply.update.md` | `Front/knowledge/updates/2026-07-18-autoConsentQuickReply.update.md` | Summary of auto-inserting a `CONSENT` ("مکث و ادامه") quick reply at index 0 of any TEXT content that has quick replies and is no longer the last content in `contents` — prevents Instagram from hiding that content's buttons once another content follows it. Reactive `useEffect` in `Contents.tsx`, `contents` array only (not `reminders`), no backend change. |
| `2026-07-18-lockConsentQuickReply.update.md` | `Front/knowledge/updates/2026-07-18-lockConsentQuickReply.update.md` | Follow-up to `2026-07-18-autoConsentQuickReply.update.md`: locks removal of a `CONSENT` quick reply in `ContentButtonsItem.tsx` while its content still has a following content (live computed condition, not a stored flag) — clicking remove shows an explanatory `AlertDialog` instead of deleting it. Unlocks automatically once the content becomes last again. |
| `2026-07-19-bannerCarouselRtlFix.update.md` | `Front/knowledge/updates/2026-07-19-bannerCarouselRtlFix.update.md` | Follow-up to `2026-07-14-bannerCarousel.update.md`: fixes `StableCarousel`'s flex-row translate math only working under LTR (broke slide 2+ under the dashboard's default `dir="rtl"`) by detecting the resolved direction and flipping the translate sign; adds `StableCarouselPrevious`/`StableCarouselNext` nav buttons. |
| `2026-07-19-bannerCarouselOverlapAndFontSize.update.md` | `Front/knowledge/updates/2026-07-19-bannerCarouselOverlapAndFontSize.update.md` | Follow-up to `2026-07-19-bannerCarouselRtlFix.update.md`: reserves an inline gutter in `BannerSlide`'s `CardContent` matching the nav buttons' footprint so long text no longer renders underneath them; adds `fluidTextSizeClass()`, a 3-tier character-count-based size lookup for title/description, so a long banner shrinks instead of inflating every shuffled slide's height. |
| `2026-07-20-adminSidebarScroll.update.md` | `Front/knowledge/updates/2026-07-20-adminSidebarScroll.update.md` | Fixes the sidebar clipping its overflow: `SidebarContent` used `overflow-hidden` (upstream shadcn uses `overflow-auto`), so opening a parent menu taller than the sidebar hid items with no scrollbar and no wheel scroll. Switches to `overflow-y-auto overflow-x-hidden overscroll-contain` + the repo's `scrollbar-thin` convention, keeping header/footer pinned; `nav-main.tsx` scrolls a newly-opened parent into view. Shared via the `components/ui` symlink, so it fixes the dashboard's `ConsoleSidebar` too. |
| `2026-08-01-consentQuickReplyUnlock.update.md` | `Front/knowledge/updates/2026-08-01-consentQuickReplyUnlock.update.md` | Fixes BEF-142, a dead end left by `2026-07-18-lockConsentQuickReply.update.md`: the lock keyed only off "a content follows this one", so deleting every other quick reply left an auto-inserted `CONSENT` button that could never be removed (and still paused the flow at send time via `shouldPauseForConsent`). `isLockedConsentButton` now also requires at least one non-`CONSENT` quick reply to still be there — an untyped, freshly-added row counts as one. Frontend-only, no backend change. |
| `2026-08-01-contentTypePickerGrid.update.md` | `Front/knowledge/updates/2026-08-01-contentTypePickerGrid.update.md` | Fixes BEF-138: reverts `ChooseAutomationType.tsx` from the `Dialog`/`Drawer` (bottom-sheet-on-mobile) rewrite back to the earlier inline `grid-cols-5` icon+title button grid (no description per button, no dialog wrapper), while keeping the newer `options`-prop template-mode filtering. Refreshes `Automations.Contents.Types.buttons.titles`/`descriptions` copy in both apps' `fa.json` per reporter-supplied text. Also fixes a `Contents.tsx` gap the revert exposed: the zero-contents empty state (`no_content_title`/`add_step`) lacked the `!isChoosingType` gate the populated-list "add content" button already has, so it stacked visibly above the newly-inline type grid instead of the old modal quietly covering it. A reporter-requested "برچسب" (Label) content type has no backing enum value yet — left out of scope. |
| `2026-08-01-dashboardStaticBannersRemoval.update.md` | `Front/knowledge/updates/2026-08-01-dashboardStaticBannersRemoval.update.md` | Deletes the hardcoded, non-admin-managed `DashboardTelegramBanner.tsx`/`DashboardInstagramBanner.tsx` (fixed copy/URL/color, no DB backing, `fa`-only 2-column grid) from the dashboard home. `DashboardBannerCarousel` (the real admin-managed banner system) moves down to the bottom of `page.tsx` — the position the deleted grid occupied. New order: `SubscriptionBoard` → `DashboardStats` → `DashboardBannerCarousel`. Frontend-only, no backend change. |
| `2026-08-02-questionImageValidation.update.md` | `Front/knowledge/updates/2026-08-02-questionImageValidation.update.md` | Adds `Image` to `ValidationTypeEnum` (`packages/ui/src/automation-builder/types/validationType.enum.ts`) for the automation Question content step's `validationType` dropdown — paired with a Back change that validates the lead's DM reply contains an Instagram image attachment. `QuestionContent.tsx`'s `<Select>` already iterates `Object.values(ValidationTypeEnum)`, so only a default Persian error message case and the `fa.json` label were needed, in **both** `apps/dashboard/src/messages/fa.json` and `apps/admin/src/messages/fa.json` — the admin app's template builder renders the same `QuestionContent.tsx` (its own separately-synced `fa.json` copy needed the key too, caught by code review). No zod schema change (`z.nativeEnum` auto-includes new enum values). |
| `2026-08-02-questionPostalCodeValidation.update.md` | `Front/knowledge/updates/2026-08-02-questionPostalCodeValidation.update.md` | Adds `PostalCode` to `ValidationTypeEnum` for the same Question content `validationType` dropdown — a plain regex-based text validator (paired Back change), same shape as `Mobile`/`NationalCode`. Both `apps/dashboard/src/messages/fa.json` and `apps/admin/src/messages/fa.json` updated together this time, applying the lesson from the `Image` feature's missed admin copy. |
| `2026-08-04-contactsMobileTableScroll.update.md` | `Front/knowledge/updates/2026-08-04-contactsMobileTableScroll.update.md` | Fixes two mobile-only `/contacts` bugs. (1) The contacts table's horizontal scroll was unreachable: its `overflow-auto` box was genuinely scrollable (client 390px vs scroll 650px) but the box itself was **1368px tall**, so its scrollbar sat ~680px below the viewport. Root cause is `SidebarInset` (`packages/ui/src/components/ui/sidebar.tsx` — note both `apps/dashboard/src/components/ui` and `apps/admin/src/components/ui` are **symlinks** to it, so this change hits both apps) capping its height only at `md` (`md:max-h-screen`) — below `md` nothing had a definite height, so `LayoutTable`'s `h-full` resolved to `auto` and `flex-1` on the table box did nothing. Fixed with an all-breakpoint `max-h-svh`, plus moving mobile scrolling onto `SidebarInset` itself (`overflow-y-auto`, `md:overflow-hidden`) so pages with no scroll container of their own are not clipped by the new cap. **App-wide change — every `(Console)` page now scrolls inside `SidebarInset` on mobile instead of on `body`.** (2) Header/filter spacing: the Excel-export button moved from `HeaderTools` (which is `w-full` on mobile, so it wrapped onto its own row as a near-invisible `outline` button on the purple gradient) into `HeaderButton` as a labelled solid button, matching `orders/page.tsx`; the Instagram filter got its own padded `empty:hidden` row in `ContactsList.tsx` instead of relying on the column `gap-4`, while the table stays full-bleed. |
| `2026-08-03-bef164AutomationDelayAndMp3Fix.update.md` | `Front/knowledge/updates/2026-08-03-bef164AutomationDelayAndMp3Fix.update.md` | Fixes BEF-164: swaps the delay/wait step's number and time-unit `Select` field order in `DelayContent.tsx` (was reading "hour 1" in the RTL UI instead of "1 hour" — pure DOM-order swap, no logic change); adds `audio/mp3` to `MediaUploader.tsx`'s accepted audio formats (some browsers report `.mp3` as `audio/mp3` rather than `audio/mpeg`), plus matching `fa.json`/`en.json` help-text updates. Frontend-only, no backend change. Ticket's 3rd item (form-builder postal-code/photo options) reported separately resolved already. |
| `2026-08-03-bef163GeneralUiFixes.update.md` | `Front/knowledge/updates/2026-08-03-bef163GeneralUiFixes.update.md` | Fixes BEF-163's 6 general UI items: adds a dashboard-wide `RadixDirectionProvider` (root cause of every Radix `Select`/dropdown rendering LTR in the Persian UI — `apps/admin` already had this, `apps/dashboard` didn't); widens the automation builder's comment/DM-cap disclaimer to full width and refreshes its copy to the current 300/hour cap; fixes the account-sessions page's dead horizontal scroll (`min-w-0` on a flex ancestor), renames it to "دستگاه‌های فعال", and adds its missing breadcrumb translation; normalizes the mobile bottom nav's icon sizes/label typography so all 5 items align; widens `InstagramSelectField`'s single-page hide guard from `=== 0` to `<= 1` with a matching auto-select effect so the required `instagramIds` field still gets filled; and reworks the subscription-renewal form (removes the "already has an active subscription" notice, consolidates the duplicated desktop/mobile discount-code blocks into one placed after the duration cards instead of before them). |
| `2026-08-05-r2ImageHosts.update.md` | Adds the two Cloudflare R2 custom domains (`dl.befroosh.app` prod, `dltest.befroosh.app` test) to `apps/dashboard/next.config.mjs` `images.remotePatterns`, fixing a `next/image` "Invalid src prop … hostname … is not configured" crash on the workspace drawer. R2's own `**.r2.cloudflarestorage.com` S3-API host is deliberately excluded — it needs a signed request, so allowlisting it would not help; the real fix was in the backend URL builder (Back PR #5). Also adds `ERROR_CODES.UPLOAD_SOURCE_IS_NOT_AN_IMAGE` to `fa.json`. **Deploy coupling: ship before or with the Back change, and note `next.config.mjs` is read at build time so a rebuild is required.** |
| `2026-08-03-automationDraftBanner.update.md` | `Front/knowledge/updates/2026-08-03-automationDraftBanner.update.md` | Fixes BEF-141: moves the automation-draft resume prompt from a blocking popup dialog on the automations list page into a dismissible `AutomationDraftBanner` at the top of the create-automation form (`headerSlot`), with "continue editing" / "new message" (clear + in-place reset via a `formResetKey` remount) actions and auto-dismiss on the first real edit. Adds a 2-day TTL to stored drafts (`automationDraft.ts`). Frontend-only, no backend change. Follow-up (2026-08-04): the banner's copy and both buttons are now grouped inside a single amber `Alert` (responsive one-line/stacked layout, amber-themed buttons) instead of an alert plus a separate button row. |
| `WorkspaceDrawerContent.tsx` | `Front/apps/dashboard/src/components/Console/WorkspaceDrawerContent.tsx` | Presentational content of the mobile workspace drawer (opened from `NavBottom.tsx`'s bottom-nav "کسب‌وکار" item via the now-thin `WorkspaceDrawer.tsx` wrapper): profile card (name/mobile + pencil `Link` to `/settings`), an always-expanded list of every workspace with its Instagram accounts nested underneath (connection dot from `isIgTokenValid`; subscription text — days-left / covered-by-credit / pending-activation — computed directly from `GET /workspaces`'s per-instagram `subscriptionDaysLeft`/`hasReservedSubscription` and per-workspace `hasCreditCoverage`, **not** `PageCoverageBadge`, which only ever has the active workspace's data), tapping any workspace/account row switches context via `useWorkspaces().changeWorkspace`, and a footer "افزودن پیج" button to `/connect` plus logout. Split out from `WorkspaceDrawer.tsx` specifically so it's testable without vaul/Drawer portal mechanics. |
| `2026-08-03-workspaceDrawerRedesign.update.md` | `Front/knowledge/updates/2026-08-03-workspaceDrawerRedesign.update.md` | Summary of the mobile workspace drawer redesign: grouped per-workspace Instagram accounts, connection/subscription status, business-labeled bottom nav, add-page footer button. Paired Back branch `feat/workspace-drawer-redesign` (added `isIgTokenValid` to `GET /workspaces`). |
| `2026-08-05-workspaceDrawerRemainingDays.update.md` | `Front/knowledge/updates/2026-08-05-workspaceDrawerRemainingDays.update.md` | Follow-up fix: the drawer only showed subscription days-left for the active workspace (real-user-caught bug). `GET /workspaces` now computes it server-side per workspace; `WorkspaceDrawerContent.tsx` drops `PageCoverageBadge` entirely in favor of the new backend fields. Paired Back branch `fix/workspace-drawer-remaining-days`. |
| `2026-08-04-bef163AutomationListSinglePage.update.md` | `Front/knowledge/updates/2026-08-04-bef163AutomationListSinglePage.update.md` | BEF-163 follow-up: `AutomationCard.tsx` (automation list) now hides its per-card Instagram username badge when the workspace has only one connected page, mirroring `InstagramSelectField.tsx`'s `<= 1` guard from the 2026-08-03 bundle — that bundle only fixed the create form, not the list. |
| `2026-08-03-dashboardDevSpeed.update.md` | `Front/knowledge/updates/2026-08-03-dashboardDevSpeed.update.md` | Cuts `apps/dashboard` dev-mode cost. `@phosphor-icons/react` is imported from 119 sites and its barrel pulls ~3025 modules (1512 CSR + 1513 SSR); Turbopack does not tree-shake in dev, and Next 16's built-in `optimizePackageImports` list covers `lucide-react`/`recharts` but not Phosphor — fixed by adding `experimental.optimizePackageImports: ['@phosphor-icons/react', '@befroosh/ui']`. Sentry also ran at full power in dev (client Session Replay under HMR, `tracesSampleRate: 1`, server-side OpenTelemetry hooking every module load) — now gated behind `enabled: isProd` in all three configs, production behaviour unchanged. Stale 5.2 GB `.next` cleared. Also rewrites all 116 barrel imports to deep per-icon paths (263 lines, incl. the separate 1513-module `/dist/ssr` barrel) and removes the 2 `import * as` namespace imports that pulled the whole barrel regardless of config — both were dynamic icon-lookup-by-name, now passing the component instead. Drops 9 unused deps plus the `motion` wrapper (`motion/react` is just `export * from 'framer-motion'`). Statically verified: 150/150 specifiers resolve, 264/264 bindings exist; typecheck 211=211 (0 introduced). **Measured:** `/contacts` 13.1s → 1.69s (~7.8x), `/directs` ~30%, `/orders` ~13%; other routes within noise. Nearly the whole win came from deleting the 2 namespace imports, not the 116-file deep-import rewrite — a namespace import defeats both `optimizePackageImports` and deep imports. `apps/admin` given the same treatment. |
| `2026-08-03-guideHelperFixes.update.md` | `Front/knowledge/updates/2026-08-03-guideHelperFixes.update.md` | Fixes BEF-140's in-app guide (راهنما) dialog: `HelpMeDialog.tsx`'s close control moves from a bottom "بستن" button to a `DialogClose`+`X` icon in the dialog's top-left corner (RTL layout, matches the header's reserved `pl-6` space); `playsInline` added to the guide `<video>` to stop iOS Safari auto-fullscreen; the shared `AutomationBuilder` package's `TitleAndEnabled`/`ContentItem` gained `helpSlot`/`contentTypeHelpSlots` so the dashboard can inject a CMS-driven `HelpMeDialog` per content type and for the automated-message-name field; the "start request message" guide's overlap with the header delete button is fixed via `noAbsolute`; new `helpId`s are registered in the Admin guides page's dropdown. |
| `2026-08-08-secondInstagramSubscriptionGate.update.md` | `Front/knowledge/updates/2026-08-08-secondInstagramSubscriptionGate.update.md` | New `SetupInstagramDialog` on `/connect`, gated by `hasAvailableSubscriptionSlot` (originally from `GET /users/me`, superseded 2026-08-08, see below), guides a user through username check → follower lookup → matched/manual plan → buy before connecting a 2nd+ Instagram account. Payment verify redirects to `/connect` (not `/settings/instagram`) for pooled purchases, with a one-time-read `pending_ig_username` cookie reminder. Also fixes a shallow-merge i18n bug: new `ERROR_CODES` translations must live in `messages/fa/ErrorCodes.json`, not the monolithic `messages/fa.json` (which is fully shadowed by the split file in `i18n/request.ts`'s merge). |
| `2026-08-08-workspaceScopedSubscriptionSlot.update.md` | `Front/knowledge/updates/2026-08-08-workspaceScopedSubscriptionSlot.update.md` | User-reported: with 1 Instagram + 0 subscriptions, the 2nd-account dialog never showed. Root cause: `GET /users/me` resolves "current workspace" from the JWT access token's `workspaceId` claim, which can go stale (never re-validated per request; ~100h TTL in non-prod). `connect/page.tsx` now reads `hasAvailableSubscriptionSlot` from `useWorkspaces()` (keyed by real membership, immune to the stale claim) instead of `useUser()`. Note: "which workspace is current" still traces back to the same JWT claim (`usePermissions().workspaceId`) — a wider, pre-existing pattern not fixed here. |
| `2026-08-07-workspaceProfileChip.update.md` | `Front/knowledge/updates/2026-08-07-workspaceProfileChip.update.md` | Adds a pill-shaped "Workspace Profile Chip" to the top of the desktop sidebar (`ConsoleSidebar.tsx`, below the logo), showing the current workspace's name, an avatar/overlapping-avatar-group/fallback-initial built from its connected Instagram accounts, and a handle-or-count summary; the whole chip opens the existing `WorkspaceDrawer` on click (no new switcher logic). New `WorkspaceProfileChip.tsx` + `utils/avatarColor.ts` (deterministic fallback-avatar color hash), new `Console.Sidebar.{accountsConnected,noInstagramConnected}` i18n keys. Implemented directly on `merged-admin` per explicit instruction, not yet manually verified in-browser. |
| `2026-08-07-userDetailsCardSimplified.update.md` | `Front/knowledge/updates/2026-08-07-userDetailsCardSimplified.update.md` | Simplifies the sidebar footer `UserDetailsCard.tsx` now that `WorkspaceProfileChip` covers workspace identity/switching: drops the workspace-switcher popover, per-account Instagram list, and subscription progress bar, down to three rows — profile icon + name, mobile/email, and two same-`variant="outline"` buttons ("ویرایش" → `/settings`, "خروج" → existing logout flow). New `Console.Dashboard.edit` i18n key; reuses `Console.Sidebar.logout`. Implemented directly on `merged-admin`, not yet manually verified in-browser. |
| `2026-08-08-settingsInstagramSubscriptionGate.update.md` | `Front/knowledge/updates/2026-08-08-settingsInstagramSubscriptionGate.update.md` | Moves the subscription-setup gate trigger from `/connect` (post-navigation) to the "Add account" button on `/settings/instagram` itself — clicking it with an existing Instagram + no available subscription slot opens `SetupInstagramDialog` in place instead of navigating; otherwise still links to `/connect` as before. `/connect`'s own gate is untouched (still covers direct navigation/OAuth callback). |
| `2026-08-08-setupInstagramDialogRedesign.update.md` | `Front/knowledge/updates/2026-08-08-setupInstagramDialogRedesign.update.md` | Restyles `SetupInstagramDialog` to visually match (not duplicate) `ChoosePlan.tsx`'s `/settings/subscription` buy dialog: gradient header, `rounded-2xl` duration cards with "best value" badge + monthly-price/savings display, dedicated per-duration "Buy" button, same treatment for the manual fallback plan grid. Reuses `Subscription` namespace i18n strings; no new keys. No purchase-flow logic changed. |
| `2026-08-09-connectUnboundPlanChoice.update.md` | `Front/knowledge/updates/2026-08-09-connectUnboundPlanChoice.update.md` | User-reported dead end: a workspace holding an **unbound paid subscription** whose follower tier does not fit a new page could never connect it. `hasAvailableSubscriptionSlot` is tier-blind, so `/connect` skipped `SetupInstagramDialog` and showed only the OAuth link; the tier check happens later in `bindOnConnect` (step 3), which throws `SUBSCRIPTION_NOT_COMPATIBLE_WITH_FOLLOWER_COUNT` and rolls back — and `useConnectInstagram` only toasts, leaving the page unchanged, so the user retried forever with no way to buy the plan the toast asked for. `/connect` now shows the unbound plan(s) with their tier badge plus two actions: continue with this plan (unchanged OAuth link) or خرید پلن دیگر (opens the existing `SetupInstagramDialog`). Detection uses `getUnboundActiveSubscriptions`, which excludes credit — credit is follower-count-blind and can never mismatch, so it still goes straight to OAuth. Frontend-only, no backend change; `/settings/instagram` needs none since its button only forwards to `/connect` in this state. 10 tests pass. |
| `2026-08-09-setupDialogUnboundStep.update.md` | `Front/knowledge/updates/2026-08-09-setupDialogUnboundStep.update.md` | **Supersedes the UI placement of `2026-08-09-connectUnboundPlanChoice.update.md`** (same dead end, wrong surface — the first attempt rendered the choice inline on `/connect`, splitting one journey across page and dialog). The continue-or-buy decision now lives inside `SetupInstagramDialog` as a new **first step**, shown only when the workspace holds an unbound paid plan: an amber warning that the plan activates only on an account inside its follower range, each plan listed with its `PlanTierBadge` tier, then «ادامه با همین اشتراک» (links to Instagram OAuth) or «خرید پلن دیگر» (sets `dismissedUnboundStep` and drops into the existing username → follower-lookup → plan flow, unchanged). With no unbound paid plan the dialog opens on the username input exactly as before. `/connect`'s only remaining change is the open condition (`needsSubscriptionSetup || hasUnboundPlan`), since the dialog was unreachable in that state. New `utils/instagramOAuthUrl.ts` shares the OAuth URL so page and dialog cannot drift on scope/redirect_uri. i18n keys moved from `Connect` to `SetupInstagramDialog`. 18 tests pass. |
| `2026-08-10-instagramWorkspacePickerWizard.update.md` | `Front/knowledge/updates/2026-08-10-instagramWorkspacePickerWizard.update.md` | `SetupInstagramDialog` becomes a 3-step wizard (username → plan/cost review → workspace choice), letting the user send a 2nd+ Instagram page to a new or different existing workspace instead of always the current one. New `useWorkspaceTargetStep` and `useInstagramWizardResume` hooks isolate the branching/resume logic from the Radix UI; the switch survives its `window.location.reload()` via URL-stamped resume state and is verified against `usePermissions().workspaceId` before payment ever fires. |
| `2026-08-09-settingsAddInstagramGate.update.md` | `Front/knowledge/updates/2026-08-09-settingsAddInstagramGate.update.md` | Closes the first two known gaps of `2026-08-09-setupDialogUnboundStep.update.md`. `/settings/instagram`'s «افزودن اکانت» button only asked half the question (`hasInstagram && !hasAvailableSubscriptionSlot`), so an **unbound paid plan** — which makes that flag true — rendered a plain `/connect` link and bounced the user through `/connect` just to reach the dialog its own button could have opened. New shared hook **`hooks/useAddInstagramGate.ts`** now owns the rule for both surfaces and returns `needsSubscriptionSetup` / `hasUnboundPlan` / `requiresSetupDialog` / `isLoading`; `hasInstagram` stays a parameter because `/connect` sources it from `useUser` while settings uses the accounts list. Behaviour: first account → `/connect`; second-or-later with no slot **or** an unbound paid plan → `SetupInstagramDialog` in place; credit-only or already-bound plan → `/connect`. Also fixes the loading race — `InstagramAccounts.onCountChange` now reports `number \| null` from a `useEffect` on the SWR loading flag instead of `onSuccess` (so a *failed* fetch resolves too), and the button is disabled while the count or gate is unknown, since a half-loaded "no accounts" answer is the one state that skips every check. `/connect`'s behaviour is unchanged; it just reads the same hook. No new i18n keys. 30 tests pass (12 new), mutation-checked. **Same-day follow-up in the same doc**: the dialog's «ادامه با همین اشتراک» button no longer links straight to `IG_OAUTH_URL` — it now routes to `/connect` so the user gets that page's instructions and help video. A plain `/connect` link would loop (the dialog also renders *on* `/connect`, whose gate is still unanswered, so it would reopen itself), so the button carries the answer: `CONTINUE_WITH_PLAN_HREF` = `/connect?continueWithPlan=1`, and `useAddInstagramGate(hasInstagram, { unboundPlanAccepted })` clears **only** `hasUnboundPlan` — `needsSubscriptionSetup` still gates, since no query param should talk a workspace with zero coverage past it. The button also calls `onOpenChange(false)`, because a `/connect` → `/connect` navigation keeps the route mounted and the dialog would otherwise cover the page. 33 tests pass. |
| `2026-08-08-teamMembersSettingsPage.update.md` | `Front/knowledge/updates/2026-08-08-teamMembersSettingsPage.update.md` | Moves team management from the `/workspace` page into a new `/settings/team` page (reachable via a new sidebar sub-item under Settings, alongside a "تنظیمات عمومی" sub-item preserving one-click access to the `/settings` hub). `TeamManager.tsx`'s active-members list switched from one unpaginated `useSWR` call to a paginated + searchable `useSWRImmutable` call, following the `Orders/OrdersCardList.tsx` pattern. `/workspace/page.tsx` now shows only the profile card. Paired Back doc: `2026-08-08-teamMembersPagination.update.md`. |
| `2026-08-09-settingsRestructure.update.md` | `Front/knowledge/updates/2026-08-09-settingsRestructure.update.md` | Replaces the app's two competing settings navigations (main-sidebar entries + the in-page `SettingsOptions` card list) with one set of five ordered, icon-labeled Settings sub-items: اطلاعات کسب‌وکار, پیج‌های متصل, خرید اشتراک, اعضای تیم, اطلاعات بانکی. New `/settings/workspace` page replaces the Telegram-style `/workspace` profile card with standard settings chrome — invitation/transfer banners, create-workspace dialog, rename form, new `WorkspaceCategoryForm` (`PATCH /workspaces/:id { categoryId }`; note the workspace *reads* its category as nested `category.id` but *writes* a flat `categoryId`), and a danger zone holding ownership transfer + delete. `/workspace` and `/settings` became redirects; `/workspace/[memberId]/permissions` is untouched and still used by `TeamManager`. `NavMain` sub-items gained optional `icon`/`badge` (badges previously rendered only on non-collapsible top-level items, so the pending-invitations dot would have vanished silently). Profile, password and sessions merged into one tabbed `/settings/profile`. Frontend-only — `PATCH /workspaces/:workspaceId` already accepted `categoryId` and `GET /workspace-categories` already existed. |
| `2026-08-09-settingsDesignConsistency.update.md` | `Front/knowledge/updates/2026-08-09-settingsDesignConsistency.update.md` | Settings design-consistency fix — workspace/subscription pages now use standard Button/Input tokens and width caps instead of ad-hoc overrides. |
| `2026-08-10-breadcrumbTranslations.update.md` | `Front/knowledge/updates/2026-08-10-breadcrumbTranslations.update.md` | Fixes hardcoded/untranslated breadcrumb text in both apps. Admin's `app-breadcrumb.tsx` returned the literal Persian string `'جزئیات'` for UUID segments instead of `t('detail')`; now fixed, plus a missing `add` segment (`/templates/add`). Dashboard's `HeaderBreadcrumb.tsx` was missing map entries for real routes `support`, `install`, `invitations`, `sendAll` (silently fell through to the raw un-translated URL slug), plus a dead reference to a nonexistent `Breadcrumbs.edit` key. All added to `fa.json` (`Breadcrumbs` namespace for dashboard, `Sidebar` namespace for admin — `en.json` intentionally left for later per convention). The `decodeURIComponent` fallback itself is kept for genuinely dynamic, non-UUID entity identifiers; see `CLAUDE.md` for the standing rule that every new *static* route segment must be added to the map + `fa.json`. |
| `2026-08-11-continuePlanDirectOAuth.update.md` | `Front/knowledge/updates/2026-08-11-continuePlanDirectOAuth.update.md` | User-reported extra hop: reverts the `/connect`-routing part of `2026-08-09-settingsAddInstagramGate.update.md`'s same-day follow-up. `SetupInstagramDialog`'s «ادامه با همین اشتراک» button now links straight to `IG_OAUTH_URL` again — same target as the plain «اتصال اکانت» button — instead of `/connect?continueWithPlan=1`, which only got the user to a page where they had to click connect a second time. Removed the now-dead `CONTINUE_WITH_PLAN_PARAM`/`CONTINUE_WITH_PLAN_HREF` exports and the `unboundPlanAccepted` option on `useAddInstagramGate` (`requiresSetupDialog` now derives straight from `needsSubscriptionSetup \|\| hasUnboundPlan`). 28 tests pass. **Same-day addendum**: merged in `fix/instagram-wizard-ui-polish` (previously uncommitted work in its own worktree, committed here per user request) — bounds `SetupInstagramDialog` to a fixed height with internal per-step scroll (fixes the dialog's layout-shift-as-content-loads), forwards the wizard-resume `igw*` params through `AuthProvider`'s auth-guard redirect (was silently dropping them right after a workspace-switch reload), and removes the superseded `pendingInstagramConnect.ts` cookie-reminder feature. Clean 3-way auto-merge, no conflicts. 33 tests pass. **Follow-up**: fixed a code-review-found gap where the igw suffix was appended as a sibling param instead of nested inside a `returnTo` target, silently dropping it one hop later through the invitations/transfer picker. **Follow-up**: fixed `/settings/subscription/verify` being silently redirected away for zero-Instagram users returning from a payment gateway (`AuthProvider`'s `!hasInstagram` branch had no allowlist entry for it, unlike `/settings/instagram?code=`). 37 tests pass. |
| `2026-08-11-mobileDrawerAddPageOpensDialog.update.md` | `Front/knowledge/updates/2026-08-11-mobileDrawerAddPageOpensDialog.update.md` | Mobile workspace drawer's «افزودن پیج» button used to go straight to `/connect`, skipping the same-day-earlier `useAddInstagramGate` gate (`2026-08-09-settingsAddInstagramGate.update.md`). Now routes to `/settings/instagram?openAdd=1` (new `AUTO_OPEN_ADD_PARAM` export); the page auto-replays its own Add button's click branch once the gate resolves — opens `SetupInstagramDialog` or continues to `/connect`, whichever the gate says. 32 tests pass. |
| `2026-08-13-howFoundUs.update.md` | `Front/knowledge/updates/2026-08-13-howFoundUs.update.md` | Asks «چطور با بفروش آشنا شدید؟» once, in a closable dialog opened on the way into creating an automation, and never again once answered — deliberately **absent** from `/settings/profile`, which is the whole point (opt-in placement would never get filled). `useBusinessInfoGate` + a zustand store hold the pending create URL; the dialog saves via `POST /users` then continues the navigation. **A loading or errored `/users/me` never gates** — trapping a user behind a dialog because our own read failed costs more than one missed answer. All **six** create entry points route through the hook (automations header button, dashboard card, empty-state wizard CTA, mobile bottom-nav `+`, template dialog ×2, duplicate-automation); `<Link>`-based ones keep their real `href` and only `preventDefault()` when the gate must open, so open-in-new-tab still works. **SWR gotcha:** `GET /users/me` is keyed twice — `useUser` relative, `ProfileForm` absolute — so the dialog mutates both or the gate re-fires on a stale null. Admin side adds `FilterHowFoundUs` on the users table (`&howFoundUs=a,b`), the same selection on `POST /users/excelExport`, and a read-only row on the user detail page. Also **fixes** two pre-existing repo problems found on the way: admin `fa.json` declared a top-level `Jobs` key **twice** (lines 125 and 1449 — JSON keeps the last, so the first 53-line block was dead; both were byte-identical, so deleting it left the parsed messages unchanged) and **51 tracked entries under `packages/ui/node_modules`** (47 pnpm symlinks + 4 `.bin` shims pointing into content-hashed `.pnpm/<hash>` paths — broken on a fresh clone, rewritten by every dependency bump; `.gitignore` already listed the directory, but gitignore does not apply to files already in the index, so they needed `git rm -r --cached`). **Trap worth remembering:** editing a messages file by round-tripping it through a JSON parser silently collapses a duplicate key and takes ~50 unrelated lines with it — it happened here and had to be reverted; edit as text. Paired backend: `Back/knowledge/updates/2026-08-13-howFoundUs.update.md`. |
| `2026-08-13-productPriceMaxBound.update.md` | `Front/knowledge/updates/2026-08-13-productPriceMaxBound.update.md` | Frontend half of the backend `bigint` money-column fix (`Back/knowledge/updates/2026-08-13-moneyColumnsToBigint.update.md`). Saving a product priced above ~2.1B Toman used to fail with an untranslatable message: the backend column was `integer`, so the value overflowed at Postgres and returned a raw `QueryFailedError`, which has no `code` for `t_ec(...)` to look up. The form had no upper bound either — `superRefine` only checked the **lower** bound (`< 1000`) and discount-vs-price. Adds `MAX_MONEY_AMOUNT = 999_999_999_999` (`src/constants/money.constant.ts`, must stay in sync with `Back/apps/core/src/common/constants/money.const.ts`), two new `superRefine` checks on `price`/`discountPrice` in `ProductForm.tsx`, the matching `Products.Form.{price,discountPrice}.errors.tooLarge` keys in `fa.json`, and `ERROR_CODES` entries `PRODUCT_PRICE_TOO_LARGE` / `PRODUCT_PRICE_SHOULD_BE_POSITIVE` / `PRODUCT_DISCOUNT_PRICE_TOO_LARGE`. Note: the cap is interpolated with `toLocaleString('en-US')`, **not** the project's `formatNumber` helper, whose declared `string \| number \| null` return type is not assignable to a next-intl message value. |
| `2026-08-13-automationCardFooterAlignment.update.md` | `Front/knowledge/updates/2026-08-13-automationCardFooterAlignment.update.md` | Fixes the automations card list (`/automations`) showing the bottom action bar (answers/edit/copy/delete) at different heights across one grid row — visible when a titled and an untitled automation sit side by side, and when an automation is assigned to an Instagram post. The grid already stretched every card to the row height, but `Card` is `flex flex-col` and `CardContent` had no `flex-1`, so the content kept its natural height and the footer floated up with the dead space left **below** it. Fix pins the footer instead of patching each height source: `h-full` on `Card`, `flex-1` on `CardContent`, `h-full` on the inner content row. Plus stabilisers for the remaining jitter — `min-w-0` + `truncate` keep a long Instagram username list on one line, `min-h-6` reserves one badge row for automations with no conditions, `shrink-0` protects the `w-20` post thumbnail. New `AutomationCard.layout.test.tsx` (7 tests, watched red first — 6 fail without the fix) asserts the pinning classes and that `CardFooter` is the card's last child across five content shapes; jsdom has no layout engine, so this is class-level, not pixel-level. Frontend-only, no new i18n keys. |
| `2026-08-14-settingsMobileScroll.update.md` | `Front/knowledge/updates/2026-08-14-settingsMobileScroll.update.md` | Fixes three mobile scroll defects on `/settings/*`, all rooted in two shared files rather than the pages. **(1)** `SidebarInset` reserved room for the fixed `NavBottom` with `pb-14`, but it is also the mobile scroll container — padding sits **inside** the scroll box so it never shrinks a height-filling child, and Blink omits that end padding from `scrollHeight` when the overflow comes from an `overflow:visible` descendant. `settings/layout.tsx` compounded it with `h-full` (100% of the box while the 56px header was already in it). Result: `/settings/instagram`, `/card`, `/zarinpal` had 56px that **no gesture could reveal**, even at maximum scroll. **(2)** `LayoutSettings` added a second `overflow-y-auto`, so `/settings/workspace`, `/subscription`, `/profile`, `/team` ran **nested** scrollers — the inner one ran out first and the last 56px needed a separate gesture on the outer one, which also dragged the header off screen. **(3)** `DialogContent` is `fixed` + vertically centred with `max-height: none` and `overflow: visible`, so a dialog taller than the screen hung off **both** edges with **no scroll container anywhere** (measured `top: -558` at 975px on an 844px viewport) — title and first fields unreachable on every base dialog. Fix takes nav clearance out of **height** instead of padding: `SidebarInset` → `max-h-[calc(100svh-3.5rem)]`, `pb-14` dropped; `settings/layout.tsx` → `flex-1` + `md:flex-none`; `LayoutSettings` → `md:overflow-y-auto` only; `DialogContent` → baseline `max-h-[90dvh] overflow-y-auto`; `ChoosePlan` → `90vh` → `90dvh`. **`dvh` not `vh`** throughout: on a real phone `vh` measures the *large* viewport, so a 90vh dialog still overflows while the URL bar shows. Desktop untouched — at `md` the pre-existing `md:max-h-screen` / `md:pb-0` variants already win. Verified in device mode across all 7 settings sub-pages (scrollers 2→1, nothing under the nav) plus `/automations` and `/orders` as regression checks. |
| `2026-08-15-shopWorkspaceScoping.update.md` | `Front/knowledge/updates/2026-08-15-shopWorkspaceScoping.update.md` | Frontend half of the shop-module workspace-scoping fix (`Back/knowledge/updates/2026-08-15-shopWorkspaceScoping.update.md`). `GET /shops/:shopId` moved payment details from `shop.user.paymentDetail` to `shop.workspace.paymentDetail` when Instagram stopped being user-owned, but the dashboard was never updated — and `IShop` still declared `user`, so **TypeScript agreed with the stale reads and flagged nothing**. Renames the type (`IShop.user` → `IShop.workspace`, interface `User` → `Workspace`) and all 8 read sites in `order/components/payment.tsx` (6) and `components/Shop/CheckoutPage.tsx` (2). Adds `ERROR_CODES.EXCEL_EXPORT_WORKSPACE_REQUIRED` to `fa.json` for the new backend guard. **Deploy-coupled — Back and Front must ship together**; either side alone leaves the checkout with no payment methods. Note the page failed earlier than this in practice: `GET /shops/:shopId` itself threw backend-side, so the shop never loaded at all. Vitrin CRUD (`/vitrin`), which the dashboard calls from `products/[id]/product.tsx` and `ProductForm.tsx`, was restored backend-side with no frontend change needed. |
| `2026-08-15-commentStartSelfGatesGuard.update.md` | `Front/knowledge/updates/2026-08-15-commentStartSelfGatesGuard.update.md` | Fixes a comment automation whose **first content is a QUESTION** being impossible to save — submit was blocked by the toast «در حالت کامنت، پیام درخواست شروع ضروری است» pointing at a field that was not on screen, so the user could never satisfy it. Root cause was drift between two copies of the same rule: BEF-162 (`2026-08-01-smartConsentDisable.update.md`) taught `StartAutomationMessage.tsx` to hide the start-request card when `content[0]` self-gates (a `QUESTION`, or a `TEXT` with quick replies) **and clear `commentStartText` to `''`**, but left `AutomationForm.tsx`'s `handleBeforeSubmit` guard on the old inline condition, which still rejected the now-blank value — a deadlock, with `setError`/`setFocus` aimed at an unrendered field. **The backend was already correct and needed no change**: `handleComment` (`Back/apps/core/src/contentCycle/contentCycleMessage.service.ts`) computes its own `firstContentSelfGates` and sends the start-request private reply only when neither `isSingleMessage` nor `firstContentSelfGates` holds, so it never reads `commentStartText` for a QUESTION-first automation — the frontend was demanding a value the backend would never use. Fix extracts the decision into one shared predicate, `packages/ui/src/automation-builder/utils/commentStart.ts` (`firstContentSelfGates` + `isCommentStartMessageRequired`), and points **both** the field's activation effect and the submit guard at it so they cannot drift again. Note the helper's param type is deliberately looser than `AutomationFormType` (every field optional there). **Same pass also fixes a second, opposite defect**: `justFollowers` was treated as a blanket exemption from needing `commentStartText`, but `followerGuard` (`Back/apps/core/src/contentCycle/follower.service.ts`) returns `countinue: true` for a comment only when `isUserFollowBusiness && contents.length === 1 && !reminderTime` — so one shape does reach the start message and still sends it: a **single PRODUCT content with no reminder** (a lone PRODUCT keeps `isSingleMessage` false because products need the lead authenticated). There the backend sent `commentStartText` while the frontend had guaranteed it was empty. `justFollowers` is now a *narrowing* of which shapes reach the message, not an exemption; `StartAutomationMessage` additionally watches `reminderTime`, and its one visible behavior change is that the card now **shows** for that single-PRODUCT case. **`contents.length > 1` with `justFollowers` is NOT the broken case** — `followerGuard` stops those comments first; only the single-PRODUCT shape is reachable. 15 new util tests encode the backend's truth table branch by branch; a new `AutomationForm.commentStart.test.tsx` renders the real form end-to-end and was confirmed red before the fix, with the blocking toast verified as the *only* validation error involved. 132 `packages/ui` + 34 dashboard tests pass. **Not backfilled**: automations saved before this fix keep their empty `commentStartText`, so a backend guard that skips the send when it is empty is the durable follow-up. |
| `2026-08-16-mainOnlyFixesPort.update.md` | `Front/knowledge/updates/2026-08-16-mainOnlyFixesPort.update.md` | Audit and port of the **8 non-merge commits that existed only on `main`** before `merged-admin` was promoted to `main`. **4 already present** (zibal, `dl.befroosh.app` next/image allowlist, IG-callback raw error code, WAV re-encode — the last already living in `packages/ui`). **1 real gap ported**: `3e9956f2` duplicate verify-payment. This branch had already rewritten the verify page from `useSWRImmutable` to `api.get` in an effect but never carried the guard, so the duplicate stayed reachable another way — the effect depends on `searchParams`, whose object identity can change between renders, and StrictMode double-invokes effects in dev; a `useRef` latch placed **after** the gateway-detection check fixes it without touching the "direct visit, never fake success" path. **1 trap — deliberate divergence, must never be ported**: `main` flip-flopped the auth `next` path (`data.data.next` → `data.data.result.next` → back), and its final state matches **master's** backend where `prelogin` returns the service result raw; `workspace-refactor` wraps it in `ResponseMessage` as `data:{result}`, so `data.data.result.next` — what `merged-admin` already has — is correct, and porting main's flip **would break login**. Confirmed by reading `auth.controller.ts` on both backend branches. **1 deliberately skipped**: `902aaebf` rewrites the `docker-compose-Front*.yml` build context to `/home/cicd/manual-deploy/Sources/Front`; on `main` those files sit at the repo root beside a root `Dockerfile`, but here they moved under `apps/dashboard/` while the root has `Dockerfile.dashboard`/`Dockerfile.admin` — neither value builds under the monorepo layout, so it was left for the deploy rework rather than guessed. Verified: `apps/dashboard` tsc 188 errors before and after, identical set, none in the verify page. Paired backend: `Back/knowledge/updates/2026-08-16-masterOnlyFixesPort.update.md`. |
| `2026-08-18-subscriptionRemainingDaysSum.update.md` | `Front/knowledge/updates/2026-08-18-subscriptionRemainingDaysSum.update.md` | "Remaining days" must sum the active sub's days-left with every RESERVED sub's full `planDuration.durationDays` (a reserved sub always has `expire = null`) — fixes `PageSubscriptionCard.tsx` (only used the active sub's `expire`, now matches `PageCoverageBadge.tsx`'s existing pattern for both the radial's numerator and denominator) and two admin spots (`users/columns.tsx`, `users/[id]/page.tsx`) whose `&& s.expire` filter silently dropped every reserved row before their own reserved-sum branch could run; also widens the user-detail "باقی مانده" row to show whenever there's an active OR reserved sub, not only active. **Same-day follow-up:** the dashboard header (`SubscriptionBoard.tsx`) picked the free-trial automation-count radial vs. the subscription radial from `hasActiveSubscription` (ACTIVE-only), so a reserved-only user (already paid, just queued) wrongly saw the free-trial view and the "promotion is active" banner; replaced with `hasSubscription = active || reserved`. Paired backend: `Back/knowledge/updates/2026-08-18-subscriptionRemainingDaysSum.update.md` (fixes the majority of call sites — the workspace header, admin stats CTEs, and a stray `subscription.userId` reference). |
| `2026-08-19-automationLinkCountRadial.update.md` | `Front/knowledge/updates/2026-08-19-automationLinkCountRadial.update.md` | Dashboard header ad-alert now gates on `freeAutomationQuotaExceeded && isPromotion` per account, not `!hasSubscription`. `ProgressRadial.tsx`'s automation ring turns solid red (`isAutomationOverLimit`) when the count reaches/exceeds the free limit — 5 new vitest cases. `SubscriptionBoard.tsx`'s radial count switched from the live `automationCount` to the monotonic `automationLinkCount` (stays consistent with the sticky ad-alert instead of implying quota room that's already used up); new `automationLinkCount` field on `InstagramNamespace.Account`. Paired backend: `Back/knowledge/updates/2026-08-19-automationLinkCountExposed.update.md` (had to explicitly expose the field first). |
| `2026-08-26-referralPlanGift.update.md` | `Front/knowledge/updates/2026-08-26-referralPlanGift.update.md` | Admin `/referral-codes` create dialog gains the third referral type, `PLAN` (`پلن (هدیه)`) — a code that gifts a subscription instead of a discount. The type existed backend-side all along and core honoured it at signup, but no create path ever accepted `planDurationId`, so such codes could only be hand-inserted into the DB. Adds a cascading plan → duration picker (`useSWR('/plans')` → `useSWR('/plans/planDurations?planId=…')`, same pair as `components/customer/AddSubscriptionDialog.tsx`, both conditional keys so nothing is fetched until `PLAN` is picked), a `useEffect` clearing `planDurationId` when the plan changes (a duration belongs to exactly one plan), and hides `discount`/`سقف تخفیف`/`حداقل تخفیف`/`حداکثر دفعات استفاده` for `PLAN` since the backend forces them to `0 / 0 / null / 1`. `columns.tsx` renders the gifted plan + duration name for `PLAN` rows (which store `discount = 0`) instead of a meaningless `0`, via a new optional `planDuration` field the backend's `read()` now left-joins. Error toast moved off hardcoded per-code strings onto `t_ec` (§10); `ERROR_CODES.REFERRALCODE_ALREADY_EXISTS` added to `fa.json` (`PLAN_DURATION_NOT_FOUND` was already there). **Zod gotcha encoded in a regression test**: `discount` is `z.number().optional()` with the positivity rule in `superRefine`, *not* `.positive().optional()` — a field-level failure aborts the object parse before `superRefine` runs, so the form's default `discount: 0` would have failed validation on an input that is **hidden** for `PLAN`, leaving the form unsubmittable with no visible error; the test was confirmed red against the buggy shape. 9/9 vitest. Paired backend: `Back/knowledge/updates/2026-08-26-referralPlanGift.update.md`. |
