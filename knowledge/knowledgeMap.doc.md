# Front Knowledge Map

This document indexes all documentation files inside the `Front/knowledge` directory.

| File Name / Folder | Path | Description |
|---|---|---|
| `front-back-relations.md` | `Front/knowledge/front-back-relations.md` | Documents the relationships and API dependencies between the Front and the Back projects. |
| `2026-08-19-userDetailsMobileScroll.update.md` | `Front/knowledge/updates/2026-08-19-userDetailsMobileScroll.update.md` | Summary of the admin `/users/[id]` mobile scroll fix: `(main)/layout.tsx` forced `overflow-hidden` on `LayoutPage` at all breakpoints, which (via the flex automatic-minimum-size rule) clipped page content below `md:` instead of letting `SidebarInset`'s own mobile scroll container reach it. Fixed to `md:overflow-hidden`. |
| `2026-09-12-retireLegacyShopCheckout.update.md` | `Front/knowledge/updates/2026-09-12-retireLegacyShopCheckout.update.md` | **The legacy public web checkout is deleted.** `app/(Shop)/` (37 files, ~2.4k lines) and `components/Shop/` are gone, along with the orphaned `types/shops/` and the unused `Checkout` i18n namespace (47 keys, removed from `fa.json`/`en.json` by line-slice text edit — never a JSON round-trip, per the `howFoundUs` duplicate-key incident). `PRODUCT` is removed from the automation form's `contentTypeOptions` in BOTH builder modes; Back refuses the type on create and update, so an un-ported `product` automation cannot be saved until migration Phase 13. `proxy.ts` keeps its `/:shop/:product/order` pass-through and `payments/verify` matcher exclusion **on purpose** — deleting them would redirect buyers on stale, permanently-tappable template buttons to `/auth` instead of a 404. Dashboard tsc 210→206 with a set-level diff showing zero new errors; 965 dashboard + 198 packages/ui tests pass. **Deploy-coupled AND order-dependent** with Back `chore/retire-legacy-shop-checkout` — see the 2026-09-12 row in `before-prod-cutover.md`. |
| `2026-09-11-dashboardStuckLoading.update.md` | `Front/knowledge/updates/2026-09-11-dashboardStuckLoading.update.md` | Why users sat on the dashboard's boot spinner (server HTML is only AuthProvider's spinner; `/users/me` had no timeout/retry; chunk loads that fail before React starts hang forever) and the fix: 20s read timeout + transient-error retry in `fetcher`, single-flight refresh + opt-in session bootstrap (skips the 401 round trip, never in the shop checkout), inline `<head>` chunk-reload script with a 2-per-60s loop guard, and a CSS-only delayed reload link under the spinner. |
| `2026-09-02-globalChunkLoadErrorReload.update.md` | `Front/knowledge/updates/2026-09-02-globalChunkLoadErrorReload.update.md` | First version of the chunk-load auto-reload in `global-error.tsx` (reload once per 10s). Superseded by `2026-09-11-dashboardStuckLoading.update.md`, which moves the guard into the inline `utils/chunkReload.ts` script. |
| `knowledgeMap.doc.md` | `Front/knowledge/knowledgeMap.doc.md` | Indexes and describes all documentation files in this folder. |
| `2026-09-07-staleArvanCdnUrls.update.md` | `Front/knowledge/updates/2026-09-07-staleArvanCdnUrls.update.md` | Summary of replacing 10 dead `*.arvanstorage.ir` media URLs in the dashboard after the 2026-07-23 move to Cloudflare R2 (both Arvan buckets are now deleted). The 2 "how to connect" help-video refs were a pure host swap (object already on R2, trailing empty `?versionId=` dropped); the 8 automation-wizard hint videos lived on the `testz` bucket and were never copied, so the sources were recovered locally, remuxed `.MOV`→`.mp4` (`-c copy -movflags +faststart`) and uploaded to `befroosh` R2 under `site/hints/` with an immutable 1-year cache — **so replacing a hint video needs a new filename, not an overwrite**. `next.config.mjs`'s Arvan `images.remotePatterns` entry is deliberately kept: it covers pre-R2 `file.url` DB rows, not code. |
| `updates/` | `Front/knowledge/updates/` | Folder containing date-stamped markdown updates for individual features and bug fixes. |
| `2026-09-08-adminCodeUsageCounts.update.md` | `Front/knowledge/updates/2026-09-08-adminCodeUsageCounts.update.md` | Summary of adding user-count columns to the admin referral-codes and discount-codes tables: `referral-codes/columns.tsx` gets one "تعداد کاربران" column (`userCount`), `discount-codes/columns.tsx` gets two ("تعداد کاربران" `userCount` + "تعداد استفاده" `usageCount`, which differ when `maxUsagePerUser > 1`). Backed by new fields on the paired `GET /referral-codes`/`GET /discount-codes` responses (see `Back/knowledge/front-back-relations.md`). |
| `2026-07-12-subscriptionDisplayPolish.update.md` | `Front/knowledge/updates/2026-07-12-subscriptionDisplayPolish.update.md` | Summary of subscription validity merging and reserved subscription UI polish. |
| `2026-07-12-userDetailsCardSubscriptionInfo.update.md` | `Front/knowledge/updates/2026-07-12-userDetailsCardSubscriptionInfo.update.md` | Summary of removing the top-level credit/renewal row and displaying remaining subscription days next to Instagram profiles. |
| `2026-07-13-mobileExportFormsResponsiveness.update.md` | `Front/knowledge/updates/2026-07-13-mobileExportFormsResponsiveness.update.md` | Summary of the shared Drawer/DialogStyled scroll fix and the date-picker font-size fix (iOS Safari zoom-on-focus) across the Sessions/Orders/Contacts excel-export forms and the Contacts edit dialog. |
| `2026-07-13-hideSubscriptionFromWebView.update.md` | `Front/knowledge/updates/2026-07-13-hideSubscriptionFromWebView.update.md` | Summary of detecting the Android WebView via the UA `wv` marker and hiding credit-type subscription info plus all navigation to `/settings/subscription` in that context. |
| `2026-07-13-installSection.update.md` | `Front/knowledge/updates/2026-07-13-installSection.update.md` | Summary of the new `/install` page (Android store links / iOS PWA-install steps by device) and its sidebar entry. |
| `2026-07-13-automationCopy.update.md` | `Front/knowledge/updates/2026-07-13-automationCopy.update.md` | Summary of the "Copy" button on automation cards that pre-fills `/automations/add` from an existing automation's full content (including Instagram accounts), always creating a new, independent automation. |
| `2026-07-13-accountSessions.update.md` | `Front/knowledge/updates/2026-07-13-accountSessions.update.md` | Summary of the new `/settings/account-session-management` ("نشست‌های فعال حساب") page that lists the user's active login sessions (`GET /auth/sessions`), pins the current device, and terminates sessions older than 5 days (`DELETE /auth/sessions/:id`). |
| `2026-07-14-sidebarNavPendingSpinner.update.md` | `Front/knowledge/updates/2026-07-14-sidebarNavPendingSpinner.update.md` | Summary of swapping the sidebar/bottom-nav icon and dashboard home stat-card icons for a spinning `CircleNotchIcon` while a clicked link's target route is pending, via Next.js's `useLinkStatus` hook. |
| `2026-07-14-workspaceCategories.update.md` | `Front/knowledge/updates/2026-07-14-workspaceCategories.update.md` | Summary of the workspace categories feature: admin CRUD page + category column/filter on the admin workspaces page, and the dashboard's required category on workspace creation/onboarding plus the forced-category guard for legacy workspaces. |
| `2026-07-14-bannerCarousel.update.md` | `Front/knowledge/updates/2026-07-14-bannerCarousel.update.md` | Summary of the banner carousel feature: admin CRUD page for banners with category multi-select, and the dashboard carousel component that fetches active banners, shuffles them, and auto-advances every 5s. |
| `workspace-categories/` (admin) | `Front/apps/admin/src/app/(main)/workspace-categories/` | Admin CRUD page for the workspace-category taxonomy: `page.tsx` renders `client-page.tsx`, which drives a paginated/searchable `/workspace-categories` SWR query (matching the `workspaces` page's list style) and passes data to `workspace-categories-table.tsx` (search input, add/edit dialog with `nameFa`/`nameEn` fields via `react-hook-form` + `zod`, delete with confirm) and `columns.tsx` (`useWorkspaceCategoryColumns` — `nameFa`, `nameEn`, `workspaceCount`, `createDate`, row actions). |
| `banners/` (admin) | `Front/apps/admin/src/app/(main)/banners/` | Admin CRUD page for banners: `page.tsx` renders `client-page.tsx`, which drives a paginated/searchable `/banners` SWR query and passes data to `banners-table.tsx` (search input, add/edit dialog with title/description bilingual fields, buttons field array, and category multi-select via `react-hook-form` + `zod`, delete with confirm) and `columns.tsx` (`useBannerColumns` — `title`, `category`, `isActive`, `order`, `createDate`, row actions). |
| `workspaces/{columns,workspace-table,client-page}.tsx` (admin, category addition) | `Front/apps/admin/src/app/(main)/workspaces/` | The existing admin workspaces list gained a `category` column (`columns.tsx`, shows `category.nameFa` or a "no category" placeholder) and a category filter `Select` (`workspace-table.tsx`, sourced from `/workspace-categories?limit=100`); `client-page.tsx` holds the `categoryId` filter state and appends it to the `/workspaces` query string. |
| `useWorkspaceCategories.ts` | `Front/apps/dashboard/src/hooks/useWorkspaceCategories.ts` | SWR hook wrapping `GET /workspace-categories` (`{ id, nameEn, nameFa }[]`), shared by the "new workspace" creation dialog, the onboarding form, and `WorkspaceCategoryGuard` — the single source of the category list on the dashboard. |
| `WorkspaceCategoryGuard.tsx` | `Front/apps/dashboard/src/components/Console/WorkspaceCategoryGuard.tsx` | Non-cancelable dialog (no close button, `Escape`/outside-click suppressed) mounted in `(Console)/layout.tsx` next to `WorkspaceSessionGuard`. Renders only when the active workspace has no `category` **and** the current user is its `ownerId` (non-owners are never blocked); submits `PATCH /workspaces/:id` with the chosen `categoryId` and revalidates `useWorkspaces()`. |
| `useActiveBanners.ts` | `Front/apps/dashboard/src/hooks/useActiveBanners.ts` | SWR hook wrapping `GET /banners/active` (`{ id, titleEn, titleFa, descriptionEn, descriptionFa, color, buttons: [{ id, textEn, textFa, url, isExternal }] }[]` — no `categoryId(s)`/`isActive`, by design: the endpoint never reveals why a banner was selected), shared by the dashboard home carousel component. Handles error states and provides fallback empty array. |
| `DashboardBannerCarousel.tsx` | `Front/apps/dashboard/src/components/Console/Dashboard/DashboardBannerCarousel.tsx` | Carousel component that renders active banners fetched from `useActiveBanners`, using `StableCarousel` (with `StableCarouselPrevious`/`StableCarouselNext` nav buttons) with shuffle-once-per-load and auto-advance every 5s. Each banner displays bilingual title/description, background color, and clickable buttons (internal page links or external URLs). Replaces the deleted `DashboardHelpBanner.tsx` in the dashboard home. |
| `2026-07-14-freeAutomationQuotaWarningDialog.update.md` | `Front/knowledge/updates/2026-07-14-freeAutomationQuotaWarningDialog.update.md` | Summary of the `AutomationForm` confirmation dialog that warns before the automation that would push a selected Instagram page over its free automation quota, using new `automationCount`/`freeAutomationLimit`/`freeAutomationQuotaExceeded` fields on `GET /instagram/accounts` (paired Back branch `feat/free-automation-quota`). |
| `2026-07-14-templateSystemFrontend.update.md` | `Front/knowledge/updates/2026-07-14-templateSystemFrontend.update.md` | Summary of the Template System feature: the shared `packages/ui/src/automation-builder` extraction (`AutomationBuilder` with `mode='automation'\|'template'`), the admin `/templates` CRUD pages, the dashboard Create-Automation-from-template picker, `?templateId=` prefill, and the in-form "Template" content-type insert. |
| `2026-07-16-delayContentSelectbox.update.md` | `Front/knowledge/updates/2026-07-16-delayContentSelectbox.update.md` | Summary of the shared 23-hour delay budget feature: `DelayContent.tsx`'s free-text magnitude input replaced by a budget-aware `<Select>`, `Contents.tsx` blocking a new DELAY item once the budget is used up (`DelayBudgetExhaustedDialog`), and an admin `TemplateForm.tsx` submit-time 23h-sum guard added for parity with the dashboard's existing check. Frontend-only, no backend changes. |
| `2026-07-17-automationTextareaAutogrow.update.md` | `Front/knowledge/updates/2026-07-17-automationTextareaAutogrow.update.md` | Summary of the auto-growing textareas in the automation contents editor (4-row floor, 12-row cap, JS scrollHeight measurement instead of CSS field-sizing). |
| `2026-07-17-instagramPostButton.update.md` | `Front/knowledge/updates/2026-07-17-instagramPostButton.update.md` | Summary of the "Instagram Post" button type: reuses the "پست خاص" select-post dialog (generalized to a headless/controlled mode) to fill a URL button's link from a picked post's permalink, with the same single-Instagram-account guard, no backend changes. |
| `automation-builder/` (`@befroosh/ui`) | `Front/packages/ui/src/automation-builder/` | Shared automation-authoring builder, extracted from the dashboard's `AutomationForm` so both the dashboard (real automations) and the admin templates UI render the same form. `AutomationBuilder.tsx` owns the `react-hook-form` instance (`AutomationFormSchema` from `schemas/automationForm.ts`) and takes a `mode: 'automation' \| 'template'` prop (`'template'` renders only Conditions/Triggers/Contents, skipping JustFollowers/comment-trigger — templates have no workspace/live-automation context), an `apiClient: { upload, get }` (`types/apiClient.ts`) the caller injects (dashboard: `apps/dashboard/src/lib/automationApiClient.ts`; admin: `apps/admin/src/lib/templateAutomationApiClient.ts`), plus `initialValue`/`onSubmit`/`beforeSubmit`/`onInvalid`/`headerSlot`/`helpSlots`/`hasInstagram`/`isPromotion`/`commentRepliesSlot`. `Contents/` holds every content-type editor plus `remapTemplateContents.ts` (strips server ids/timestamps/back-refs and workspace-scoped product/Instagram-post refs, re-derives `delayUnit`/vitrin image fields/button `postbackPayloadType`, so a template's `contents[]` can be safely appended into another automation/template). `TemplatePicker/` (`TemplatePicker.tsx` + `TemplateCard.tsx`) is a shared search+grid (Dialog/Drawer) reused by the dashboard's create-automation flow and the in-form "Template" content-type insert (`Contents.tsx`, hidden in `mode="template"` and in reminder-mode Contents trees). `utils/delayBudget.ts` is a pure helper computing the shared 23-hour DELAY-item budget math (`remainingDelayBudgetMs`, `delayUnitOptionsCount`) consumed by `DelayContent.tsx` (magnitude `<Select>` option range) and `Contents.tsx` (blocking a new DELAY item once the budget is exhausted) — see `2026-07-16-delayContentSelectbox.update.md`. **Updated 2026-08-02:** `QuestionContent.tsx`'s `validationType` dropdown (`types/validationType.enum.ts`) gained an `Image` option — restricts the lead's DM reply to Instagram image attachments (paired Back change validates `message.attachments`, not `message.text`). See `2026-08-02-questionImageValidation.update.md`. **Updated 2026-08-02 (same day, PostalCode):** the same dropdown gained a `PostalCode` option (Iranian postal code regex validation, paired Back change, no attachments involved — a plain text validator like `Mobile`/`NationalCode`). See `2026-08-02-questionPostalCodeValidation.update.md`. **Updated 2026-08-03 (BEF-164):** `DelayContent.tsx`'s number/unit `Select` fields swapped to render number-first (was reading "hour 1" in the RTL UI instead of "1 hour"); `MediaUploader.tsx`'s audio `acceptedFormats` gained `audio/mp3`. See `2026-08-03-bef164AutomationDelayAndMp3Fix.update.md`. **Updated 2026-08-31 (Buy in Direct):** new `BUY_IN_DIRECT` content type — `Contents/BuyInDirectContent.tsx` (product picker + dnd-kit reorder, `{ mode, index, apiClient }` + `useFieldArray`, same convention as `ProductContentComp`), a `superRefine` "at least one product" branch in `schemas/automationForm.ts` (paired with a Back-side non-emptiness + ownership check added 2026-09-01), and a `buyInDirectProducts` strip in `remapTemplateContents.ts`. Paired Back feature: `Back/knowledge/core/commerce/instagramDmCheckout.doc.md`. See `2026-08-31-buyInDirectAutomationContent.update.md`. |
| `templates/` (admin) | `Front/apps/admin/src/app/(main)/templates/` | Admin CRUD pages for templates, mirroring the `workspace-categories` admin pattern: `page.tsx`/`client-page.tsx` drive a paginated/searchable `/templates` SWR query into `templates-table.tsx` (thumbnail, title, description, categories/"applies to all" badge, row actions) + `columns.tsx`. `add/page.tsx` and `[id]/page.tsx` both render `TemplateForm.tsx`, which wraps `AutomationBuilder mode="template"` with a `headerSlot` holding a separate `metaForm` (title, description, "applies to all categories" switch + category multi-select from `/workspace-categories`, thumbnail upload via `POST /templates/:id/upload-image`) merged into one payload at submit (`POST`/`PATCH /templates[/:id]`). Editing prefills from `GET /templates/:id`'s raw `ContentCycle` graph via `transformTemplateToFormValues`. |
| Dashboard Create-Automation-from-template | `Front/apps/dashboard/src/components/Automations/CreateAutomationTemplateDialog.tsx`, `Front/apps/dashboard/src/app/(Console)/automations/add/page.tsx`, `Front/apps/dashboard/src/components/Automations/AutomationForm.tsx` | As of `2026-07-18-automationSkipTemplatePicker.update.md`, `automations/page.tsx`'s "create automation" button no longer opens `CreateAutomationTemplateDialog` — it (and the draft-flow "create new" branch) navigates straight to plain `/automations/add`, same as the dialog's old "start from scratch" button and the dashboard quick-action link. `CreateAutomationTemplateDialog.tsx` (listing `GET /templates?search=` via the shared `TemplatePicker`, selecting one navigates to `/automations/add?templateId=<id>`) and the in-form "Template" content-type insert (`Contents.tsx`) still use `TemplatePicker` — only the create-automation entry point stopped opening this dialog. The `add/page.tsx` route reads and UUID-validates `?templateId=` alongside the existing `?copyFrom=`; `AutomationForm.tsx` accepts a `templateId` prop and — only for a brand-new automation (no `id`/`copyFromId`) — fetches `GET /templates/:id` and feeds it through the same `initialValue` transform as `copyFromId`, silently (no toast, since nothing is persisted yet). |
| `2026-07-17-httpsLinkNormalization.update.md` | `Front/knowledge/updates/2026-07-17-httpsLinkNormalization.update.md` | Summary of the submit-time zod transforms that upgrade user-entered `http://` links to `https://` (and add a scheme to bare-domain button urls) across the shared automation form (including message fields from Task 2b) and the dashboard product form. |
| `2026-07-18-subscriptionBuyButtonVisibility.update.md` | `Front/knowledge/updates/2026-07-18-subscriptionBuyButtonVisibility.update.md` | Summary of fixing the invisible "خرید اشتراک" button on `/settings/subscription` — `bg-violet-650` in `ChoosePlan.tsx` isn't a real Tailwind class, so no background CSS was generated; changed to `bg-violet-600`. |
| `2026-07-18-buttonTemplateUiSizing.update.md` | `Front/knowledge/updates/2026-07-18-buttonTemplateUiSizing.update.md` | Summary of the sizing/alignment cleanup of the shared button-row editor (`ContentButtonsItem.tsx`) used by `buttonTemplate`/`text`/`question`/`vitrin` content buttons — fixed type-select width jumping, type+title not sharing a row, asymmetric card padding from a `-mt-2` hack, inconsistent per-card top padding, and a mismatched icon-button height next to the URL input. No functional/behavior changes. |
| `2026-07-18-automationSkipTemplatePicker.update.md` | `Front/knowledge/updates/2026-07-18-automationSkipTemplatePicker.update.md` | Summary of removing the template-picker step from the "create automation" flow — clicking the button on `automations/page.tsx` now navigates straight to `/automations/add` instead of opening `CreateAutomationTemplateDialog`, matching the dialog's old "start from scratch" button. `TemplatePicker`/`CreateAutomationTemplateDialog` left in place for other uses. |
| `2026-07-18-vitrinOptionalTitleDescription.update.md` | `Front/knowledge/updates/2026-07-18-vitrinOptionalTitleDescription.update.md` | Summary of making vitrin (generic template) `title`/`description` optional in `VitrinItemSchema` to match the backend DTO relaxation, plus updated "(اختیاری)" placeholders and a 5-row description `Textarea`. |
| `2026-07-18-autoConsentQuickReply.update.md` | `Front/knowledge/updates/2026-07-18-autoConsentQuickReply.update.md` | Summary of auto-inserting a `CONSENT` ("مکث و ادامه") quick reply at index 0 of any TEXT content that has quick replies and is no longer the last content in `contents` — prevents Instagram from hiding that content's buttons once another content follows it. Reactive `useEffect` in `Contents.tsx`, `contents` array only (not `reminders`), no backend change. |
| `2026-07-18-lockConsentQuickReply.update.md` | `Front/knowledge/updates/2026-07-18-lockConsentQuickReply.update.md` | Follow-up to `2026-07-18-autoConsentQuickReply.update.md`: locks removal of a `CONSENT` quick reply in `ContentButtonsItem.tsx` while its content still has a following content (live computed condition, not a stored flag) — clicking remove shows an explanatory `AlertDialog` instead of deleting it. Unlocks automatically once the content becomes last again. |
| `2026-07-19-bannerCarouselRtlFix.update.md` | `Front/knowledge/updates/2026-07-19-bannerCarouselRtlFix.update.md` | Follow-up to `2026-07-14-bannerCarousel.update.md`: fixes `StableCarousel`'s flex-row translate math only working under LTR (broke slide 2+ under the dashboard's default `dir="rtl"`) by detecting the resolved direction and flipping the translate sign; adds `StableCarouselPrevious`/`StableCarouselNext` nav buttons. |
| `2026-07-19-bannerCarouselOverlapAndFontSize.update.md` | `Front/knowledge/updates/2026-07-19-bannerCarouselOverlapAndFontSize.update.md` | Follow-up to `2026-07-19-bannerCarouselRtlFix.update.md`: reserves an inline gutter in `BannerSlide`'s `CardContent` matching the nav buttons' footprint so long text no longer renders underneath them; adds `fluidTextSizeClass()`, a 3-tier character-count-based size lookup for title/description, so a long banner shrinks instead of inflating every shuffled slide's height. |
| `2026-07-20-adminSidebarScroll.update.md` | `Front/knowledge/updates/2026-07-20-adminSidebarScroll.update.md` | Fixes the sidebar clipping its overflow: `SidebarContent` used `overflow-hidden` (upstream shadcn uses `overflow-auto`), so opening a parent menu taller than the sidebar hid items with no scrollbar and no wheel scroll. Switches to `overflow-y-auto overflow-x-hidden overscroll-contain` + the repo's `scrollbar-thin` convention, keeping header/footer pinned; `nav-main.tsx` scrolls a newly-opened parent into view. Shared via the `components/ui` symlink, so it fixes the dashboard's `ConsoleSidebar` too. |
| `2026-07-22-commerceProductManagementFoundation.update.md` | `Front/knowledge/updates/2026-07-22-commerceProductManagementFoundation.update.md` | Summary of Tasks 1-3 of the commerce product management rebuild (branch `feat/commerce-product-core`, replacing the legacy `/products` pages against the Back's new `/commerce/*` catalog API): shared `commerce.ts` types + `buildCategoryTree` util, the `ProductListPage`/`CommerceProductCard` list against `GET /commerce/products`, and the `ProductEditorPage` create/edit shell (`EditorScrollspyNav`, `BasicInfoSection`, `ShippingSection`, `productForm.schema.ts`). Also documents the post-review fixes: `shippingCost` now included in both create/update payloads (paired with Back's `386378c0`), and the editor's desktop/mobile layout now uses a single JS-driven `isMobile` (900px) breakpoint instead of mixing it with Tailwind's independent `md:` (768px) class. |
| `2026-07-22-commerceVariantsPricing.update.md` | `Front/knowledge/updates/2026-07-22-commerceVariantsPricing.update.md` | Summary of Task 5 of the commerce product management rebuild: `variantMatrix.util.ts` (pure cartesian-product generator + `VARIANT_LIMIT`/`OPTION_LIMIT`), `VariantsSection.tsx` (options builder + generated variant table with diff-preserving "بازسازی جدول تنوع‌ها" regenerate, at-least-one-active-variant guard, per-variant sale-window Popover), `productForm.schema.ts`'s new price cross-field `.superRefine` (`compareAtPrice > price`, `salePrice < price`, sale-window both-or-neither) and array-level active-variant `.refine`, and `ProductEditorPage.tsx`'s `buildUpdatePayload` switching from always-omitting to `dirtyFields`-gated inclusion of `options`/`variants`. Also exposes `Popover`/`PopoverContent`/`PopoverTrigger` from the shared `@befroosh/ui` barrel. |
| `2026-07-23-commerceVariantMediaPicker.update.md` | `Front/knowledge/updates/2026-07-23-commerceVariantMediaPicker.update.md` | Summary of Task 6 of the commerce product management rebuild: `VariantMediaPickerDialog.tsx` (select-tiles-from-product's-media-pool + star-sets-cover modal, full-replace `PUT /commerce/products/:id/variants/:variantId/media`), wired into `VariantsSection.tsx`'s per-row media button (disabled with a tooltip for a variant with no real persisted id yet; shows the variant's own cover thumbnail or a dashed "uses product cover" placeholder). The self-flagged Back gap it originally documented (`GET /commerce/products/:id` not returning per-variant media, forcing a `revalidate: false` workaround) was fixed same-day (Back commit `869261f8`) — the save handler now follows `MediaSection.tsx`'s standard optimistic-write-then-revalidate convention (`revalidate: false` for the instant local write, a plain revalidating `mutate(key)` in `finally`, decoupled from the success/error toast so a revalidation hiccup can't misreport a successful save as failed). |
| `2026-07-23-commerceTaxonomyManagement.update.md` | `Front/knowledge/updates/2026-07-23-commerceTaxonomyManagement.update.md` | Summary of Task 9 of the commerce product management rebuild: a new standalone `products/taxonomy` page (`CategoryTree.tsx` + `CategoryDialog.tsx` for the category tree, `CollectionsList.tsx` + `CollectionDialog.tsx` for collections, both wired into `taxonomy/page.tsx`'s two-pane `LayoutPage` with their "new" actions in the header via `useHeaderFeatures`). Category drag-and-drop is scoped to sibling-reorder-only (nested `SortableContext`s under one `DndContext`, cross-parent drops are a no-op) — re-parenting is done via `CategoryDialog`'s parent select instead, which excludes the category's own subtree from the options. Surfaces `COMMERCE_CATEGORY_IN_USE`/`COMMERCE_CATEGORY_CYCLE` via `t_ec` (added to the `ExceptionMessage` union). No collection-type UI anywhere, per spec correction item 6 — confirmed no such field exists on the backend. |
| `2026-07-23-commerceProductCorePermissionGapFix.update.md` | `Front/knowledge/updates/2026-07-23-commerceProductCorePermissionGapFix.update.md` | Whole-branch final review fix: adds the missing `usePermissions().can(...)` gate (matching `ProductListPage.tsx`'s established convention) to every commerce mutation that previously fired with no frontend permission check — `MediaSection.tsx` (upload/delete/reorder), `VariantMediaPickerDialog.tsx` + its opening button in `VariantsSection.tsx`, `AdjustStockDialog.tsx` + its opening button in `InventorySection.tsx`, `CollectionsSection.tsx`'s chip toggle, all of `CategoryTree.tsx`/`CategoryDialog.tsx`/`CollectionsList.tsx`/`CollectionDialog.tsx`'s create/update/delete/reorder actions (+ `taxonomy/page.tsx`'s header buttons), and `ImportWizard.tsx`'s upload/start-import. Slugs verified against the real backend controllers rather than assumed — category/collection create+update+delete all require `PRODUCT_EDIT` (no separate create/delete slug), import requires `PRODUCT_CREATE`. |
| `2026-07-23-commerceMediaInventoryImportNav.update.md` | `Front/knowledge/updates/2026-07-23-commerceMediaInventoryImportNav.update.md` | Closes out the commerce product management rebuild: Task 4's `MediaSection.tsx` (upload/reorder/delete against the product's media pool, establishing the optimistic-write-then-revalidate SWR convention every later mutating section reuses) plus a real shared-component fix (`FileUploader.tsx`'s `multiple` prop was silently ignored); Task 7's `InventorySection.tsx`/`reconstructLedgerBalances.util.ts`/`AdjustStockDialog.tsx` (client-side ledger balance reconstruction, fixed post-implementation from a per-page-anchored bug to a fetch-once-paginate-client-side one); Task 8's `CollectionsSection.tsx` (collections-membership-only, read-modify-write full-replace PUT) plus the urgent same-pattern `UpsertCommerceCollectionDto.name` optional-field bug that had silently broken Task 8 since it shipped; Task 10's `ImportWizard.tsx`/`useImportJobPolling.ts` (3-step upload→processing→result, no client-side column mapping) plus the backend gap it surfaced (no route resolved an import job's error-report file id to a URL); and Task 11's `ConsoleSidebar.tsx` nav wiring plus deletion of the entire legacy `components/Products/` and `app/(Console)/products/components/` folders (keeping `types/product.ts`, still used by the live Shop/Checkout flow). Documents the final whole-branch review verdict (TARGET MET) and the first full-project `tsc --noEmit` run's 2 real fixes (two test files using vitest globals instead of explicit imports; a stale `VariantsSection.test.tsx` fixture missing `lowStockThreshold`). |
| `2026-07-25-commerceSecurityReviewFixes.update.md` | `Front/knowledge/updates/2026-07-25-commerceSecurityReviewFixes.update.md` | Front half of the `/security-review` fix pass on `feat/commerce-product-core` — **i18n only**; the substantive fixes are all backend (see `Back/knowledge/updates/2026-07-25-commerceSecurityReviewFixes.update.md`). The frontend finder returned **no vulnerabilities**, verified by grep against the working tree: zero unsafe sinks (no `dangerouslySetInnerHTML`/`innerHTML`/`eval`/`javascript:`) across all new commerce code, the import error-report download `href` traced end-to-end to a server-built CDN path (never a filename or spreadsheet cell), media URLs going through `next/image`, and no secrets or new `console` logging. Adds four `ERROR_CODES` entries to `fa.json` per CLAUDE.md §10: `ORDER_QUANTITY_INVALID` and `COMMERCE_INVALID_QUANTITY` (the new negative-quantity guards), plus `COMMERCE_INVALID_MEDIA` and `COMMERCE_CATEGORY_NOT_FOUND` which the backend already threw but were never translated. Note: the backend's public buyer route now gates real stock on workspace ownership rather than mere authentication — anonymous buyers (the actual consumers of that route) see identical output, so no Front code change was needed. |
| `2026-07-25-commerceCreateTimeMediaAndCollections.update.md` | `Front/knowledge/updates/2026-07-25-commerceCreateTimeMediaAndCollections.update.md` | **Media + collections usable while CREATING a product.** Both sections used to render a `saveProductFirst` placeholder in create mode (category was never affected — it is a plain `categoryId` field in `BasicInfoSection` and always shipped in the create payload). **Collections:** `ProductFormValues` gained `collectionIds[]`; create mode renders `PendingCollectionsPicker`, the same chips bound to that form field via `FormField` instead of PUTting each collection, and `buildCreatePayload` sends the ids so the backend writes membership in the same transaction. Edit mode unchanged; the collections list is now fetched in BOTH modes (key used to be `null`). **Media:** create mode renders `PendingMediaPicker`, holding the chosen `File[]` in `ProductEditorPage` state (NOT the form — keeps the zod schema serialisable) with `createObjectURL` previews, remove, and a cover badge on index 0; `uploadPendingMedia` then POSTs each file SEQUENTIALLY after create returns an id — sequential on purpose, since positions are assigned server-side in arrival order and parallel uploads would make the cover non-deterministic. A product-saved-but-upload-failed case surfaces as `Toast.createdWithMediaErrors` rather than a plain success, and the user lands in the editor to retry. Object URLs revoked in a cleanup effect. **Inventory (تراز موجودی):** also ungated — the ledger/adjust dialog genuinely need real variant ids, but OPENING stock does not (`variants[].initialStock` is already in the create payload and seeds the level + a `manual`/`initial` ledger row), so create mode now shows one opening-stock input per variant, writing the SAME form field the Variants table exposes; input follows CLAUDE.md §18 (text + `inputMode=numeric` + `onInputP2EHandler`, never `type=number`). Adds 5 `fa.json` keys incl. `ERROR_CODES.COMMERCE_COLLECTION_NOT_FOUND` (backend code existed, had no Persian string). 88/88 editor tests; `tsc` clean of anything from this change — remaining noise in these files (`Badge` children/variant, `zodResolver` ZodObject) is pre-existing and also fires on untouched lines. |
| `2026-07-25-commerceEditorRedesign.update.md` | `Front/knowledge/updates/2026-07-25-commerceEditorRedesign.update.md` | **claude-design product-editor redesign** (stages R4 tokens/contract + R5 variations table; R6 pending). The template is a 1751-line single component with ~60 handlers over one flat state and NO I/O — adopted for its visual language and interactions, NOT its state model; our editor keeps react-hook-form + zod + dirtyFields + permission gating. Two things deliberately NOT ported: `paintSwatches()` (full querySelectorAll + inline style write) and `autoCreate()` running on EVERY `componentDidUpdate`, and aggregates recomputed inside `render()` — ours are memoised over pure functions. **R4:** the design system's palette is byte-identical to ours and 13/37 tokens already existed, so only 16 semantic tokens were added, all but `--warning` derived via `color-mix` so dark mode follows automatically. **R5:** variations render as a two-level tree grouped by the first option's values, with roll-ups (`variantTree.util`) and bulk edit (`variantBulk.util`) as pure tested modules. Key rules: `null` counts as MISSING not zero (else a group reads '0 – 445,000'); a group with any missing leaf is not uniform (else collapsing hides unpriced rows); `Infinity` = untracked stock; a group is a branch only when >1 option AND >1 leaf. **Perf:** groups start COLLAPSED and only an expanded group's leaves are emitted, so a 10x200 product renders 10 rows — the tree IS the windowing, no virtualiser dependency needed. Selection is keyed by RHF's stable `_vid`, never array index (indexes shift under regenerate). **Bug fixed not ported:** the template's `1 - amount/100` is unguarded so a >100% discount yields a NEGATIVE price, which `CHECK price >= 0` rejects on save — clamped at zero. `OPTION_LIMIT`/`VARIANT_LIMIT` were already enforced, so the template's unlimited `addAttr` does not apply. 168/168 commerce tests; regenerate-diff logic untouched. |
| `2026-07-27-commerceEditorVisualRefactor.update.md` | `Front/knowledge/updates/2026-07-27-commerceEditorVisualRefactor.update.md` | **Visual half of the claude-design product-editor refactor** (the 2026-07-25 pass ported behaviour only; every section still rendered as a default shadcn `Card` inside a scrollspy the design does not have). **Chrome:** `ui/editorChrome.ts` (class strings, not wrapper components — most land on a plain `<input>` already carrying a RHF `field` spread), `ui/EditorSection.tsx` (numbered step header OUTSIDE the card + `EditorRailCard`; step digits rendered Persian via `e2pNumbers`). **Token fix:** R4's 16 tokens were declared in `:root` only and never in `@theme`, so Tailwind could not see them and `bg-tint2` compiled to nothing — they were dead from the day they were added; now registered as `--color-*`, plus a new `--ink` (named NOT `--dark`, since `.dark` is the theme class). **Layout:** `EditorTopBar` (breadcrumb, live title, status pill, revert) over a `minmax(0,1fr)/308px` grid with Collections+Tags in a sticky rail; `EditorScrollspyNav` and `BasicInfoSection` deleted. Steps 1-9 are the design's; 10 (stock ledger) and 11 (shipping) are ours — the design has no home for them but both are real saved data. **Nothing dropped to fit:** the design's row has 7 columns, ours had 12, so SKU/sale-window/backorder/active moved into a per-row settings popover whose trigger tints when something inside is set; `status`/`kind` keep real controls in the category card. **Dead fields revived:** `basePrice`/`baseCompare`/`baseStock` existed in `ProductFormValues` since R4 but nothing rendered or read them — `baseSeed.util.ts` now defines the seeding, with two rules: price/compare are TEMPLATES but stock is a QUANTITY (so the stock seed lands on the first new variation only — copying '30 in stock' onto 12 sizes would claim 360 units), and a seed NEVER overwrites a number already typed. Used by `handleRegenerate` and by `buildCreatePayload` for the lone implicit variation of an option-less product (the only place its price can come from). **Category:** flat `<select>` → two-level `CategoryPickerDialog` with inline create; creating needs `product:edit`, a DIFFERENT permission from the `product:create` that lets someone add a product, so create rows are hidden rather than 403-ing after a name is typed (same split for the new inline collection create). **Options:** dnd drag → labelled up/down buttons, because option ORDER decides the tree's top level and drag hid that; values accept comma-separated lists and reject case-insensitive duplicates. 197/197 commerce tests + 11 new `baseSeed` tests; eslint clean; `tsc` noise (`zodResolver` ZodObject, `Badge variant`) pre-existing. **Not done:** the design's storefront preview dialog — button absent rather than present-and-dead. |
| `2026-07-27-commerceProductEditorRemoval.update.md` | `Front/knowledge/updates/2026-07-27-commerceProductEditorRemoval.update.md` | **The product add/edit page was removed from the frontend** (`feat/commerce-product-core`, Front only — the Back's `/commerce/*` catalog API is untouched and every endpoint the editor called still exists). Deletes both routes (`products/add`, `products/[id]`), all 44 files under `components/Commerce/ProductEditor/` (shell + `productForm.schema.ts`, all 12 sections, the category/variant-media/adjust-stock dialogs, the `ui/` chrome from the visual refactor, and the `variantMatrix`/`variantTree`/`variantBulk`/`variantIdentity`/`baseSeed`/`reconstructLedgerBalances`/`markdownPreview` utils), the now-orphaned `utils/commerce/toggleProductInCollection.ts` (only `CollectionsSection` used it — `buildCategoryTree.ts` is KEPT, Taxonomy still needs it), and the whole `Commerce.Editor` i18n namespace plus `Commerce.List.add`/`Commerce.List.Card.edit` from `fa.json`. The list/taxonomy/import screens stay and never imported from `ProductEditor`, so the split was clean. **The product list is now read-only apart from delete**: the `product:create` header button and the per-card edit button both routed to the deleted pages, so both are gone along with `useRouter`/`canEdit` in `ProductListPage.tsx` and `CommerceProductCard.tsx` (the card footer now renders only under `canDelete`, delete takes full width). `ProductListPage.test.tsx`'s three edit/delete permission tests collapse into two delete-only ones plus a regression test asserting `can()` is never asked for `product:create`/`product:edit` — asserted on the permission probe, not rendered text, because the header button lives in the `useHeaderFeatures` store rather than this component's DOM. Note that the six earlier commerce-editor update docs are kept for history but now describe code that no longer exists on this branch. |
| `2026-07-27-commerceProductEditorRebuild.update.md` | `Front/knowledge/updates/2026-07-27-commerceProductEditorRebuild.update.md` | **The product add/edit page rebuilt from the claude design**, reversing `2026-07-27-commerceProductEditorRemoval.update.md` — a rebuild, not a revert. Nine numbered steps in one scrolling column, a 308px sticky rail, a sticky top bar and four dialogs, on react-hook-form + zod + SWR. Three structural changes from the old editor: RHF owns the state (uncontrolled `register` inputs, so a keystroke in one of up to 2000 rows re-renders nothing); the form stores stable option-value KEYS instead of the backend's positional `valueIndexes`, with positions derived once at payload time against the same options array the payload just built (reordering an axis used to silently repoint every row); and combination regeneration runs in the axis event handlers, never in an effect, so a deliberately deleted row stays deleted. `useProductSave`'s order is forced by the schema rather than chosen — product, then media sequentially (`position` is arrival order, so index 0 is the cover), then a re-read (variant ids exist nowhere else), then variant media, then collection membership (owned by the collection side). Only the product write can fail the save; a failed upload is a warning naming the count. **Three accepted trade-offs:** no status/kind/shipping control anywhere (create hardcodes `active`/`physical`/`0`, update omits all three — draft/digital/paid-shipping products become unsettable in the dashboard); media sits outside Save/Revert (`productId` is NOT NULL, so edit mode uploads on drop and deletes on ✕, and بازگردانی does not bring a deleted file back); and a variant with no price blocks the save (`price` is NOT NULL with `CHECK price >= 0`, so no placeholder is ever written). Restores both routes and both list entry points, replacing `18456153`'s now-wrong "no add/edit entry point exists" regression test. Front only — no backend change. **Final whole-branch review fix wave (2026-07-28)** appended to the same doc: the two CRITICAL data-loss paths in `variant/useVariantSync.ts` — a regenerated row losing its `id` and all seven no-UI fields (fatal because `PUT` replaces the variants array under `cascadeDeleteVariants`, so a cosmetic axis REORDER wiped every SKU), and `donorOf` matching only the grow direction (so removing one of two axes blanked every price/stock/photo) — plus the new pure `realignValueIds` that makes a reorder a permutation instead of a regeneration; a fresh `mutate(COLLECTIONS_KEY)` before the membership read-modify-write; per-variant media failures surfaced instead of swallowed under a success toast; `errors.options`/`errors.specs` tinted and reachable by `setFocus`; `posterUrl` carried into `EditorMedia` so video stops rendering as a broken image; and four duplicated shared types single-sourced. 199 tests (was 185), the 4 critical ones negative-controlled against the pre-fix code; tsc unchanged at the 205 baseline. |
| `2026-07-30-commerceValidationSync.update.md` | `Front/knowledge/updates/2026-07-30-commerceValidationSync.update.md` | **Front/back validation contract sync** (spec: `docs/superpowers/specs/2026-07-30-commerce-validation-sync-design.md`; Back counterpart: `Back/knowledge/updates/2026-07-30-commerceValidationSync.update.md`). `buildProductEditorSchema` (`ProductEditor/productEditor.schema.ts`) tightened to match the backend's `CreateCommerceProductDto`/`product.service.ts` contract in both directions: `description`/`categoryId` become required, every free-text field gets a length ceiling, `colorHex` gets a real shape regex, and variant-level rules add tracked-stock-required-unless-∞, `salePrice < price`, sale-pairing, and sale-window ordering — mirroring the backend, where `variants` had NO min/max at all (API silently accepted 0 or unbounded variants) and `weight` sat behind an unbounded `@IsInt()` over an `int4` column (500 instead of 400). Also adds 8 top-level `ERROR_CODES` keys to `fa.json`: the 6 new backend codes plus 2 pre-existing ones that had never gotten Persian copy (`COMMERCE_INVALID_SALE_PRICE`, `PRODUCT_DISCOUNT_PRICE_IS_BIGGER_THAN_PRICE`), plus 2 more found by a verification sweep of every thrown commerce code (`COMMERCE_DUPLICATE_VARIANT`, `COMMERCE_OPTION_VALUE_IN_USE`). 236+ commerce tests pass; `tsc` unchanged at the 205 baseline. |
| `2026-08-01-consentQuickReplyUnlock.update.md` | `Front/knowledge/updates/2026-08-01-consentQuickReplyUnlock.update.md` | Fixes BEF-142, a dead end left by `2026-07-18-lockConsentQuickReply.update.md`: the lock keyed only off "a content follows this one", so deleting every other quick reply left an auto-inserted `CONSENT` button that could never be removed (and still paused the flow at send time via `shouldPauseForConsent`). `isLockedConsentButton` now also requires at least one non-`CONSENT` quick reply to still be there — an untyped, freshly-added row counts as one. Frontend-only, no backend change. |
| `2026-08-01-contentTypePickerGrid.update.md` | `Front/knowledge/updates/2026-08-01-contentTypePickerGrid.update.md` | Fixes BEF-138: reverts `ChooseAutomationType.tsx` from the `Dialog`/`Drawer` (bottom-sheet-on-mobile) rewrite back to the earlier inline `grid-cols-5` icon+title button grid (no description per button, no dialog wrapper), while keeping the newer `options`-prop template-mode filtering. Refreshes `Automations.Contents.Types.buttons.titles`/`descriptions` copy in both apps' `fa.json` per reporter-supplied text. Also fixes a `Contents.tsx` gap the revert exposed: the zero-contents empty state (`no_content_title`/`add_step`) lacked the `!isChoosingType` gate the populated-list "add content" button already has, so it stacked visibly above the newly-inline type grid instead of the old modal quietly covering it. A reporter-requested "برچسب" (Label) content type has no backing enum value yet — left out of scope. |
| `2026-08-01-dashboardStaticBannersRemoval.update.md` | `Front/knowledge/updates/2026-08-01-dashboardStaticBannersRemoval.update.md` | Deletes the hardcoded, non-admin-managed `DashboardTelegramBanner.tsx`/`DashboardInstagramBanner.tsx` (fixed copy/URL/color, no DB backing, `fa`-only 2-column grid) from the dashboard home. `DashboardBannerCarousel` (the real admin-managed banner system) moves down to the bottom of `page.tsx` — the position the deleted grid occupied. New order: `SubscriptionBoard` → `DashboardStats` → `DashboardBannerCarousel`. Frontend-only, no backend change. |
| `2026-08-02-questionImageValidation.update.md` | `Front/knowledge/updates/2026-08-02-questionImageValidation.update.md` | Adds `Image` to `ValidationTypeEnum` (`packages/ui/src/automation-builder/types/validationType.enum.ts`) for the automation Question content step's `validationType` dropdown — paired with a Back change that validates the lead's DM reply contains an Instagram image attachment. `QuestionContent.tsx`'s `<Select>` already iterates `Object.values(ValidationTypeEnum)`, so only a default Persian error message case and the `fa.json` label were needed, in **both** `apps/dashboard/src/messages/fa.json` and `apps/admin/src/messages/fa.json` — the admin app's template builder renders the same `QuestionContent.tsx` (its own separately-synced `fa.json` copy needed the key too, caught by code review). No zod schema change (`z.nativeEnum` auto-includes new enum values). |
| `2026-08-02-questionPostalCodeValidation.update.md` | `Front/knowledge/updates/2026-08-02-questionPostalCodeValidation.update.md` | Adds `PostalCode` to `ValidationTypeEnum` for the same Question content `validationType` dropdown — a plain regex-based text validator (paired Back change), same shape as `Mobile`/`NationalCode`. Both `apps/dashboard/src/messages/fa.json` and `apps/admin/src/messages/fa.json` updated together this time, applying the lesson from the `Image` feature's missed admin copy. |
| `2026-08-04-contactsMobileTableScroll.update.md` | `Front/knowledge/updates/2026-08-04-contactsMobileTableScroll.update.md` | Fixes two mobile-only `/contacts` bugs. (1) The contacts table's horizontal scroll was unreachable: its `overflow-auto` box was genuinely scrollable (client 390px vs scroll 650px) but the box itself was **1368px tall**, so its scrollbar sat ~680px below the viewport. Root cause is `SidebarInset` (`packages/ui/src/components/ui/sidebar.tsx` — note both `apps/dashboard/src/components/ui` and `apps/admin/src/components/ui` are **symlinks** to it, so this change hits both apps) capping its height only at `md` (`md:max-h-screen`) — below `md` nothing had a definite height, so `LayoutTable`'s `h-full` resolved to `auto` and `flex-1` on the table box did nothing. Fixed with an all-breakpoint `max-h-svh`, plus moving mobile scrolling onto `SidebarInset` itself (`overflow-y-auto`, `md:overflow-hidden`) so pages with no scroll container of their own are not clipped by the new cap. **App-wide change — every `(Console)` page now scrolls inside `SidebarInset` on mobile instead of on `body`.** (2) Header/filter spacing: the Excel-export button moved from `HeaderTools` (which is `w-full` on mobile, so it wrapped onto its own row as a near-invisible `outline` button on the purple gradient) into `HeaderButton` as a labelled solid button, matching `orders/page.tsx`; the Instagram filter got its own padded `empty:hidden` row in `ContactsList.tsx` instead of relying on the column `gap-4`, while the table stays full-bleed. |
| `2026-08-03-bef164AutomationDelayAndMp3Fix.update.md` | `Front/knowledge/updates/2026-08-03-bef164AutomationDelayAndMp3Fix.update.md` | Fixes BEF-164: swaps the delay/wait step's number and time-unit `Select` field order in `DelayContent.tsx` (was reading "hour 1" in the RTL UI instead of "1 hour" — pure DOM-order swap, no logic change); adds `audio/mp3` to `MediaUploader.tsx`'s accepted audio formats (some browsers report `.mp3` as `audio/mp3` rather than `audio/mpeg`), plus matching `fa.json`/`en.json` help-text updates. Frontend-only, no backend change. Ticket's 3rd item (form-builder postal-code/photo options) reported separately resolved already. |
| `2026-08-03-bef163GeneralUiFixes.update.md` | `Front/knowledge/updates/2026-08-03-bef163GeneralUiFixes.update.md` | Fixes BEF-163's 6 general UI items: adds a dashboard-wide `RadixDirectionProvider` (root cause of every Radix `Select`/dropdown rendering LTR in the Persian UI — `apps/admin` already had this, `apps/dashboard` didn't); widens the automation builder's comment/DM-cap disclaimer to full width and refreshes its copy to the current 300/hour cap; fixes the account-sessions page's dead horizontal scroll (`min-w-0` on a flex ancestor), renames it to "دستگاه‌های فعال", and adds its missing breadcrumb translation; normalizes the mobile bottom nav's icon sizes/label typography so all 5 items align; widens `InstagramSelectField`'s single-page hide guard from `=== 0` to `<= 1` with a matching auto-select effect so the required `instagramIds` field still gets filled; and reworks the subscription-renewal form (removes the "already has an active subscription" notice, consolidates the duplicated desktop/mobile discount-code blocks into one placed after the duration cards instead of before them). |
| `2026-08-05-r2ImageHosts.update.md` | Adds the two Cloudflare R2 custom domains (`dl.befroosh.app` prod, `dltest.befroosh.app` test) to `apps/dashboard/next.config.mjs` `images.remotePatterns`, fixing a `next/image` "Invalid src prop … hostname … is not configured" crash on the workspace drawer. R2's own `**.r2.cloudflarestorage.com` S3-API host is deliberately excluded — it needs a signed request, so allowlisting it would not help; the real fix was in the backend URL builder (Back PR #5). Also adds `ERROR_CODES.UPLOAD_SOURCE_IS_NOT_AN_IMAGE` to `fa.json`. **Deploy coupling: ship before or with the Back change, and note `next.config.mjs` is read at build time so a rebuild is required.** |
| `2026-08-03-automationDraftBanner.update.md` | `Front/knowledge/updates/2026-08-03-automationDraftBanner.update.md` | Fixes BEF-141: moves the automation-draft resume prompt from a blocking popup dialog on the automations list page into a dismissible `AutomationDraftBanner` at the top of the create-automation form (`headerSlot`), with "continue editing" / "new message" (clear + in-place reset via a `formResetKey` remount) actions and auto-dismiss on the first real edit. Adds a 2-day TTL to stored drafts (`automationDraft.ts`). Frontend-only, no backend change. Follow-up (2026-08-04): the banner's copy and both buttons are now grouped inside a single amber `Alert` (responsive one-line/stacked layout, amber-themed buttons) instead of an alert plus a separate button row. |
| `WorkspaceDrawerContent.tsx` | `Front/apps/dashboard/src/components/Console/WorkspaceDrawerContent.tsx` | Presentational content of the mobile workspace drawer (opened from `NavBottom.tsx`'s bottom-nav "کسب‌وکار" item via the now-thin `WorkspaceDrawer.tsx` wrapper): profile card (name/mobile + pencil `Link` to `/settings`), an always-expanded list of every workspace with its Instagram accounts nested underneath (connection dot from `isIgTokenValid`; subscription text — days-left / covered-by-credit / pending-activation — computed directly from `GET /workspaces`'s per-instagram `subscriptionDaysLeft`/`hasReservedSubscription` and per-workspace `hasCreditCoverage`, **not** `PageCoverageBadge`, which only ever has the active workspace's data), tapping any workspace/account row switches context via `useWorkspaces().changeWorkspace`, and a footer "افزودن پیج" button to `/connect` plus logout. Split out from `WorkspaceDrawer.tsx` specifically so it's testable without vaul/Drawer portal mechanics. |
| `2026-08-03-workspaceDrawerRedesign.update.md` | `Front/knowledge/updates/2026-08-03-workspaceDrawerRedesign.update.md` | Summary of the mobile workspace drawer redesign: grouped per-workspace Instagram accounts, connection/subscription status, business-labeled bottom nav, add-page footer button. Paired Back branch `feat/workspace-drawer-redesign` (added `isIgTokenValid` to `GET /workspaces`). |
| `2026-08-05-workspaceDrawerRemainingDays.update.md` | `Front/knowledge/updates/2026-08-05-workspaceDrawerRemainingDays.update.md` | Follow-up fix: the drawer only showed subscription days-left for the active workspace (real-user-caught bug). `GET /workspaces` now computes it server-side per workspace; `WorkspaceDrawerContent.tsx` drops `PageCoverageBadge` entirely in favor of the new backend fields. Paired Back branch `fix/workspace-drawer-remaining-days`. |
| `2026-08-04-bef163AutomationListSinglePage.update.md` | `Front/knowledge/updates/2026-08-04-bef163AutomationListSinglePage.update.md` | BEF-163 follow-up: `AutomationCard.tsx` (automation list) now hides its per-card Instagram username badge when the workspace has only one connected page, mirroring `InstagramSelectField.tsx`'s `<= 1` guard from the 2026-08-03 bundle — that bundle only fixed the create form, not the list. |
| `2026-08-03-dashboardDevSpeed.update.md` | `Front/knowledge/updates/2026-08-03-dashboardDevSpeed.update.md` | Cuts `apps/dashboard` dev-mode cost. `@phosphor-icons/react` is imported from 119 sites and its barrel pulls ~3025 modules (1512 CSR + 1513 SSR); Turbopack does not tree-shake in dev, and Next 16's built-in `optimizePackageImports` list covers `lucide-react`/`recharts` but not Phosphor — fixed by adding `experimental.optimizePackageImports: ['@phosphor-icons/react', '@befroosh/ui']`. Sentry also ran at full power in dev (client Session Replay under HMR, `tracesSampleRate: 1`, server-side OpenTelemetry hooking every module load) — now gated behind `enabled: isProd` in all three configs, production behaviour unchanged. Stale 5.2 GB `.next` cleared. Also rewrites all 116 barrel imports to deep per-icon paths (263 lines, incl. the separate 1513-module `/dist/ssr` barrel) and removes the 2 `import * as` namespace imports that pulled the whole barrel regardless of config — both were dynamic icon-lookup-by-name, now passing the component instead. Drops 9 unused deps plus the `motion` wrapper (`motion/react` is just `export * from 'framer-motion'`). Statically verified: 150/150 specifiers resolve, 264/264 bindings exist; typecheck 211=211 (0 introduced). **Measured:** `/contacts` 13.1s → 1.69s (~7.8x), `/directs` ~30%, `/orders` ~13%; other routes within noise. Nearly the whole win came from deleting the 2 namespace imports, not the 116-file deep-import rewrite — a namespace import defeats both `optimizePackageImports` and deep imports. `apps/admin` given the same treatment. |
| `2026-08-03-guideHelperFixes.update.md` | `Front/knowledge/updates/2026-08-03-guideHelperFixes.update.md` | Fixes BEF-140's in-app guide (راهنما) dialog: `HelpMeDialog.tsx`'s close control moves from a bottom "بستن" button to a `DialogClose`+`X` icon in the dialog's top-left corner (RTL layout, matches the header's reserved `pl-6` space); `playsInline` added to the guide `<video>` to stop iOS Safari auto-fullscreen; the shared `AutomationBuilder` package's `TitleAndEnabled`/`ContentItem` gained `helpSlot`/`contentTypeHelpSlots` so the dashboard can inject a CMS-driven `HelpMeDialog` per content type and for the automated-message-name field; the "start request message" guide's overlap with the header delete button is fixed via `noAbsolute`; new `helpId`s are registered in the Admin guides page's dropdown. |
| `2026-08-08-secondInstagramSubscriptionGate.update.md` | `Front/knowledge/updates/2026-08-08-secondInstagramSubscriptionGate.update.md` | New `SetupInstagramDialog` on `/connect`, gated by `hasAvailableSubscriptionSlot` (originally from `GET /users/me`, superseded 2026-08-08, see below), guides a user through username check → follower lookup → matched/manual plan → buy before connecting a 2nd+ Instagram account. Payment verify redirects to `/connect` (not `/settings/instagram`) for pooled purchases, with a one-time-read `pending_ig_username` cookie reminder. Also fixes a shallow-merge i18n bug: new `ERROR_CODES` translations must live in `messages/fa/ErrorCodes.json`, not the monolithic `messages/fa.json` (which is fully shadowed by the split file in `i18n/request.ts`'s merge). |
| `2026-08-08-workspaceScopedSubscriptionSlot.update.md` | `Front/knowledge/updates/2026-08-08-workspaceScopedSubscriptionSlot.update.md` | User-reported: with 1 Instagram + 0 subscriptions, the 2nd-account dialog never showed. Root cause: `GET /users/me` resolves "current workspace" from the JWT access token's `workspaceId` claim, which can go stale (never re-validated per request; ~100h TTL in non-prod). `connect/page.tsx` now reads `hasAvailableSubscriptionSlot` from `useWorkspaces()` (keyed by real membership, immune to the stale claim) instead of `useUser()`. Note: "which workspace is current" still traces back to the same JWT claim (`usePermissions().workspaceId`) — a wider, pre-existing pattern not fixed here. |
| `2026-08-07-workspaceProfileChip.update.md` | `Front/knowledge/updates/2026-08-07-workspaceProfileChip.update.md` | Adds a pill-shaped "Workspace Profile Chip" to the top of the desktop sidebar (`ConsoleSidebar.tsx`, below the logo), showing the current workspace's name, an avatar/overlapping-avatar-group/fallback-initial built from its connected Instagram accounts, and a handle-or-count summary; the whole chip opens the existing `WorkspaceDrawer` on click (no new switcher logic). New `WorkspaceProfileChip.tsx` + `utils/avatarColor.ts` (deterministic fallback-avatar color hash), new `Console.Sidebar.{accountsConnected,noInstagramConnected}` i18n keys. Implemented directly on `merged-admin` per explicit instruction, not yet manually verified in-browser. |
| `2026-08-07-userDetailsCardSimplified.update.md` | `Front/knowledge/updates/2026-08-07-userDetailsCardSimplified.update.md` | Simplifies the sidebar footer `UserDetailsCard.tsx` now that `WorkspaceProfileChip` covers workspace identity/switching: drops the workspace-switcher popover, per-account Instagram list, and subscription progress bar, down to three rows — profile icon + name, mobile/email, and two same-`variant="outline"` buttons ("ویرایش" → `/settings`, "خروج" → existing logout flow). New `Console.Dashboard.edit` i18n key; reuses `Console.Sidebar.logout`. Implemented directly on `merged-admin`, not yet manually verified in-browser. |
| `2026-08-08-settingsInstagramSubscriptionGate.update.md` | `Front/knowledge/updates/2026-08-08-settingsInstagramSubscriptionGate.update.md` | Moves the subscription-setup gate trigger from `/connect` (post-navigation) to the "Add account" button on `/settings/instagram` itself — clicking it with an existing Instagram + no available subscription slot opens `SetupInstagramDialog` in place instead of navigating; otherwise still links to `/connect` as before. `/connect`'s own gate is untouched (still covers direct navigation/OAuth callback). |
| `2026-08-08-setupInstagramDialogRedesign.update.md` | `Front/knowledge/updates/2026-08-08-setupInstagramDialogRedesign.update.md` | Restyles `SetupInstagramDialog` to visually match (not duplicate) `ChoosePlan.tsx`'s `/settings/subscription` buy dialog: gradient header, `rounded-2xl` duration cards with "best value" badge + monthly-price/savings display, dedicated per-duration "Buy" button, same treatment for the manual fallback plan grid. Reuses `Subscription` namespace i18n strings; no new keys. No purchase-flow logic changed. |
| `2026-08-09-connectUnboundPlanChoice.update.md` | `Front/knowledge/updates/2026-08-09-connectUnboundPlanChoice.update.md` | User-reported dead end: a workspace holding an **unbound paid subscription** whose follower tier does not fit a new page could never connect it. `hasAvailableSubscriptionSlot` is tier-blind, so `/connect` skipped `SetupInstagramDialog` and showed only the OAuth link; the tier check happens later in `bindOnConnect` (step 3), which throws `SUBSCRIPTION_NOT_COMPATIBLE_WITH_FOLLOWER_COUNT` and rolls back — and `useConnectInstagram` only toasts, leaving the page unchanged, so the user retried forever with no way to buy the plan the toast asked for. `/connect` now shows the unbound plan(s) with their tier badge plus two actions: continue with this plan (unchanged OAuth link) or خرید پلن دیگر (opens the existing `SetupInstagramDialog`). Detection uses `getUnboundActiveSubscriptions`, which excludes credit — credit is follower-count-blind and can never mismatch, so it still goes straight to OAuth. Frontend-only, no backend change; `/settings/instagram` needs none since its button only forwards to `/connect` in this state. 10 tests pass. |
| `2026-08-09-setupDialogUnboundStep.update.md` | `Front/knowledge/updates/2026-08-09-setupDialogUnboundStep.update.md` | **Supersedes the UI placement of `2026-08-09-connectUnboundPlanChoice.update.md`** (same dead end, wrong surface — the first attempt rendered the choice inline on `/connect`, splitting one journey across page and dialog). The continue-or-buy decision now lives inside `SetupInstagramDialog` as a new **first step**, shown only when the workspace holds an unbound paid plan: an amber warning that the plan activates only on an account inside its follower range, each plan listed with its `PlanTierBadge` tier, then «ادامه با همین اشتراک» (links to Instagram OAuth) or «خرید پلن دیگر» (sets `dismissedUnboundStep` and drops into the existing username → follower-lookup → plan flow, unchanged). With no unbound paid plan the dialog opens on the username input exactly as before. `/connect`'s only remaining change is the open condition (`needsSubscriptionSetup || hasUnboundPlan`), since the dialog was unreachable in that state. New `utils/instagramOAuthUrl.ts` shares the OAuth URL so page and dialog cannot drift on scope/redirect_uri. i18n keys moved from `Connect` to `SetupInstagramDialog`. 18 tests pass. |
| `2026-08-10-instagramWorkspacePickerWizard.update.md` | `Front/knowledge/updates/2026-08-10-instagramWorkspacePickerWizard.update.md` | `SetupInstagramDialog` becomes a 3-step wizard (username → plan/cost review → workspace choice), letting the user send a 2nd+ Instagram page to a new or different existing workspace instead of always the current one. New `useWorkspaceTargetStep` and `useInstagramWizardResume` hooks isolate the branching/resume logic from the Radix UI; the switch survives its `window.location.reload()` via URL-stamped resume state and is verified against `usePermissions().workspaceId` before payment ever fires. |
| `2026-08-09-settingsAddInstagramGate.update.md` | `Front/knowledge/updates/2026-08-09-settingsAddInstagramGate.update.md` | Closes the first two known gaps of `2026-08-09-setupDialogUnboundStep.update.md`. `/settings/instagram`'s «افزودن اکانت» button only asked half the question (`hasInstagram && !hasAvailableSubscriptionSlot`), so an **unbound paid plan** — which makes that flag true — rendered a plain `/connect` link and bounced the user through `/connect` just to reach the dialog its own button could have opened. New shared hook **`hooks/useAddInstagramGate.ts`** now owns the rule for both surfaces and returns `needsSubscriptionSetup` / `hasUnboundPlan` / `requiresSetupDialog` / `isLoading`; `hasInstagram` stays a parameter because `/connect` sources it from `useUser` while settings uses the accounts list. Behaviour: first account → `/connect`; second-or-later with no slot **or** an unbound paid plan → `SetupInstagramDialog` in place; credit-only or already-bound plan → `/connect`. Also fixes the loading race — `InstagramAccounts.onCountChange` now reports `number \| null` from a `useEffect` on the SWR loading flag instead of `onSuccess` (so a *failed* fetch resolves too), and the button is disabled while the count or gate is unknown, since a half-loaded "no accounts" answer is the one state that skips every check. `/connect`'s behaviour is unchanged; it just reads the same hook. No new i18n keys. 30 tests pass (12 new), mutation-checked. **Same-day follow-up in the same doc**: the dialog's «ادامه با همین اشتراک» button no longer links straight to `IG_OAUTH_URL` — it now routes to `/connect` so the user gets that page's instructions and help video. A plain `/connect` link would loop (the dialog also renders *on* `/connect`, whose gate is still unanswered, so it would reopen itself), so the button carries the answer: `CONTINUE_WITH_PLAN_HREF` = `/connect?continueWithPlan=1`, and `useAddInstagramGate(hasInstagram, { unboundPlanAccepted })` clears **only** `hasUnboundPlan` — `needsSubscriptionSetup` still gates, since no query param should talk a workspace with zero coverage past it. The button also calls `onOpenChange(false)`, because a `/connect` → `/connect` navigation keeps the route mounted and the dialog would otherwise cover the page. 33 tests pass. |
| `2026-08-08-teamMembersSettingsPage.update.md` | `Front/knowledge/updates/2026-08-08-teamMembersSettingsPage.update.md` | Moves team management from the `/workspace` page into a new `/settings/team` page (reachable via a new sidebar sub-item under Settings, alongside a "تنظیمات عمومی" sub-item preserving one-click access to the `/settings` hub). `TeamManager.tsx`'s active-members list switched from one unpaginated `useSWR` call to a paginated + searchable `useSWRImmutable` call, following the `Orders/OrdersCardList.tsx` pattern. `/workspace/page.tsx` now shows only the profile card. Paired Back doc: `2026-08-08-teamMembersPagination.update.md`. |
| `2026-08-09-settingsRestructure.update.md` | `Front/knowledge/updates/2026-08-09-settingsRestructure.update.md` | Replaces the app's two competing settings navigations (main-sidebar entries + the in-page `SettingsOptions` card list) with one set of five ordered, icon-labeled Settings sub-items: اطلاعات کسب‌وکار, پیج‌های متصل, خرید اشتراک, اعضای تیم, اطلاعات بانکی. New `/settings/workspace` page replaces the Telegram-style `/workspace` profile card with standard settings chrome — invitation/transfer banners, create-workspace dialog, rename form, new `WorkspaceCategoryForm` (`PATCH /workspaces/:id { categoryId }`; note the workspace *reads* its category as nested `category.id` but *writes* a flat `categoryId`), and a danger zone holding ownership transfer + delete. `/workspace` and `/settings` became redirects; `/workspace/[memberId]/permissions` is untouched and still used by `TeamManager`. `NavMain` sub-items gained optional `icon`/`badge` (badges previously rendered only on non-collapsible top-level items, so the pending-invitations dot would have vanished silently). Profile, password and sessions merged into one tabbed `/settings/profile`. Frontend-only — `PATCH /workspaces/:workspaceId` already accepted `categoryId` and `GET /workspace-categories` already existed. |
| `2026-08-09-settingsDesignConsistency.update.md` | `Front/knowledge/updates/2026-08-09-settingsDesignConsistency.update.md` | Settings design-consistency fix — workspace/subscription pages now use standard Button/Input tokens and width caps instead of ad-hoc overrides. |
| `2026-08-10-breadcrumbTranslations.update.md` | `Front/knowledge/updates/2026-08-10-breadcrumbTranslations.update.md` | Fixes hardcoded/untranslated breadcrumb text in both apps. Admin's `app-breadcrumb.tsx` returned the literal Persian string `'جزئیات'` for UUID segments instead of `t('detail')`; now fixed, plus a missing `add` segment (`/templates/add`). Dashboard's `HeaderBreadcrumb.tsx` was missing map entries for real routes `support`, `install`, `invitations`, `sendAll` (silently fell through to the raw un-translated URL slug), plus a dead reference to a nonexistent `Breadcrumbs.edit` key. All added to `fa.json` (`Breadcrumbs` namespace for dashboard, `Sidebar` namespace for admin — `en.json` intentionally left for later per convention). The `decodeURIComponent` fallback itself is kept for genuinely dynamic, non-UUID entity identifiers; see `CLAUDE.md` for the standing rule that every new *static* route segment must be added to the map + `fa.json`. |
| `2026-08-11-continuePlanDirectOAuth.update.md` | `Front/knowledge/updates/2026-08-11-continuePlanDirectOAuth.update.md` | User-reported extra hop: reverts the `/connect`-routing part of `2026-08-09-settingsAddInstagramGate.update.md`'s same-day follow-up. `SetupInstagramDialog`'s «ادامه با همین اشتراک» button now links straight to `IG_OAUTH_URL` again — same target as the plain «اتصال اکانت» button — instead of `/connect?continueWithPlan=1`, which only got the user to a page where they had to click connect a second time. Removed the now-dead `CONTINUE_WITH_PLAN_PARAM`/`CONTINUE_WITH_PLAN_HREF` exports and the `unboundPlanAccepted` option on `useAddInstagramGate` (`requiresSetupDialog` now derives straight from `needsSubscriptionSetup \|\| hasUnboundPlan`). 28 tests pass. **Same-day addendum**: merged in `fix/instagram-wizard-ui-polish` (previously uncommitted work in its own worktree, committed here per user request) — bounds `SetupInstagramDialog` to a fixed height with internal per-step scroll (fixes the dialog's layout-shift-as-content-loads), forwards the wizard-resume `igw*` params through `AuthProvider`'s auth-guard redirect (was silently dropping them right after a workspace-switch reload), and removes the superseded `pendingInstagramConnect.ts` cookie-reminder feature. Clean 3-way auto-merge, no conflicts. 33 tests pass. **Follow-up**: fixed a code-review-found gap where the igw suffix was appended as a sibling param instead of nested inside a `returnTo` target, silently dropping it one hop later through the invitations/transfer picker. **Follow-up**: fixed `/settings/subscription/verify` being silently redirected away for zero-Instagram users returning from a payment gateway (`AuthProvider`'s `!hasInstagram` branch had no allowlist entry for it, unlike `/settings/instagram?code=`). 37 tests pass. |
| `2026-08-11-mobileDrawerAddPageOpensDialog.update.md` | `Front/knowledge/updates/2026-08-11-mobileDrawerAddPageOpensDialog.update.md` | Mobile workspace drawer's «افزودن پیج» button used to go straight to `/connect`, skipping the same-day-earlier `useAddInstagramGate` gate (`2026-08-09-settingsAddInstagramGate.update.md`). Now routes to `/settings/instagram?openAdd=1` (new `AUTO_OPEN_ADD_PARAM` export); the page auto-replays its own Add button's click branch once the gate resolves — opens `SetupInstagramDialog` or continues to `/connect`, whichever the gate says. 32 tests pass. |
| `2026-08-13-howFoundUs.update.md` | `Front/knowledge/updates/2026-08-13-howFoundUs.update.md` | Asks «چطور با بفروش آشنا شدید؟» once, in a closable dialog opened on the way into creating an automation, and never again once answered — deliberately **absent** from `/settings/profile`, which is the whole point (opt-in placement would never get filled). `useBusinessInfoGate` + a zustand store hold the pending create URL; the dialog saves via `POST /users` then continues the navigation. **A loading or errored `/users/me` never gates** — trapping a user behind a dialog because our own read failed costs more than one missed answer. All **six** create entry points route through the hook (automations header button, dashboard card, empty-state wizard CTA, mobile bottom-nav `+`, template dialog ×2, duplicate-automation); `<Link>`-based ones keep their real `href` and only `preventDefault()` when the gate must open, so open-in-new-tab still works. **SWR gotcha:** `GET /users/me` is keyed twice — `useUser` relative, `ProfileForm` absolute — so the dialog mutates both or the gate re-fires on a stale null. Admin side adds `FilterHowFoundUs` on the users table (`&howFoundUs=a,b`), the same selection on `POST /users/excelExport`, and a read-only row on the user detail page. Also **fixes** two pre-existing repo problems found on the way: admin `fa.json` declared a top-level `Jobs` key **twice** (lines 125 and 1449 — JSON keeps the last, so the first 53-line block was dead; both were byte-identical, so deleting it left the parsed messages unchanged) and **51 tracked entries under `packages/ui/node_modules`** (47 pnpm symlinks + 4 `.bin` shims pointing into content-hashed `.pnpm/<hash>` paths — broken on a fresh clone, rewritten by every dependency bump; `.gitignore` already listed the directory, but gitignore does not apply to files already in the index, so they needed `git rm -r --cached`). **Trap worth remembering:** editing a messages file by round-tripping it through a JSON parser silently collapses a duplicate key and takes ~50 unrelated lines with it — it happened here and had to be reverted; edit as text. Paired backend: `Back/knowledge/updates/2026-08-13-howFoundUs.update.md`. |
| `2026-08-13-productPriceMaxBound.update.md` | `Front/knowledge/updates/2026-08-13-productPriceMaxBound.update.md` | Frontend half of the backend `bigint` money-column fix (`Back/knowledge/updates/2026-08-13-moneyColumnsToBigint.update.md`). Saving a product priced above ~2.1B Toman used to fail with an untranslatable message: the backend column was `integer`, so the value overflowed at Postgres and returned a raw `QueryFailedError`, which has no `code` for `t_ec(...)` to look up. The form had no upper bound either — `superRefine` only checked the **lower** bound (`< 1000`) and discount-vs-price. Adds `MAX_MONEY_AMOUNT = 999_999_999_999` (`src/constants/money.constant.ts`, must stay in sync with `Back/apps/core/src/common/constants/money.const.ts`), two new `superRefine` checks on `price`/`discountPrice` in `ProductForm.tsx`, the matching `Products.Form.{price,discountPrice}.errors.tooLarge` keys in `fa.json`, and `ERROR_CODES` entries `PRODUCT_PRICE_TOO_LARGE` / `PRODUCT_PRICE_SHOULD_BE_POSITIVE` / `PRODUCT_DISCOUNT_PRICE_TOO_LARGE`. Note: the cap is interpolated with `toLocaleString('en-US')`, **not** the project's `formatNumber` helper, whose declared `string \| number \| null` return type is not assignable to a next-intl message value. |
| `2026-08-13-automationCardFooterAlignment.update.md` | `Front/knowledge/updates/2026-08-13-automationCardFooterAlignment.update.md` | Fixes the automations card list (`/automations`) showing the bottom action bar (answers/edit/copy/delete) at different heights across one grid row — visible when a titled and an untitled automation sit side by side, and when an automation is assigned to an Instagram post. The grid already stretched every card to the row height, but `Card` is `flex flex-col` and `CardContent` had no `flex-1`, so the content kept its natural height and the footer floated up with the dead space left **below** it. Fix pins the footer instead of patching each height source: `h-full` on `Card`, `flex-1` on `CardContent`, `h-full` on the inner content row. Plus stabilisers for the remaining jitter — `min-w-0` + `truncate` keep a long Instagram username list on one line, `min-h-6` reserves one badge row for automations with no conditions, `shrink-0` protects the `w-20` post thumbnail. New `AutomationCard.layout.test.tsx` (7 tests, watched red first — 6 fail without the fix) asserts the pinning classes and that `CardFooter` is the card's last child across five content shapes; jsdom has no layout engine, so this is class-level, not pixel-level. Frontend-only, no new i18n keys. |
| `2026-08-14-settingsMobileScroll.update.md` | `Front/knowledge/updates/2026-08-14-settingsMobileScroll.update.md` | Fixes three mobile scroll defects on `/settings/*`, all rooted in two shared files rather than the pages. **(1)** `SidebarInset` reserved room for the fixed `NavBottom` with `pb-14`, but it is also the mobile scroll container — padding sits **inside** the scroll box so it never shrinks a height-filling child, and Blink omits that end padding from `scrollHeight` when the overflow comes from an `overflow:visible` descendant. `settings/layout.tsx` compounded it with `h-full` (100% of the box while the 56px header was already in it). Result: `/settings/instagram`, `/card`, `/zarinpal` had 56px that **no gesture could reveal**, even at maximum scroll. **(2)** `LayoutSettings` added a second `overflow-y-auto`, so `/settings/workspace`, `/subscription`, `/profile`, `/team` ran **nested** scrollers — the inner one ran out first and the last 56px needed a separate gesture on the outer one, which also dragged the header off screen. **(3)** `DialogContent` is `fixed` + vertically centred with `max-height: none` and `overflow: visible`, so a dialog taller than the screen hung off **both** edges with **no scroll container anywhere** (measured `top: -558` at 975px on an 844px viewport) — title and first fields unreachable on every base dialog. Fix takes nav clearance out of **height** instead of padding: `SidebarInset` → `max-h-[calc(100svh-3.5rem)]`, `pb-14` dropped; `settings/layout.tsx` → `flex-1` + `md:flex-none`; `LayoutSettings` → `md:overflow-y-auto` only; `DialogContent` → baseline `max-h-[90dvh] overflow-y-auto`; `ChoosePlan` → `90vh` → `90dvh`. **`dvh` not `vh`** throughout: on a real phone `vh` measures the *large* viewport, so a 90vh dialog still overflows while the URL bar shows. Desktop untouched — at `md` the pre-existing `md:max-h-screen` / `md:pb-0` variants already win. Verified in device mode across all 7 settings sub-pages (scrollers 2→1, nothing under the nav) plus `/automations` and `/orders` as regression checks. |
| `2026-08-15-shopWorkspaceScoping.update.md` | `Front/knowledge/updates/2026-08-15-shopWorkspaceScoping.update.md` | Frontend half of the shop-module workspace-scoping fix (`Back/knowledge/updates/2026-08-15-shopWorkspaceScoping.update.md`). `GET /shops/:shopId` moved payment details from `shop.user.paymentDetail` to `shop.workspace.paymentDetail` when Instagram stopped being user-owned, but the dashboard was never updated — and `IShop` still declared `user`, so **TypeScript agreed with the stale reads and flagged nothing**. Renames the type (`IShop.user` → `IShop.workspace`, interface `User` → `Workspace`) and all 8 read sites in `order/components/payment.tsx` (6) and `components/Shop/CheckoutPage.tsx` (2). Adds `ERROR_CODES.EXCEL_EXPORT_WORKSPACE_REQUIRED` to `fa.json` for the new backend guard. **Deploy-coupled — Back and Front must ship together**; either side alone leaves the checkout with no payment methods. Note the page failed earlier than this in practice: `GET /shops/:shopId` itself threw backend-side, so the shop never loaded at all. Vitrin CRUD (`/vitrin`), which the dashboard calls from `products/[id]/product.tsx` and `ProductForm.tsx`, was restored backend-side with no frontend change needed. |
| `2026-08-15-commentStartSelfGatesGuard.update.md` | `Front/knowledge/updates/2026-08-15-commentStartSelfGatesGuard.update.md` | Fixes a comment automation whose **first content is a QUESTION** being impossible to save — submit was blocked by the toast «در حالت کامنت، پیام درخواست شروع ضروری است» pointing at a field that was not on screen, so the user could never satisfy it. Root cause was drift between two copies of the same rule: BEF-162 (`2026-08-01-smartConsentDisable.update.md`) taught `StartAutomationMessage.tsx` to hide the start-request card when `content[0]` self-gates (a `QUESTION`, or a `TEXT` with quick replies) **and clear `commentStartText` to `''`**, but left `AutomationForm.tsx`'s `handleBeforeSubmit` guard on the old inline condition, which still rejected the now-blank value — a deadlock, with `setError`/`setFocus` aimed at an unrendered field. **The backend was already correct and needed no change**: `handleComment` (`Back/apps/core/src/contentCycle/contentCycleMessage.service.ts`) computes its own `firstContentSelfGates` and sends the start-request private reply only when neither `isSingleMessage` nor `firstContentSelfGates` holds, so it never reads `commentStartText` for a QUESTION-first automation — the frontend was demanding a value the backend would never use. Fix extracts the decision into one shared predicate, `packages/ui/src/automation-builder/utils/commentStart.ts` (`firstContentSelfGates` + `isCommentStartMessageRequired`), and points **both** the field's activation effect and the submit guard at it so they cannot drift again. Note the helper's param type is deliberately looser than `AutomationFormType` (every field optional there). **Same pass also fixes a second, opposite defect**: `justFollowers` was treated as a blanket exemption from needing `commentStartText`, but `followerGuard` (`Back/apps/core/src/contentCycle/follower.service.ts`) returns `countinue: true` for a comment only when `isUserFollowBusiness && contents.length === 1 && !reminderTime` — so one shape does reach the start message and still sends it: a **single PRODUCT content with no reminder** (a lone PRODUCT keeps `isSingleMessage` false because products need the lead authenticated). There the backend sent `commentStartText` while the frontend had guaranteed it was empty. `justFollowers` is now a *narrowing* of which shapes reach the message, not an exemption; `StartAutomationMessage` additionally watches `reminderTime`, and its one visible behavior change is that the card now **shows** for that single-PRODUCT case. **`contents.length > 1` with `justFollowers` is NOT the broken case** — `followerGuard` stops those comments first; only the single-PRODUCT shape is reachable. 15 new util tests encode the backend's truth table branch by branch; a new `AutomationForm.commentStart.test.tsx` renders the real form end-to-end and was confirmed red before the fix, with the blocking toast verified as the *only* validation error involved. 132 `packages/ui` + 34 dashboard tests pass. **Not backfilled**: automations saved before this fix keep their empty `commentStartText`, so a backend guard that skips the send when it is empty is the durable follow-up. |
| `2026-08-16-mainOnlyFixesPort.update.md` | `Front/knowledge/updates/2026-08-16-mainOnlyFixesPort.update.md` | Audit and port of the **8 non-merge commits that existed only on `main`** before `merged-admin` was promoted to `main`. **4 already present** (zibal, `dl.befroosh.app` next/image allowlist, IG-callback raw error code, WAV re-encode — the last already living in `packages/ui`). **1 real gap ported**: `3e9956f2` duplicate verify-payment. This branch had already rewritten the verify page from `useSWRImmutable` to `api.get` in an effect but never carried the guard, so the duplicate stayed reachable another way — the effect depends on `searchParams`, whose object identity can change between renders, and StrictMode double-invokes effects in dev; a `useRef` latch placed **after** the gateway-detection check fixes it without touching the "direct visit, never fake success" path. **1 trap — deliberate divergence, must never be ported**: `main` flip-flopped the auth `next` path (`data.data.next` → `data.data.result.next` → back), and its final state matches **master's** backend where `prelogin` returns the service result raw; `workspace-refactor` wraps it in `ResponseMessage` as `data:{result}`, so `data.data.result.next` — what `merged-admin` already has — is correct, and porting main's flip **would break login**. Confirmed by reading `auth.controller.ts` on both backend branches. **1 deliberately skipped**: `902aaebf` rewrites the `docker-compose-Front*.yml` build context to `/home/cicd/manual-deploy/Sources/Front`; on `main` those files sit at the repo root beside a root `Dockerfile`, but here they moved under `apps/dashboard/` while the root has `Dockerfile.dashboard`/`Dockerfile.admin` — neither value builds under the monorepo layout, so it was left for the deploy rework rather than guessed. Verified: `apps/dashboard` tsc 188 errors before and after, identical set, none in the verify page. Paired backend: `Back/knowledge/updates/2026-08-16-masterOnlyFixesPort.update.md`. |
| `2026-08-18-subscriptionRemainingDaysSum.update.md` | `Front/knowledge/updates/2026-08-18-subscriptionRemainingDaysSum.update.md` | "Remaining days" must sum the active sub's days-left with every RESERVED sub's full `planDuration.durationDays` (a reserved sub always has `expire = null`) — fixes `PageSubscriptionCard.tsx` (only used the active sub's `expire`, now matches `PageCoverageBadge.tsx`'s existing pattern for both the radial's numerator and denominator) and two admin spots (`users/columns.tsx`, `users/[id]/page.tsx`) whose `&& s.expire` filter silently dropped every reserved row before their own reserved-sum branch could run; also widens the user-detail "باقی مانده" row to show whenever there's an active OR reserved sub, not only active. **Same-day follow-up:** the dashboard header (`SubscriptionBoard.tsx`) picked the free-trial automation-count radial vs. the subscription radial from `hasActiveSubscription` (ACTIVE-only), so a reserved-only user (already paid, just queued) wrongly saw the free-trial view and the "promotion is active" banner; replaced with `hasSubscription = active || reserved`. Paired backend: `Back/knowledge/updates/2026-08-18-subscriptionRemainingDaysSum.update.md` (fixes the majority of call sites — the workspace header, admin stats CTEs, and a stray `subscription.userId` reference). |
| `2026-08-19-automationLinkCountRadial.update.md` | `Front/knowledge/updates/2026-08-19-automationLinkCountRadial.update.md` | Dashboard header ad-alert now gates on `freeAutomationQuotaExceeded && isPromotion` per account, not `!hasSubscription`. `ProgressRadial.tsx`'s automation ring turns solid red (`isAutomationOverLimit`) when the count reaches/exceeds the free limit — 5 new vitest cases. `SubscriptionBoard.tsx`'s radial count switched from the live `automationCount` to the monotonic `automationLinkCount` (stays consistent with the sticky ad-alert instead of implying quota room that's already used up); new `automationLinkCount` field on `InstagramNamespace.Account`. Paired backend: `Back/knowledge/updates/2026-08-19-automationLinkCountExposed.update.md` (had to explicitly expose the field first). |
| `2026-08-26-referralPlanGift.update.md` | `Front/knowledge/updates/2026-08-26-referralPlanGift.update.md` | Admin `/referral-codes` create dialog gains the third referral type, `PLAN` (`پلن (هدیه)`) — a code that gifts a subscription instead of a discount. The type existed backend-side all along and core honoured it at signup, but no create path ever accepted `planDurationId`, so such codes could only be hand-inserted into the DB. Adds a cascading plan → duration picker (`useSWR('/plans')` → `useSWR('/plans/planDurations?planId=…')`, same pair as `components/customer/AddSubscriptionDialog.tsx`, both conditional keys so nothing is fetched until `PLAN` is picked), a `useEffect` clearing `planDurationId` when the plan changes (a duration belongs to exactly one plan), and hides `discount`/`سقف تخفیف`/`حداقل تخفیف`/`حداکثر دفعات استفاده` for `PLAN` since the backend forces them to `0 / 0 / null / 1`. `columns.tsx` renders the gifted plan + duration name for `PLAN` rows (which store `discount = 0`) instead of a meaningless `0`, via a new optional `planDuration` field the backend's `read()` now left-joins. Error toast moved off hardcoded per-code strings onto `t_ec` (§10); `ERROR_CODES.REFERRALCODE_ALREADY_EXISTS` added to `fa.json` (`PLAN_DURATION_NOT_FOUND` was already there). **Zod gotcha encoded in a regression test**: `discount` is `z.number().optional()` with the positivity rule in `superRefine`, *not* `.positive().optional()` — a field-level failure aborts the object parse before `superRefine` runs, so the form's default `discount: 0` would have failed validation on an input that is **hidden** for `PLAN`, leaving the form unsubmittable with no visible error; the test was confirmed red against the buggy shape. 9/9 vitest. Paired backend: `Back/knowledge/updates/2026-08-26-referralPlanGift.update.md`. |
| `2026-08-26-referralCodeEdit.update.md` | `Front/knowledge/updates/2026-08-26-referralCodeEdit.update.md` | Admin `/referral-codes` gains an **edit** dialog (there was none — and no backend update route either) plus a dedicated `پلن هدیه` column. The create form was extracted into `referral-code-form-dialog.tsx` used by **both** modes (`isEdit = !!referralCode`), so create and edit are identical by construction rather than by copy; edit prefills from the row already in the table, which is why no `GET /referral-codes/:id` was needed — only the new `PATCH /referral-codes/:id`. `columns` const became a `makeColumns({ onEdit })` factory so rows can reach the dialog; the discount column shows only the discount again (`—` for `PLAN`, which stores `discount 0`) now that the gift has its own column. `FormSchema` moved out of `referral-codes-table.tsx` (vitest import updated). **Gotcha**: the clear-`planDurationId`-on-`planId`-change effect fires on first render and would wipe the duration just prefilled for an edit — it now tracks a `seededPlanId`, skips the first run after open, and resets on close. Also: the create button clears `editing` first, so create never opens holding a previous row's values. Everything is editable incl. `code` and owner `mobile`, redeemed codes editable with no warning (explicit decisions); nothing retroactive. 9/9 vitest, eslint clean, tsc unchanged at the 118-error baseline. Paired backend: `Back/knowledge/updates/2026-08-26-referralCodeEdit.update.md`. |
| `2026-08-27-audioTooLongErrorCode.update.md` | `Front/knowledge/updates/2026-08-27-audioTooLongErrorCode.update.md` | Adds the `AUDIO_TOO_LONG` Persian key to the dashboard's `src/messages/fa/ErrorCodes.json`, pairing the Back audio-converter back-port that introduced the code (ffprobe now rejects audio over the 10-minute cap instead of silently storing only its first 10 minutes at `200 OK`). Without the key `t_ec` falls through to the backend's raw English `message`. Note `messages/fa/ErrorCodes.json` is the live file — `messages/fa.json`'s `ERROR_CODES` block is shadowed by the shallow spread in `i18n/request.ts` and is dead. `en/` left alone per §8. |
| `2026-08-28-audioRecorderIosCrash.update.md` | `Front/knowledge/updates/2026-08-28-audioRecorderIosCrash.update.md` | Fixes Sentry `MY-2B` — the voice recorder crashed with `TypeError: undefined is not an object (evaluating 't.onstop=...')` on **every iOS device** (276 events, 10 users, 100% iOS 17.3–18.2, zero on iOS >= 18.4 / Android / desktop). `startRecording`'s mimeType chain `audio/mpeg -> audio/webm -> 'audio/wav'` left the last value **unchecked**, but WebKit before 18.4 supports only `audio/mp4` for MediaRecorder (WebM arrived in 18.4), so `new MediaRecorder(stream, { mimeType: 'audio/wav' })` threw `NotSupportedError`. The throw became a crash because `setIsRecording(true)` ran **before** it and the `.catch` only `alert`ed without resetting — leaving the Save button on screen wired to a `recorder` that was never assigned, which `stopRecording` then dereferenced. Two things hid it: `let recorder: MediaRecorder` (not `| null`) made the unguarded read type-check, and **the recorder that threw captured nothing** — it had no `ondataavailable`; all audio came from a second, default-constructed recorder and the waveform from the `analyser`, so it only duplicated the mic capture. Fix deletes that redundant recorder and the whole mimeType chain (the default container always works and `recordingToWavBlob` re-encodes to WAV anyway — see `2026-07-20-audioRecorderWavReencode.update.md`), moves `setIsRecording(true)` after a successful start, guards `stopRecording`, and adds `releaseStream()` so a failed start no longer leaks a live microphone. Also fixes `resetRecording` leaving the real recorder running, and replaces three `alert()` calls (raw English DOMExceptions + a `'recorder instance is null!'` debug string) with i18n toasts. New tests fake WebKit-pre-18.4: **3 of 4 fail on the old code reproducing the production error verbatim, while the Chromium case passes** — matching prod, where only iOS crashed. 171 `packages/ui` tests pass; dashboard tsc 188 errors before and after. **Not verified on real iOS hardware** (no device; Playwright WebKit could not launch on this host) — one manual smoke test on iOS < 18.4 is still owed. Latent follow-up: `recorder`/`recordingChunks`/`timerTimeout` are module-level and would collide if two recorders were ever rendered at once. |
| `2026-08-28-automationDraftFileTempCrash.update.md` | `Front/knowledge/updates/2026-08-28-automationDraftFileTempCrash.update.md` | Fixes Sentry `MY-41` — `/automations/add` died at render (`TypeError: ... reading 'split'`, 375 events / 94 users) whenever a saved local draft held a picked-but-unuploaded file. `writeAutomationDraft` persists the form with `JSON.stringify`, and a `File` has no enumerable own properties, so `contents[].fileTemp.file` serialized to a bare `{}`; on restore `MediaUploader.renderPreview` ran `uploadedFile.type.split("/")` on it. Located by reading the deployed minified chunk at the exact stack column, not guessed. Two-sided fix: `stripFileTemp()` in `automationDraft.ts` drops `fileTemp` from `contents[]`/`reminders[]` on **write** and on **read** (read matters — 2 days of poisoned drafts already sit in users' browsers), and `MediaUploader` gained `getFileBlob()` (`file.file instanceof Blob`) that every display helper reads through, so a blob-less entry degrades to the generic file icon instead of crashing, stops rendering `NaN MB`, and no longer reports "uploading" forever (which had hidden its own delete button). Frontend-only, no backend/i18n change. |
| `2026-08-28-iceBreakers.update.md` | `Front/knowledge/updates/2026-08-28-iceBreakers.update.md` | Ice Breakers (پیام خوش‌آمدگویی): sidebar automations parent + `/automations/welcome` page, per-page 4-slot editor with per-locale tabs, whole-list save, save-vs-publish distinction. |
| `2026-09-02-automationsMobileScrollChaining.update.md` | `Front/knowledge/updates/2026-09-02-automationsMobileScrollChaining.update.md` | `/automations` and `/automations/:id` nest a second scroller (`LayoutCard`/`LayoutPage`) inside `SidebarInset`'s mobile scroll container with no `overscroll-behavior`, so a swipe past the inner scroller's end chained to `body` — rubber-band bounce revealed `SidebarWrapper`'s purple gradient background, and at the top it triggered Chrome's native pull-to-refresh. Fix: `overscroll-contain` on all three, same pattern already used on `SidebarContent`. Deployed to live prod 2026-09-02. |
| `2026-09-02-analyticsWebhookTestEvent.update.md` | `Front/knowledge/updates/2026-09-02-analyticsWebhookTestEvent.update.md` | **SUPERSEDED 2026-09-08** by `2026-09-08-analyticsTestEventInjection.update.md`. New "ارسال رویداد تست" (Send Test Event) dialog on the admin `/webhooks` detail drawer (`test-event-dialog.tsx`) — lets a marketing-team operator send a fully synthetic, signed event to one endpoint (event type + `context.user.status`, reusing the real customer-list's Persian status labels, plus optional workspace/Instagram-page fields) and see the live pass/fail result plus the exact JSON sent. Paired backend: `Back/knowledge/updates/2026-09-02-analyticsWebhookTestEvent.update.md`. |
| `2026-09-08-analyticsTestEventInjection.update.md` | `Front/knowledge/updates/2026-09-08-analyticsTestEventInjection.update.md` | Rewrites the Send Test Event dialog now that the backend **injects** a real event instead of simulating one (fixes the Befroosh Wait node's `ANALYTICS_WEBHOOK_EVENT_NOTFOUND`). Three cascading pickers of REAL rows — debounced user search → workspace → automation — each shown only when the chosen event type needs it, with stale selections cleared on type/workspace change. Delivery is async, so the result panel polls the delivery row every 3s until terminal and warns when the event type has no snapshot adapter in core. Uses `t_ec`/`ERROR_CODES` per CLAUDE.md §10. Front tsc 120→120 (baseline measured); eslint clean. Paired backend: `Back/knowledge/updates/2026-09-08-analyticsTestEventInjection.update.md`. |
| `2026-08-12-commerceMediaPoolResort.update.md` | `Front/knowledge/updates/2026-08-12-commerceMediaPoolResort.update.md` | Adds drag-and-drop reorder to the product editor's media pool (`MediaSection.tsx` + new `SortableMediaTile.tsx`, `@dnd-kit`, same pattern as `apps/dashboard/src/components/Commerce/Taxonomy/CategoryTree.tsx`), wired to the backend's already-built `PATCH /commerce/products/:id/media` endpoint via a new `ProductEditorPage.tsx` `handleReorderMedia` (optimistic, rolls back on failure, local-only in create mode). No backend change. |
| `2026-08-26-errorCodeTranslationsComplete.update.md` | `Front/knowledge/updates/2026-08-26-errorCodeTranslationsComplete.update.md` | Completes Persian text for **every** backend error code: `messages/fa/ErrorCodes.json` goes 184 → 253 keys, covering all 241 codes in the two Back `ERROR_CODES` unions plus 12 frontend-only ones. Also fixes a shipped instance of the shallow-merge shadowing bug first flagged in `2026-08-08-secondInstagramSubscriptionGate.update.md` — 23 `COMMERCE_*` translations had been added to the monolithic `messages/fa.json`, which `i18n/request.ts` spreads *before* `messages/fa/ErrorCodes.json`, so the whole `ERROR_CODES` object was replaced and those keys were dead at runtime. `i18n/messages.test.ts` upgraded from a 3-key spot check to a general guard that fails on any key added to the shadowed file. |
| `2026-08-12-productListCardToCardNotice.update.md` | `Front/knowledge/updates/2026-08-12-productListCardToCardNotice.update.md` | `/products` used to just disable the "add product" button when the merchant had no `cardToCard` method, with no explanation. Now the grid is blurred and locked behind an `absolute inset-0` notice panel sized exactly to the grid (so its height always matches the table), with `Commerce.List.NoCardToCard.{title,description,action}` in `fa.json` and the app's real warning tokens `border-wline`/`bg-wtint`/`text-wtext`. Also fixes a stale-cache bug: `settings/card/page.tsx` POSTed to `/payments/cardToCard` but never called `mutate`, and both pages use `useSWRImmutable` on that key, so saving a card never cleared the notice without a hard reload. |
| `2026-08-13-productEditorNoticeAndToneFixes.update.md` | `Front/knowledge/updates/2026-08-13-productEditorNoticeAndToneFixes.update.md` | Two product-editor visual bugs. (1) `AttributesSection`'s "use مشخصات instead" notice compiles via `t.rich` to five sibling nodes, handed straight to `AlertDescription` — a flex ROW — so one sentence rendered as five narrow side-by-side columns; wrapped in a `<p>` it flows again, and a `<div>` replaces the fragment that let the notice float 24px clear and read as a footnote on the previous step. (2) `VariantNumberCell` tinted a *missing* stock amber instead of red: the tone branch keyed off `field === 'price'`, so `stock` fell into the present-but-wrong bucket. Now only `compare` is amber — verified against `productEditor.schema.ts`'s `superRefine` (`stock` raises only `stockRequired`; `price` also carries `salePriceInvalid`/`amountMax`, so its red tint is a deliberate simplification, unchanged by this fix). |
| `2026-08-27-commerceShippingMethods.update.md` | `Front/knowledge/updates/2026-08-27-commerceShippingMethods.update.md` | New «تنظیمات ارسال پستی» screen at `/products/shipping` (fourth sub-item under «کالا و خدمات»), the merchant UI for the paired Back shipping feature — **deploy-coupled**, every route it calls ships in `feat/commerce-shipping-methods` on the Back side. Cards edit local drafts and nothing is written until one header save (the API is one route per option plus a second for its exceptions, so save-per-field would fire three writes for one edit); writes run sequentially and stop at the first failure so a retry resends only what is left; deletion is the deliberate exception and runs immediately behind `DeleteConfirmationDialog`. `ShippingMethodCard` folds the API's three-way `flat`/`free_over`/`post_kerayeh` enum into the two switches a merchant actually thinks in, and **hides** the price, threshold and exceptions under پس‌کرایه rather than leaving them contradicting the mode. `RateOverrideEditor` keeps the per-city exceptions collapsed and empty by default — the sparse model exists so 1,119 cities never render as a table. Deviates from the prototype on purpose by keeping an *inactive* method editable (the design's collapsed body would strand the price that made you disable it). Prototype chrome was re-tokenised onto the shared `ProductEditor/ui/editorChrome.ts` vocabulary, not copied. Also fixes the missing `taxonomy`/`import` breadcrumb segments and adds `ICity.provinceId`. 486 tests pass (56 new); tsc identical to baseline; **not yet verified in a browser**. |
| `2026-08-31-buyInDirectAutomationContent.update.md` | `Front/knowledge/updates/2026-08-31-buyInDirectAutomationContent.update.md` | Dashboard authoring UI for the paired Back «خرید در دایرکت» (Buy in Direct) feature: new `BUY_IN_DIRECT` automation content type, product picker + dnd-kit reorder, `AutomationFormSchema` "at least one product" gate, `remapTemplateContents.ts` strip. **2026-09-01 addendum:** adds the Front translations for the two new backend error codes a later Back review pass introduced (`CONTENT_BUY_IN_DIRECT_PRODUCT_NOT_FOUND`, `CONTENT_SHOULD_HAVE_AT_LEAST_ONE_BUY_IN_DIRECT_PRODUCT`) to `messages/fa/ErrorCodes.json`, closing the doc's own previously-flagged gap that the backend had no matching validation. **2026-09-03 addendum:** fixes the picker rendering permanently empty — the catalog fetch omitted `page`, which `ReadCommerceProductsDto` requires, so the route 400'd and the component's `.catch` swallowed it. Paired Back doc: `Back/knowledge/core/commerce/instagramDmCheckout.doc.md`. |
| `2026-08-31-rateOverrideEditorMultiPick.update.md` | `Front/knowledge/updates/2026-08-31-rateOverrideEditorMultiPick.update.md` | Two user-reported bugs on the shipping-exception search box: (1) city search only ever suggested provinces — root cause on the Back (`GET /cities`'s `provinceId` required a value with no `@IsOptional()`, 400ing the no-filter fetch `useShippingDestinations` needs; see the paired `Back/knowledge/updates/2026-08-31-citiesEndpointRequiredProvinceId.update.md`); (2) picking a city then typing to search for a second one silently discarded the first pick (a single `pending` slot, wiped by the search box's own `onChange`). Replaced `pending` with `pendingList: ShippingDestination[]`: picking now appends a removable chip and clears the box for the next search, and "افزودن" commits every picked destination as its own row sharing the one price entered — a merchant pricing several cities the same enters that price once. `RateOverrideEditor.test.tsx` 11 → 16 tests, all pass; `ShippingMethodCard`/`ShippingSettings` suites unaffected; tsc/eslint clean. |
| `2026-09-02-buyInDirectPhase3b.update.md` | `Front/knowledge/updates/2026-09-02-buyInDirectPhase3b.update.md` | Buy-in-Direct **phase 3b** — the merchant orders screen for the new `commerce_order` schema, at `/products/orders` (list) and `/products/orders/[id]` (detail), as a **fifth** sub-item under «کالا و خدمات». Sits *alongside* the legacy `/orders` screen, which only knows the old `order` table; retiring that route is tracked in the outer repo's `before-prod-cutover.md`. `components/Commerce/Orders/orderTransitions.ts` mirrors the Back `ORDER_TRANSITIONS` map exactly (`ACTIONS_BY_STATUS`), with the physical/digital rule kept in `actionsFor` rather than the mirror — a digital order offered `ship` would hit a backend refusal *before* its UPDATE, returning a false "status changed" that revalidates into the same button and loops forever. `canMarkPaid` is `paidAt === null && status !== 'cancelled'`: COD orders are routinely `completed` with a null `paidAt`, and `markPaid` is the only writer of that column for them. Note the two response envelopes (`CLAUDE.md` §9): the list route returns `PaginatedResult` (`{items, meta}`) directly while every single-item write returns `ResponseMessage` (`.data`), and the SWR fetcher unwraps neither. Seven actions (approve/reject/markPaid/ship/complete/cancel/export), a receipt strip + lightbox showing all receipts newest-first, and an emailed Excel export. Also rewrote `utils/jalali.ts` onto `Intl.DateTimeFormat` over the absolute instant — `packages/ui`'s `dayjs-jalali.ts` calls `dayjs.calendar('jalali')` at module scope, and its barrel re-exports the date-picker, so any `@/components/ui` import could render year 784. 618 tests / 73 files green; `tsc` steady at the 204-error baseline; zero files under `packages/` touched. Paired Back doc: `Back/knowledge/updates/2026-09-02-buyInDirectPhase3a.update.md`. |
| `2026-09-03-buyInDirectPickerRedesign.update.md` | `Front/knowledge/updates/2026-09-03-buyInDirectPickerRedesign.update.md` | Rebuilds the `BUY_IN_DIRECT` («خرید مستقیم») product picker on the «فروش» (`PRODUCT`) content type's layout so the two neighbours in the content-type picker stop looking unrelated: square cover-tile grid, modal picker behind an «انتخاب» tile, hover «تعویض», drag reorder, cap of 10. Adds `BuyInDirectContentItem.tsx` + `BuyInDirectContentItemDialog.tsx` beside the rewritten `BuyInDirectContent.tsx`, mirroring `ProductContent{,Item,ItemDialog}.tsx` file-for-file. **The one implementation difference, and the reason for it:** «فروش» parks a real empty `{}` row in its field array as the add-target and lets the `products`→`productIds` remap drop it before submit; Buy-in-Direct has NO remap (form state IS the backend DTO), so a placeholder would be POSTed verbatim and rejected — its add tile is a virtual grid cell that is not a field-array entry. Also: search moved server-side into the dialog (`ReadCommerceProductsDto.search`), `Item` gained the nullable `minPrice`/`maxPrice` the list route already returned, and the cap of 10 is a **parity choice, not a platform limit** — the DM catalogue paginates at `DM_LIMITS.cardsPerPage = 9` plus a «موارد بیشتر» card and would serve more. 161 tests pass, mutation-proven; `tsc` 204→206, both new errors the accepted `next/image` class that both «فروش» reference files already carry. |
| `2026-09-04-ordersPageRefactor.update.md` | `Front/knowledge/updates/2026-09-04-ordersPageRefactor.update.md` | Presentation-only pass over the phase-3b `/products/orders` screen (see `2026-09-02-buyInDirectPhase3b.update.md` for the feature itself); no data, URL-filter or transition logic changed. Fixes six things that made it read as a different app from `/products` one click away: `OrderCard` was a bare `rounded-lg border` button with no `Card`/`bg-card`/shadow/image and one full-width row at every breakpoint, now `CommerceProductCard`'s exact skin in a `md:2 lg:3 2xl:4` grid with the status badge over the media and a `+N` chip counting **distinct lines** (not `itemCount`, which sums quantity); there was **no loading branch at all**, so the panel sat blank for the whole first fetch — now `LoaderSpin`, with the error branch still first and now also beating the loading branch, since SWR keeps `isLoading` true on a retry that already failed and a never-resolving spinner reads as a hang; the bare error `<div>` became `NoDataError`, which gained an **optional `message` prop** (additive, all 7 existing call sites unchanged) so the screen keeps its specific «دریافت سفارش‌ها انجام نشد» copy. `page.tsx` now passes **`md:overflow-hidden`** to `LayoutCard` so the filter bar and pager stay pinned and only the middle band scrolls — `md:min-h-0` on both the root and that band is load-bearing, without it a flex child refuses to shrink below content height and the original overflow returns. The `md:` prefix is a constraint, not caution: `packages/ui`'s `SidebarInset` is `overflow-y-auto` below `md` and only `md:overflow-hidden`, its comment stating mobile is meant to scroll at the inset level, so pinning on a phone would leave ~one card between a ~200px filter header and the pager; below `md` every band is a plain block and the page scrolls as a whole, with `min-h-[280px]` on the list band because the loader/empty/`NoDataError` all centre with `h-full`, which collapses against an auto-height parent. Filter bar made phone-usable: new module-level `DateFilterCell` (module-level so the picker is not remounted, closing its popover, on every search keystroke) whose clear button keeps its slot with `invisible` + `aria-hidden`/`tabIndex={-1}` instead of unmounting and shifting the row sideways; status chips scroll in one row below `sm`; and a clear-all control now lives in the bar whenever a filter is active — previously the only reset was in the *empty* state, so a filtered list that DID match had no way back. `OrderStatusBadge` got `dark:` pairs (translucent `/15`, the last hardcoded light-only surface here) plus an optional `className`. The load-bearing "never import `packages/ui`'s `DatePicker`" comment and all four exported helpers are preserved verbatim. 393 tests / 35 files pass; the 4 new behavioural tests were confirmed red first, and the clear-all test seeds a non-empty list on purpose because with `orders: []` the empty state's own button would satisfy it. tsc/eslint/prettier clean on every touched file. **Not yet verified in a browser.** |
| `2026-09-04-ordersScreenRedesign.update.md` | `Front/knowledge/updates/2026-09-04-ordersScreenRedesign.update.md` | **Supersedes `2026-09-04-ordersPageRefactor.update.md`** (one day old) — the card grid becomes a real table. Full design: `docs/superpowers/specs/2026-09-04-orders-screen-redesign-design.md`. List: `OrderCard` deleted; `OrdersTable.tsx` (`md`+, six columns, no id column) and `OrderRowCard.tsx` (below `md`) both derive every field from one shared `orderRowFields.ts` so the two renderings cannot drift; the breakpoint switch is CSS (`hidden md:table`/`md:hidden`), not a `useMediaQuery` hook, which would paint the wrong layout on first render and flash. `OrderThumbs.tsx` adds the کارت‌به‌کارت receipt thumbnail to each row (paired Back change: `OrderListView`'s `receiptUrl`/`receiptCount`), opening `ReceiptLightbox` in place so a seller can judge and clear `awaiting_review` orders without opening each one. Detail: `OrderDetail.tsx` becomes `lg:grid-cols-[1fr_320px]` with `OrderSummaryRail.tsx` declared FIRST in the DOM and pushed to the second column via `lg:order-2` — source order (what a phone/screen-reader follows) puts the decision rail first at every width; visual order (`order-2`) is what desktop alone gets to override. Three new reference cards (`OrderBuyerCard`/`OrderItemsCard`/`OrderTotalsCard`) replace the flat `border-t` sections; `OrderActions.tsx` and the mobile sticky action bar are deleted. Six action buttons become one status `<Select>` + «بروزرسانی» confirm dialog: `orderTransitions.ts` gains `targetStatusesFor`/`actionForTransition`, and «لغو شده» resolves to `reject` (reason required) from `awaiting_review` but `cancel` (restock warning) from `processing`/`sending`, since one status is reachable by two different actions depending on where the order is now. Two deliberate behaviour changes: `approve` now needs confirmation (was one click), and `markPaid` gained a confirm dialog (irreversible — no un-mark endpoint). **Known limitation, not fixed here:** a pickup order cannot say where to collect — `pickupAddress` lives only on the live, mutable `commerce_shipping_option`, and `commerce_order` freezes title/kind/settlement at promotion but stores no `shippingOptionId`, so `OrderBuyerCard` states the collection point is not recorded rather than showing the buyer's home address as a delivery destination; real fix (freeze `shippingPickupAddress` onto `commerce_order`) is a separate follow-up. **Closed by `2026-09-05-orderFollowUpAndShipping.update.md`** — the Back added a snapshotted `pickupAddress` to both `commerce_order` and `commerce_cart`, and `OrderSummaryRail`'s new tracking row reads it. Leaves the orphaned `Commerce.Orders.detail.notPaid` fa.json key in place (no longer read). Verified: Back `orders` jest 12 suites/188 tests, Back `tsc` 63 (baseline, none new); Front `vitest run src/components/Commerce/` 40 files/434 tests, Front `tsc` 206 (at the measured baseline ceiling). **Not yet verified in a browser.** Paired Back doc: `Back/knowledge/updates/2026-09-04-orderListViewReceipts.update.md`. |
| `2026-09-06-digitalProductsFinalMessage.update.md` | `Front/knowledge/updates/2026-09-06-digitalProductsFinalMessage.update.md` | Digital products + final message feature (Tasks 6-12 of the commerce-product-core epic). Extends the product editor with a physical/digital kind choice (**the `ChooseProductKindDialog` and `?kind=` param described here were REMOVED on 2026-09-07 — see `2026-09-07-productKindDialogRemovalAndInfiniteStock.update.md`**; the `kind` field and `ProductKindSection` remain), and a final-message textarea for completion DMs sent to buyers on order completion. Schema gains `kind: CommerceProductKind` and `finalMessage: string` fields to `ProductFormValues` with zod validation; mapping layer wires both through create/update payloads (asymmetric: create omits blank message, update sends null). New `ProductKindSection` (two cards) and `FinalMessageSection` (textarea + counter) rendered as editor steps 1 and 11 respectively. New i18n namespaces: `Commerce.Editor.Kind`, `Commerce.Editor.FinalMessage`, `Commerce.List.ChooseKind`, plus `Validation.finalMessageMax`. All editor + product-list test suites passing, tsc clean on all touched files. Paired Back doc: `Back/knowledge/updates/2026-09-06-digitalProductsFinalMessage.update.md`. |
| `2026-09-06-followUpCodeAsText.update.md` | `Front/knowledge/updates/2026-09-06-followUpCodeAsText.update.md` | Reshapes the row below: `trackingUrl` renamed to `followUpCode` throughout (`OrderView`, both dialogs, `OrderSummaryRail`), URL validation (`isValidTrackingUrl`, deleted) replaced by `isValidFollowUpCode` (English alnum+hyphen, max 50, mirrors Back's `@Matches`) plus a letter-preserving Persian-digit normalizer (`normalizeFollowUpCodeInput`) distinct from the dashboard's own digit-stripping `p2eNumber.ts`. `OrderSummaryRail`'s tracking value stops being a clickable `<a>` link and becomes plain copyable text — a code is pasted into a tracker, never tapped. 178 tests / 18 files pass, tsc clean on every touched file. |
| `2026-09-05-orderFollowUpAndShipping.update.md` | `Front/knowledge/updates/2026-09-05-orderFollowUpAndShipping.update.md` | Front half of the DM order follow-up/shipping feature (Tasks 15-18, `feat/dm-order-followup`, unmerged). `DescriptionSection.tsx` becomes a plain 60-char `<textarea>` (was a `contentEditable` markdown WYSIWYG with a `lastPushed` push/pull arbitration; `utils/markdown.util.ts` deleted) — the description is now plain text with nothing markdown left for any future storefront render to strip. New `ShipOrderDialog` on `processing → sending` collects an optional http(s)-only carrier tracking URL, hidden for `shippingKind === 'pickup'`. New tracking row on `OrderSummaryRail` (visible when `status IN (sending, completed) && shippingKind !== 'pickup'`) plus `EditTrackingDialog` → `PATCH /commerce/orders/:id/tracking`, `notify` defaulting off. `ShippingMethodCard`/`shippingDraft.ts` gain `SETTLEMENTS_BY_KIND` to stop offering «پس‌کرایه» on «تحویل حضوری» and make the «پرداخت در محل» note kind-aware (`settlementNotes.cash_on_delivery_pickup`). Records the vocabulary trap («پرداخت در محل» means courier-collects on a پیک/پست but pay-at-counter on تحویل حضوری — `kind` alone decides posting, never `settlement`), the decision to keep `fireEvent` over `@testing-library/user-event`, and two known gaps: `EditTrackingDialog`'s re-seed effect can silently wipe an in-progress edit on a concurrent update, and `ShipOrderDialog.reset()` omits `setIsSubmitting(false)` unlike the `RejectPaymentDialog` pattern it copies. 80 test files / 716 tests pass (`pnpm test -- src/components/Commerce`, apps/dashboard — note the path filter does not scope this repo's vitest run; a same-session unfiltered `pnpm test` produces the identical count). |
| `2026-09-07-productKindDialogRemovalAndInfiniteStock.update.md` | `Front/knowledge/updates/2026-09-07-productKindDialogRemovalAndInfiniteStock.update.md` | **Supersedes the "Add product" half of `2026-09-06-digitalProductsFinalMessage.update.md`.** Deletes `ChooseProductKindDialog` (and the `?kind=` query param nothing produced any more): «افزودن محصول» pushes `/products/add` directly, `kind` starts `physical`, and editor step ۱ (`ProductKindSection`, unchanged) is the only place it is ever picked — the dialog asked a mandatory question about a control the merchant could change one screen later anyway. The load-bearing half is the second fix: `BasePriceSection`/`BaseStockSection` wrote **only** `basePrice`/`baseCompare`/`baseStock`, whose sole reader is `useVariantSync` when it GENERATES rows — and generation only runs on an axis edit (`combosOf` returns `[]` with no axes). So for a product that never got a رنگ/سایز the numbers typed at the top of the page reached no variant at all: the real price/stock live on the one implicit variation (`buildEmptyProductForm`'s blank row, shown in the grid as «محصول بدون ویژگی»), zod then failed `priceRequired`/`stockRequired` on `variants.0.*` and jumped the merchant down to retype what they had just typed. Unlocked (no live axis), both cards now read AND write `variants[0]` while still mirroring the seeds — the implicit row carries no option values so it never matches `donorOf`, and the seeds are what rows generated by the FIRST axis inherit. New `baseInfinite` field + «موجودی نامحدود» switch on the quantity card (maps to `trackInventory: false` through the existing `variants[].infinite` path, **no backend change**); turning it on clears the count, since a leftover would be sent as `initialStock` and rewrite an untracked variant's ledger. `useVariantSync` seeds `infinite` onto EVERY generated row, not just the first — ∞ is a tracking MODE, not a quantity, the rule its own donor branch already applied. `BasePriceSection`'s `useState`/`useEffect`/`lastLocal` discount-switch arbitration is deleted: bound to the row, the stored `hasDiscount` flag answers directly. New `BaseStockSection.test.tsx` (12 tests) where there was none; `BasePriceSection` suite rewritten to carry a real implicit row and probe row-vs-seed (16 tests). Verified: 45 files / 518 tests green across `src/components/Commerce`, `tsc` clean on every touched file (the ~165 pre-existing `src/` errors unchanged). **Also carries a same-day follow-up section fixing two PRE-EXISTING variation-grid bugs the user found on the deployed build.** (A) "an error without explanation" when setting an offer: `compareInvalid` BLOCKS the save but `VariantNumberCell` tinted `compare` `zero` — which `globals.css` defines by severity as the soft-warning amber — while the failure toast said «موردهای قرمز را درست کنید», so the merchant hunted for a red cell that did not exist; and the message itself was never rendered anywhere, the grid having no message line and `onInvalid` throwing the reason away for a fixed string. Now any real zod issue tints red (the amber `tone` prop keeps its real job, the live pre-submit hint), the reason rides on the cell `title`, and `firstErrorPath` → `firstError` returns `{path, message}` so the toast carries the schema's own translated reason. Same trace also killed that function's bare `variants.<i>.stock` fallback, which focused a healthy stock cell whenever a row failed on `saleStartsAt`/`saleEndsAt` (no control exists for either). (B) "didn't split the price when I'm typing": the grid cells are UNCONTROLLED (`register`, for 2000-row perf) so nothing re-renders them mid-type and `onInputP2EHandler` strips separators every keystroke — new `formatAmountInPlace` writes the formatted value straight back, restoring the caret **by digit count, not character index** (character counting drifts one per separator and walks the caret backwards as the number grows). Both reproduced as failing tests first; suite 518 → **530**. **Not verified in a browser.** |
| `2026-09-07-orderFreeStatusChoice.update.md` | `Front/knowledge/updates/2026-09-07-orderFreeStatusChoice.update.md` | **The order status select now offers EVERY status from every status**, the one omission being «ارسال شده» on a digital order — asked for directly by the seller, who could not mark an order already handed to a courier as «ارسال شده» without first walking it through «در حال آماده‌سازی», and for whom a mis-clicked «تکمیل شده»/«لغو شده» was permanent (both were dead ends with the select disabled). `ACTIONS_BY_STATUS` is a mirror of Back's `ORDER_TRANSITIONS` and was widened in lockstep, plus a new `revert` action (`→ در انتظار بررسی`, the first backward move, `POST :id/revert`) with its own `ConfirmActionDialog` — its consequence differs by source (the backend restocks out of `processing`/`sending`/`completed`, moves nothing out of `cancelled`), so the copy states both rather than promising one. **The safety is all on the backend** (stock follows the STATUS, not the edge — see the paired Back doc); this side only stops offering transitions the API would refuse. Two smaller consequences: `targetStatusesFor` now sorts through a `TARGET_ORDER` constant and dedupes, because with four actions per row the select's order would otherwise shuffle depending on which order the seller opened (and `reject`/`cancel` both target `cancelled`); and no status is terminal any more, so `hasAnyAction` is always true, the «this order is closed» line never renders, and `isTerminal` survives only as a guard against an empty Radix select. Corrected stale copy: `dialogs.complete.description` said «این تغییر برگشت‌پذیر نیست», which stopped being true and would have been the most misleading string on the screen. New `COMMERCE_INSUFFICIENT_STOCK_FOR_STATUS` key in `messages/fa/ErrorCodes.json`. Verified: 19 files / **197 tests** (was 190), `tsc` clean on every touched file. **Not clicked through in a browser.** Paired Back doc: `Back/knowledge/updates/2026-09-07-orderFreeStatusChoice.update.md`. |
| `2026-09-09-orderPrintDocumentsCommerce.update.md` | `Front/knowledge/updates/2026-09-09-orderPrintDocumentsCommerce.update.md` | Adds print-shipping-label / print-invoice buttons to `OrderDetail.tsx` (label disabled for digital orders), backed by new pure HTML-string builders under `components/Commerce/Orders/print/` (`buildLabelDocument`/`buildInvoiceDocument`/`printDocument` — no PDF library, browser print-to-PDF is the PDF). The seller's own return address is set at `/products/shop` under "کالا و خدمات" (`ShopAddressSettings.tsx`, `components/Commerce/Shop/`) — **same-day rearchitecture**: originally a per-Instagram-account `ShopAddressDialog` opened from a 4th `InstagramAccounts.tsx` button, replaced by this workspace-wide settings page once the Back side moved `ShopAddress` from `instagramId`-keyed to `workspaceId`-keyed (a workspace has ONE return address, not one per connected page). Two real bugs found+fixed in the original dialog's account-switch/province-change logic (carried over into the new page for the parts still applicable — the province-change one): react-hook-form's `reset()`/`setValue()` don't reliably clear a manually-controlled `Controller`-bound Select on an explicit `undefined` (only plain registered inputs handle it) — one instance was a real cross-account data leak (previous account's city stayed submittable, moot now that there's only one address), the other was display-only (submitted data was always correct) and still applies to any province change. Front half of the Back `CommerceShopAddress`/`ShopAddress` entity+endpoints and the widened `GET /commerce/orders/:id` `shop` block (Back commits `a4e996e0`..`e8fb5660` on `feat/commerce-product-core`) — paired Back docs: `Back/knowledge/updates/2026-09-09-orderPrintDocumentsCommerce.update.md`, `Back/knowledge/core/commerce/shopAddress.doc.md`. |
| `2026-09-10-printLabelResponsivePaper.update.md` | `Front/knowledge/updates/2026-09-10-printLabelResponsivePaper.update.md` | Makes the چاپ برچسب (shipping label) document scale to the sheet it is printed on, instead of being frozen to A5. `buildLabelDocument` moves from `@page { size: A5 portrait }` + a hard-coded `.lbl { height: 190mm }` to `@page { size: auto }` + `height: 100%`, so the print dialog's paper selector is live. Key insight: **in print the media viewport IS the page box**, so `vmin` is the short edge of the actual sheet — which only works because `size: auto` stops pinning it. **Scale is continuous, structure is by breakpoint**: two units, `--su: calc(100vmin / 148)` (space/boxes/logo, pure proportion) and `--tu: max(0.82mm, 100vmin / 148)` (type, with a legibility floor), and every dimension is a multiple of one of them. 148 = A5 portrait's short edge, so both are exactly 1mm there and every multiplier is the millimetre number the label used to hard-code. **A first attempt used five media-query size buckets and was wrong upward** — A3, A2, A1 and A0 all matched the same `min-width: 180mm` rule and printed A4-sized type on sheets up to 16× the area; buckets step, they do not scale. Only genuinely non-continuous decisions stay as queries (landscape → two-column sender/receiver grid with brand+sticker below their own party, which is what makes a 150×100 thermal roll fit; narrow-landscape → one field per row; short sheet → tighter leading to pay for the type floor). The floor is also the failure mode: the label prints in a hidden 0×0 iframe, and if `vmin` ever resolved against that instead of the page box, `max()` still yields 0.82mm so it prints small-but-correct rather than collapsing. `documentShell()` gained `'auto'` (zero @page margin); the A4 invoice is untouched. Verified by rendering two fixtures to PDF in Chromium across thermal/A7/A6/A5/A4/Letter/A3/A2/A1/A0 × portrait+landscape, counting PDF pages AND measuring body type per sheet (2.79mm floor ≤A6, 3.40mm A5 = the original, up to 19.32mm at A0, each A-step √2 apart). A5 pixel-diffed against a pre-change render: geometrically identical to within −0.066% (browser rounds the page box to whole CSS pixels), NOT bit-identical. A7 still overflows, deliberately. |
| `2026-09-09-commerceStoreSettings.update.md` | `Front/knowledge/updates/2026-09-09-commerceStoreSettings.update.md` | New "تنظیمات فروشگاه" page at `/products/settings` (sibling of `/products/shipping`) for a workspace-level default `finalMessage` (`useStoreSettings`, `StoreSettings.tsx`). Product editor, CREATE mode only: a new PHYSICAL product's `finalMessage` is prefilled from this default once the settings fetch settles — a one-time snapshot (`ProductEditorPage`'s seeding effect, same shape as the existing collection-membership seed), never re-applied later; `FinalMessageSection` shows a hint + settings link whenever prefilled. `buildProductEditorSchema` gains a `.superRefine` requiring `finalMessage` whenever `kind === 'digital'` (mirrors the backend's `@ValidateIf`), which also required fixing one pre-existing schema test that asserted a digital product with a blank `finalMessage` was valid. No breadcrumb change needed — `/products/settings`'s last segment already resolves through the existing generic `Breadcrumbs.settings` key. 4 new `ProductEditorPage.test.tsx` cases, new `StoreSettings.test.tsx`, extended `FinalMessageSection.test.tsx`/`productEditor.schema.test.ts`; `src/components/Commerce` 48 files / 583 tests pass; tsc/eslint/prettier clean on every touched file. Paired Back doc: `Back/knowledge/updates/2026-09-09-commerceStoreSettings.update.md`. **Superseded 2026-09-10, see `2026-09-10-storeSettingsPrefillStaleCacheFix.update.md` below**: the prefill's "seeded once" ref was found to be a real bug — SWR can settle `isLoading:false` with a STALE cached value first (from an earlier visit in the same SPA session), and the one-shot latch then permanently skipped the real default once it arrived a moment later, leaving the field blank under a hint that already said it was prefilled. Fixed by dropping the latch: the effect now reruns on every `storeSettings` change and keeps applying as long as the field is still blank. |
| `2026-09-10-storeSettingsPrefillStaleCacheFix.update.md` | `Front/knowledge/updates/2026-09-10-storeSettingsPrefillStaleCacheFix.update.md` | Fixes the exact bug above: `ProductEditorPage`'s final-message prefill `useEffect` dropped its one-shot `storeSettingsSeeded` ref latch — it now reruns on every `useStoreSettings` change and keeps applying the default as long as `finalMessage` is still blank (that blank check alone is what stops it from clobbering merchant-typed text or a kind switch, no separate "already ran" flag needed). New regression test using a STATEFUL mock (not `mockReturnValueOnce`, which drains before assertions run because the component re-renders itself several times during mount) — confirmed it fails pre-fix, passes post-fix. Verified live via the real click path (settings page → product list → "کالای جدید", client-side nav, no reload). |
| `2026-09-10-shopAddressIntoStoreSettings.update.md` | `Front/knowledge/updates/2026-09-10-shopAddressIntoStoreSettings.update.md` | "آدرس فروشگاه" (`ShopAddressSettings.tsx`) and "تنظیمات فروشگاه" (`StoreSettings.tsx`) merged onto one page: `/products/settings` now renders both as two `<section>`s (each keeping its own fetch/save/permission gate — presentation-only composition, not a merged form); `/products/shop` is now a `redirect('/products/settings')` (same pattern as `/settings/page.tsx`), not deleted, so an old link still lands somewhere useful. `ConsoleSidebar.tsx` drops the separate `productsShop` sub-item; the now-dead `Sidebar.productsShop` key was removed from `fa/Console.json`. Front-only, no backend change. Verified live: both sections render real data, the redirect works, sidebar shows one item. |
| `2026-09-10-variantGridMobileReflowFix.update.md` | `Front/knowledge/updates/2026-09-10-variantGridMobileReflowFix.update.md` | Fixes a real mobile-only bug in the variant grid: an unwrapped Radix `<Checkbox>` (which renders a hidden native `<input>` as an invisible sibling) threw off every `nth-child`-based `grid-template-areas` cell below 760px, silently rendering the stock input inside the delete-button column. Fix wraps every checkbox in its own `data-cell="chk"` element and replaces the whole mobile layout with a stacked flex list keyed by `data-cell` (`chk`/`label`/`media`/`price`/`cmp`/`stock`), each carrying its own inline field label since the desktop column headers are hidden at that width. `VariantGroupRow.tsx`/`VariantLeafRow.tsx`/`VariantsSection.tsx`/`globals.css` touched. 263 `src/components/Commerce` tests pass unchanged, `tsc` clean on touched files. **Not re-verified live in a mobile browser** — jsdom has no viewport to exercise the reflow. |
| `2026-09-10-bulkImportHidden.update.md` | `Front/knowledge/updates/2026-09-10-bulkImportHidden.update.md` | Hides bulk product import (وارد کردن گروهی) without deleting it: the `/products/import` sidebar entry is commented out and the page itself returns `notFound()` (real 404), while `ImportWizard.tsx`/`useImportJobPolling.ts` and their tests stay in the tree untouched. Paired with the Back change that unregisters `ImportController`, so `POST /commerce/import` and `GET /commerce/import/:jobId` 404 as well. Lists the exact three steps to turn it all back on. |
