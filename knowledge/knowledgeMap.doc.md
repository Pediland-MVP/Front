# Front Knowledge Map

This document indexes all documentation files inside the `Front/knowledge` directory.

| File Name / Folder | Path | Description |
|---|---|---|
| `front-back-relations.md` | `Front/knowledge/front-back-relations.md` | Documents the relationships and API dependencies between the Front and the Back projects. |
| `knowledgeMap.doc.md` | `Front/knowledge/knowledgeMap.doc.md` | Indexes and describes all documentation files in this folder. |
| `updates/` | `Front/knowledge/updates/` | Folder containing date-stamped markdown updates for individual features and bug fixes. |
| `2026-07-12-subscriptionDisplayPolish.update.md` | `Front/knowledge/updates/2026-07-12-subscriptionDisplayPolish.update.md` | Summary of subscription validity merging and reserved subscription UI polish. |
| `2026-07-12-userDetailsCardSubscriptionInfo.update.md` | `Front/knowledge/updates/2026-07-12-userDetailsCardSubscriptionInfo.update.md` | Summary of removing the top-level credit/renewal row and displaying remaining subscription days next to Instagram profiles. |
| `2026-07-13-mobileExportFormsResponsiveness.update.md` | `Front/knowledge/updates/2026-07-13-mobileExportFormsResponsiveness.update.md` | Summary of the shared Drawer/DialogStyled scroll fix and the date-picker font-size fix (iOS Safari zoom-on-focus) across the Sessions/Orders/Contacts excel-export forms and the Contacts edit dialog. |
| `2026-07-13-hideSubscriptionFromWebView.update.md` | `Front/knowledge/updates/2026-07-13-hideSubscriptionFromWebView.update.md` | Summary of detecting the Android WebView via the UA `wv` marker and hiding credit-type subscription info plus all navigation to `/settings/subscription` in that context. |
| `2026-07-13-installSection.update.md` | `Front/knowledge/updates/2026-07-13-installSection.update.md` | Summary of the new `/install` page (Android store links / iOS PWA-install steps by device) and its sidebar entry. |
| `2026-07-13-automationCopy.update.md` | `Front/knowledge/updates/2026-07-13-automationCopy.update.md` | Summary of the "Copy" button on automation cards that pre-fills `/automations/add` from an existing automation's full content (including Instagram accounts), always creating a new, independent automation. |
| `2026-07-13-accountSessions.update.md` | `Front/knowledge/updates/2026-07-13-accountSessions.update.md` | Summary of the new `/settings/account-session-management` ("نشست‌های فعال حساب") page that lists the user's active login sessions (`GET /auth/sessions`), pins the current device, and terminates sessions older than 5 days (`DELETE /auth/sessions/:id`). |
| `2026-07-14-sidebarNavPendingSpinner.update.md` | `Front/knowledge/updates/2026-07-14-sidebarNavPendingSpinner.update.md` | Summary of swapping the sidebar/bottom-nav icon and dashboard home stat-card icons for a spinning `CircleNotchIcon` while a clicked link's target route is pending, via Next.js's `useLinkStatus` hook. |
| `2026-07-14-workspaceCategories.update.md` | `Front/knowledge/updates/2026-07-14-workspaceCategories.update.md` | Summary of the workspace categories feature: admin CRUD page + category column/filter on the admin workspaces page, and the dashboard's required category on workspace creation/onboarding plus the forced-category guard for legacy workspaces. |
| `2026-07-14-bannerCarousel.update.md` | `Front/knowledge/updates/2026-07-14-bannerCarousel.update.md` | Summary of the banner carousel feature: admin CRUD page for banners with category multi-select, and the dashboard carousel component that fetches active banners, shuffles them, and auto-advances every 5s. |
| `workspace-categories/` (admin) | `Front/apps/admin/src/app/(main)/workspace-categories/` | Admin CRUD page for the workspace-category taxonomy: `page.tsx` renders `client-page.tsx`, which drives a paginated/searchable `/workspace-categories` SWR query (matching the `workspaces` page's list style) and passes data to `workspace-categories-table.tsx` (search input, add/edit dialog with `nameFa`/`nameEn` fields via `react-hook-form` + `zod`, delete with confirm) and `columns.tsx` (`useWorkspaceCategoryColumns` — `nameFa`, `nameEn`, `workspaceCount`, `createDate`, row actions). |
| `banners/` (admin) | `Front/apps/admin/src/app/(main)/banners/` | Admin CRUD page for banners: `page.tsx` renders `client-page.tsx`, which drives a paginated/searchable `/banners` SWR query and passes data to `banners-table.tsx` (search input, add/edit dialog with title/description bilingual fields, buttons field array, and category multi-select via `react-hook-form` + `zod`, delete with confirm) and `columns.tsx` (`useBannerColumns` — `title`, `category`, `isActive`, `order`, `createDate`, row actions). |
| `workspaces/{columns,workspace-table,client-page}.tsx` (admin, category addition) | `Front/apps/admin/src/app/(main)/workspaces/` | The existing admin workspaces list gained a `category` column (`columns.tsx`, shows `category.nameFa` or a "no category" placeholder) and a category filter `Select` (`workspace-table.tsx`, sourced from `/workspace-categories?limit=100`); `client-page.tsx` holds the `categoryId` filter state and appends it to the `/workspaces` query string. |
| `useWorkspaceCategories.ts` | `Front/apps/dashboard/src/hooks/useWorkspaceCategories.ts` | SWR hook wrapping `GET /workspace-categories` (`{ id, nameEn, nameFa }[]`), shared by the "new workspace" creation dialog, the onboarding form, and `WorkspaceCategoryGuard` — the single source of the category list on the dashboard. |
| `WorkspaceCategoryGuard.tsx` | `Front/apps/dashboard/src/components/Console/WorkspaceCategoryGuard.tsx` | Non-cancelable dialog (no close button, `Escape`/outside-click suppressed) mounted in `(Console)/layout.tsx` next to `WorkspaceSessionGuard`. Renders only when the active workspace has no `category` **and** the current user is its `ownerId` (non-owners are never blocked); submits `PATCH /workspaces/:id` with the chosen `categoryId` and revalidates `useWorkspaces()`. |
| `useActiveBanners.ts` | `Front/apps/dashboard/src/hooks/useActiveBanners.ts` | SWR hook wrapping `GET /banners/active` (`{ id, titleEn, titleFa, descriptionEn, descriptionFa, color, buttons: [{ id, textEn, textFa, url, isExternal }] }[]` — no `categoryId(s)`/`isActive`, by design: the endpoint never reveals why a banner was selected), shared by the dashboard home carousel component. Handles error states and provides fallback empty array. |
| `DashboardBannerCarousel.tsx` | `Front/apps/dashboard/src/components/Console/Dashboard/DashboardBannerCarousel.tsx` | Carousel component that renders active banners fetched from `useActiveBanners`, using `StableCarousel` (with `StableCarouselPrevious`/`StableCarouselNext` nav buttons) with shuffle-once-per-load and auto-advance every 5s. Each banner displays bilingual title/description, background color, and clickable buttons (internal page links or external URLs). Replaces the deleted `DashboardHelpBanner.tsx` in the dashboard home. |
| `2026-07-14-freeAutomationQuotaWarningDialog.update.md` | `Front/knowledge/updates/2026-07-14-freeAutomationQuotaWarningDialog.update.md` | Summary of the `AutomationForm` confirmation dialog that warns before the automation that would push a selected Instagram page over its free automation quota, using new `automationCount`/`freeAutomationLimit`/`freeAutomationQuotaExceeded` fields on `GET /instagram/accounts` (paired Back branch `feat/free-automation-quota`). |
| `2026-07-14-templateSystemFrontend.update.md` | `Front/knowledge/updates/2026-07-14-templateSystemFrontend.update.md` | Summary of the Template System feature: the shared `packages/ui/src/automation-builder` extraction (`AutomationBuilder` with `mode='automation'\|'template'`), the admin `/templates` CRUD pages, the dashboard Create-Automation-from-template picker, `?templateId=` prefill, and the in-form "Template" content-type insert. |
| `2026-07-16-delayContentSelectbox.update.md` | `Front/knowledge/updates/2026-07-16-delayContentSelectbox.update.md` | Summary of the shared 23-hour delay budget feature: `DelayContent.tsx`'s free-text magnitude input replaced by a budget-aware `<Select>`, `Contents.tsx` blocking a new DELAY item once the budget is used up (`DelayBudgetExhaustedDialog`), and an admin `TemplateForm.tsx` submit-time 23h-sum guard added for parity with the dashboard's existing check. Frontend-only, no backend changes. |
| `2026-07-17-instagramPostButton.update.md` | `Front/knowledge/updates/2026-07-17-instagramPostButton.update.md` | Summary of the "Instagram Post" button type: reuses the "پست خاص" select-post dialog (generalized to a headless/controlled mode) to fill a URL button's link from a picked post's permalink, with the same single-Instagram-account guard, no backend changes. |
| `automation-builder/` (`@befroosh/ui`) | `Front/packages/ui/src/automation-builder/` | Shared automation-authoring builder, extracted from the dashboard's `AutomationForm` so both the dashboard (real automations) and the admin templates UI render the same form. `AutomationBuilder.tsx` owns the `react-hook-form` instance (`AutomationFormSchema` from `schemas/automationForm.ts`) and takes a `mode: 'automation' \| 'template'` prop (`'template'` renders only Conditions/Triggers/Contents, skipping JustFollowers/comment-trigger — templates have no workspace/live-automation context), an `apiClient: { upload, get }` (`types/apiClient.ts`) the caller injects (dashboard: `apps/dashboard/src/lib/automationApiClient.ts`; admin: `apps/admin/src/lib/templateAutomationApiClient.ts`), plus `initialValue`/`onSubmit`/`beforeSubmit`/`onInvalid`/`headerSlot`/`helpSlots`/`hasInstagram`/`isPromotion`/`commentRepliesSlot`. `Contents/` holds every content-type editor plus `remapTemplateContents.ts` (strips server ids/timestamps/back-refs and workspace-scoped product/Instagram-post refs, re-derives `delayUnit`/vitrin image fields/button `postbackPayloadType`, so a template's `contents[]` can be safely appended into another automation/template). `TemplatePicker/` (`TemplatePicker.tsx` + `TemplateCard.tsx`) is a shared search+grid (Dialog/Drawer) reused by the dashboard's create-automation flow and the in-form "Template" content-type insert (`Contents.tsx`, hidden in `mode="template"` and in reminder-mode Contents trees). `utils/delayBudget.ts` is a pure helper computing the shared 23-hour DELAY-item budget math (`remainingDelayBudgetMs`, `delayUnitOptionsCount`) consumed by `DelayContent.tsx` (magnitude `<Select>` option range) and `Contents.tsx` (blocking a new DELAY item once the budget is exhausted) — see `2026-07-16-delayContentSelectbox.update.md`. **Updated 2026-08-02:** `QuestionContent.tsx`'s `validationType` dropdown (`types/validationType.enum.ts`) gained an `Image` option — restricts the lead's DM reply to Instagram image attachments (paired Back change validates `message.attachments`, not `message.text`). See `2026-08-02-questionImageValidation.update.md`. **Updated 2026-08-02 (same day, PostalCode):** the same dropdown gained a `PostalCode` option (Iranian postal code regex validation, paired Back change, no attachments involved — a plain text validator like `Mobile`/`NationalCode`). See `2026-08-02-questionPostalCodeValidation.update.md`. **Updated 2026-08-03 (BEF-164):** `DelayContent.tsx`'s number/unit `Select` fields swapped to render number-first (was reading "hour 1" in the RTL UI instead of "1 hour"); `MediaUploader.tsx`'s audio `acceptedFormats` gained `audio/mp3`. See `2026-08-03-bef164AutomationDelayAndMp3Fix.update.md`. |
| `templates/` (admin) | `Front/apps/admin/src/app/(main)/templates/` | Admin CRUD pages for templates, mirroring the `workspace-categories` admin pattern: `page.tsx`/`client-page.tsx` drive a paginated/searchable `/templates` SWR query into `templates-table.tsx` (thumbnail, title, description, categories/"applies to all" badge, row actions) + `columns.tsx`. `add/page.tsx` and `[id]/page.tsx` both render `TemplateForm.tsx`, which wraps `AutomationBuilder mode="template"` with a `headerSlot` holding a separate `metaForm` (title, description, "applies to all categories" switch + category multi-select from `/workspace-categories`, thumbnail upload via `POST /templates/:id/upload-image`) merged into one payload at submit (`POST`/`PATCH /templates[/:id]`). Editing prefills from `GET /templates/:id`'s raw `ContentCycle` graph via `transformTemplateToFormValues`. |
| Dashboard Create-Automation-from-template | `Front/apps/dashboard/src/components/Automations/CreateAutomationTemplateDialog.tsx`, `Front/apps/dashboard/src/app/(Console)/automations/add/page.tsx`, `Front/apps/dashboard/src/components/Automations/AutomationForm.tsx` | As of `2026-07-18-automationSkipTemplatePicker.update.md`, `automations/page.tsx`'s "create automation" button no longer opens `CreateAutomationTemplateDialog` — it (and the draft-flow "create new" branch) navigates straight to plain `/automations/add`, same as the dialog's old "start from scratch" button and the dashboard quick-action link. `CreateAutomationTemplateDialog.tsx` (listing `GET /templates?search=` via the shared `TemplatePicker`, selecting one navigates to `/automations/add?templateId=<id>`) and the in-form "Template" content-type insert (`Contents.tsx`) still use `TemplatePicker` — only the create-automation entry point stopped opening this dialog. The `add/page.tsx` route reads and UUID-validates `?templateId=` alongside the existing `?copyFrom=`; `AutomationForm.tsx` accepts a `templateId` prop and — only for a brand-new automation (no `id`/`copyFromId`) — fetches `GET /templates/:id` and feeds it through the same `initialValue` transform as `copyFromId`, silently (no toast, since nothing is persisted yet). |
| `2026-07-17-httpsLinkNormalization.update.md` | `Front/knowledge/updates/2026-07-17-httpsLinkNormalization.update.md` | Summary of the submit-time zod transforms that upgrade user-entered `http://` links to `https://` (and add a scheme to bare-domain button urls) across the shared automation form (including message fields from Task 2b) and the dashboard product form. |
| `2026-07-18-subscriptionBuyButtonVisibility.update.md` | `Front/knowledge/updates/2026-07-18-subscriptionBuyButtonVisibility.update.md` | Summary of fixing the invisible "خرید اشتراک" button on `/settings/subscription` — `bg-violet-650` in `ChoosePlan.tsx` isn't a real Tailwind class, so no background CSS was generated; changed to `bg-violet-600`. |
| `2026-07-18-buttonTemplateUiSizing.update.md` | `Front/knowledge/updates/2026-07-18-buttonTemplateUiSizing.update.md` | Summary of the sizing/alignment cleanup of the shared button-row editor (`ContentButtonsItem.tsx`) used by `buttonTemplate`/`text`/`question`/`vitrin` content buttons — fixed type-select width jumping, type+title not sharing a row, asymmetric card padding from a `-mt-2` hack, inconsistent per-card top padding, and a mismatched icon-button height next to the URL input. No functional/behavior changes. |
| `2026-07-18-automationSkipTemplatePicker.update.md` | `Front/knowledge/updates/2026-07-18-automationSkipTemplatePicker.update.md` | Summary of removing the template-picker step from the "create automation" flow — clicking the button on `automations/page.tsx` now navigates straight to `/automations/add` instead of opening `CreateAutomationTemplateDialog`, matching the dialog's old "start from scratch" button. `TemplatePicker`/`CreateAutomationTemplateDialog` left in place for other uses. |
| `2026-07-18-vitrinOptionalTitleDescription.update.md` | `Front/knowledge/updates/2026-07-18-vitrinOptionalTitleDescription.update.md` | Summary of making vitrin (generic template) `title`/`description` optional in `VitrinItemSchema` to match the backend DTO relaxation, plus updated "(اختیاری)" placeholders and a 5-row description `Textarea`. |
| `2026-07-18-autoConsentQuickReply.update.md` | `Front/knowledge/updates/2026-07-18-autoConsentQuickReply.update.md` | Summary of auto-inserting a `CONSENT` ("مکث و ادامه") quick reply at index 0 of any TEXT content that has quick replies and is no longer the last content in `contents` — prevents Instagram from hiding that content's buttons once another content follows it. Reactive `useEffect` in `Contents.tsx`, `contents` array only (not `reminders`), no backend change. |
| `2026-07-18-lockConsentQuickReply.update.md` | `Front/knowledge/updates/2026-07-18-lockConsentQuickReply.update.md` | Follow-up to `2026-07-18-autoConsentQuickReply.update.md`: locks removal of a `CONSENT` quick reply in `ContentButtonsItem.tsx` while its content still has a following content (live computed condition, not a stored flag) — clicking remove shows an explanatory `AlertDialog` instead of deleting it. Unlocks automatically once the content becomes last again. |
| `2026-07-19-bannerCarouselRtlFix.update.md` | `Front/knowledge/updates/2026-07-19-bannerCarouselRtlFix.update.md` | Follow-up to `2026-07-14-bannerCarousel.update.md`: fixes `StableCarousel`'s flex-row translate math only working under LTR (broke slide 2+ under the dashboard's default `dir="rtl"`) by detecting the resolved direction and flipping the translate sign; adds `StableCarouselPrevious`/`StableCarouselNext` nav buttons. |
| `2026-07-19-bannerCarouselOverlapAndFontSize.update.md` | `Front/knowledge/updates/2026-07-19-bannerCarouselOverlapAndFontSize.update.md` | Follow-up to `2026-07-19-bannerCarouselRtlFix.update.md`: reserves an inline gutter in `BannerSlide`'s `CardContent` matching the nav buttons' footprint so long text no longer renders underneath them; adds `fluidTextSizeClass()`, a 3-tier character-count-based size lookup for title/description, so a long banner shrinks instead of inflating every shuffled slide's height. |
| `2026-07-20-adminSidebarScroll.update.md` | `Front/knowledge/updates/2026-07-20-adminSidebarScroll.update.md` | Fixes the sidebar clipping its overflow: `SidebarContent` used `overflow-hidden` (upstream shadcn uses `overflow-auto`), so opening a parent menu taller than the sidebar hid items with no scrollbar and no wheel scroll. Switches to `overflow-y-auto overflow-x-hidden overscroll-contain` + the repo's `scrollbar-thin` convention, keeping header/footer pinned; `nav-main.tsx` scrolls a newly-opened parent into view. Shared via the `components/ui` symlink, so it fixes the dashboard's `ConsoleSidebar` too. |
| `2026-08-01-consentQuickReplyUnlock.update.md` | `Front/knowledge/updates/2026-08-01-consentQuickReplyUnlock.update.md` | Fixes BEF-142, a dead end left by `2026-07-18-lockConsentQuickReply.update.md`: the lock keyed only off "a content follows this one", so deleting every other quick reply left an auto-inserted `CONSENT` button that could never be removed (and still paused the flow at send time via `shouldPauseForConsent`). `isLockedConsentButton` now also requires at least one non-`CONSENT` quick reply to still be there — an untyped, freshly-added row counts as one. Frontend-only, no backend change. |
| `2026-08-01-contentTypePickerGrid.update.md` | `Front/knowledge/updates/2026-08-01-contentTypePickerGrid.update.md` | Fixes BEF-138: reverts `ChooseAutomationType.tsx` from the `Dialog`/`Drawer` (bottom-sheet-on-mobile) rewrite back to the earlier inline `grid-cols-5` icon+title button grid (no description per button, no dialog wrapper), while keeping the newer `options`-prop template-mode filtering. Refreshes `Automations.Contents.Types.buttons.titles`/`descriptions` copy in both apps' `fa.json` per reporter-supplied text. Also fixes a `Contents.tsx` gap the revert exposed: the zero-contents empty state (`no_content_title`/`add_step`) lacked the `!isChoosingType` gate the populated-list "add content" button already has, so it stacked visibly above the newly-inline type grid instead of the old modal quietly covering it. A reporter-requested "برچسب" (Label) content type has no backing enum value yet — left out of scope. |
| `2026-08-01-dashboardStaticBannersRemoval.update.md` | `Front/knowledge/updates/2026-08-01-dashboardStaticBannersRemoval.update.md` | Deletes the hardcoded, non-admin-managed `DashboardTelegramBanner.tsx`/`DashboardInstagramBanner.tsx` (fixed copy/URL/color, no DB backing, `fa`-only 2-column grid) from the dashboard home. `DashboardBannerCarousel` (the real admin-managed banner system) moves down to the bottom of `page.tsx` — the position the deleted grid occupied. New order: `SubscriptionBoard` → `DashboardStats` → `DashboardBannerCarousel`. Frontend-only, no backend change. |
| `2026-08-02-questionImageValidation.update.md` | `Front/knowledge/updates/2026-08-02-questionImageValidation.update.md` | Adds `Image` to `ValidationTypeEnum` (`packages/ui/src/automation-builder/types/validationType.enum.ts`) for the automation Question content step's `validationType` dropdown — paired with a Back change that validates the lead's DM reply contains an Instagram image attachment. `QuestionContent.tsx`'s `<Select>` already iterates `Object.values(ValidationTypeEnum)`, so only a default Persian error message case and the `fa.json` label were needed, in **both** `apps/dashboard/src/messages/fa.json` and `apps/admin/src/messages/fa.json` — the admin app's template builder renders the same `QuestionContent.tsx` (its own separately-synced `fa.json` copy needed the key too, caught by code review). No zod schema change (`z.nativeEnum` auto-includes new enum values). |
| `2026-08-02-questionPostalCodeValidation.update.md` | `Front/knowledge/updates/2026-08-02-questionPostalCodeValidation.update.md` | Adds `PostalCode` to `ValidationTypeEnum` for the same Question content `validationType` dropdown — a plain regex-based text validator (paired Back change), same shape as `Mobile`/`NationalCode`. Both `apps/dashboard/src/messages/fa.json` and `apps/admin/src/messages/fa.json` updated together this time, applying the lesson from the `Image` feature's missed admin copy. |
| `2026-08-04-contactsMobileTableScroll.update.md` | `Front/knowledge/updates/2026-08-04-contactsMobileTableScroll.update.md` | Fixes two mobile-only `/contacts` bugs. (1) The contacts table's horizontal scroll was unreachable: its `overflow-auto` box was genuinely scrollable (client 390px vs scroll 650px) but the box itself was **1368px tall**, so its scrollbar sat ~680px below the viewport. Root cause is `SidebarInset` (`packages/ui/src/components/ui/sidebar.tsx` — note both `apps/dashboard/src/components/ui` and `apps/admin/src/components/ui` are **symlinks** to it, so this change hits both apps) capping its height only at `md` (`md:max-h-screen`) — below `md` nothing had a definite height, so `LayoutTable`'s `h-full` resolved to `auto` and `flex-1` on the table box did nothing. Fixed with an all-breakpoint `max-h-svh`, plus moving mobile scrolling onto `SidebarInset` itself (`overflow-y-auto`, `md:overflow-hidden`) so pages with no scroll container of their own are not clipped by the new cap. **App-wide change — every `(Console)` page now scrolls inside `SidebarInset` on mobile instead of on `body`.** (2) Header/filter spacing: the Excel-export button moved from `HeaderTools` (which is `w-full` on mobile, so it wrapped onto its own row as a near-invisible `outline` button on the purple gradient) into `HeaderButton` as a labelled solid button, matching `orders/page.tsx`; the Instagram filter got its own padded `empty:hidden` row in `ContactsList.tsx` instead of relying on the column `gap-4`, while the table stays full-bleed. |
| `2026-08-03-bef164AutomationDelayAndMp3Fix.update.md` | `Front/knowledge/updates/2026-08-03-bef164AutomationDelayAndMp3Fix.update.md` | Fixes BEF-164: swaps the delay/wait step's number and time-unit `Select` field order in `DelayContent.tsx` (was reading "hour 1" in the RTL UI instead of "1 hour" — pure DOM-order swap, no logic change); adds `audio/mp3` to `MediaUploader.tsx`'s accepted audio formats (some browsers report `.mp3` as `audio/mp3` rather than `audio/mpeg`), plus matching `fa.json`/`en.json` help-text updates. Frontend-only, no backend change. Ticket's 3rd item (form-builder postal-code/photo options) reported separately resolved already. |
| `2026-08-03-bef163GeneralUiFixes.update.md` | `Front/knowledge/updates/2026-08-03-bef163GeneralUiFixes.update.md` | Fixes BEF-163's 6 general UI items: adds a dashboard-wide `RadixDirectionProvider` (root cause of every Radix `Select`/dropdown rendering LTR in the Persian UI — `apps/admin` already had this, `apps/dashboard` didn't); widens the automation builder's comment/DM-cap disclaimer to full width and refreshes its copy to the current 300/hour cap; fixes the account-sessions page's dead horizontal scroll (`min-w-0` on a flex ancestor), renames it to "دستگاه‌های فعال", and adds its missing breadcrumb translation; normalizes the mobile bottom nav's icon sizes/label typography so all 5 items align; widens `InstagramSelectField`'s single-page hide guard from `=== 0` to `<= 1` with a matching auto-select effect so the required `instagramIds` field still gets filled; and reworks the subscription-renewal form (removes the "already has an active subscription" notice, consolidates the duplicated desktop/mobile discount-code blocks into one placed after the duration cards instead of before them). |
| `2026-08-05-r2ImageHosts.update.md` | Adds the two Cloudflare R2 custom domains (`dl.befroosh.app` prod, `dltest.befroosh.app` test) to `apps/dashboard/next.config.mjs` `images.remotePatterns`, fixing a `next/image` "Invalid src prop … hostname … is not configured" crash on the workspace drawer. R2's own `**.r2.cloudflarestorage.com` S3-API host is deliberately excluded — it needs a signed request, so allowlisting it would not help; the real fix was in the backend URL builder (Back PR #5). Also adds `ERROR_CODES.UPLOAD_SOURCE_IS_NOT_AN_IMAGE` to `fa.json`. **Deploy coupling: ship before or with the Back change, and note `next.config.mjs` is read at build time so a rebuild is required.** |
| `2026-08-03-automationDraftBanner.update.md` | `Front/knowledge/updates/2026-08-03-automationDraftBanner.update.md` | Fixes BEF-141: moves the automation-draft resume prompt from a blocking popup dialog on the automations list page into a dismissible `AutomationDraftBanner` at the top of the create-automation form (`headerSlot`), with "continue editing" / "new message" (clear + in-place reset via a `formResetKey` remount) actions and auto-dismiss on the first real edit. Adds a 2-day TTL to stored drafts (`automationDraft.ts`). Frontend-only, no backend change. Follow-up (2026-08-04): the banner's copy and both buttons are now grouped inside a single amber `Alert` (responsive one-line/stacked layout, amber-themed buttons) instead of an alert plus a separate button row. |
| `WorkspaceDrawerContent.tsx` | `Front/apps/dashboard/src/components/Console/WorkspaceDrawerContent.tsx` | Presentational content of the mobile workspace drawer (opened from `NavBottom.tsx`'s bottom-nav "کسب‌وکار" item via the now-thin `WorkspaceDrawer.tsx` wrapper): profile card (name/mobile + pencil `Link` to `/settings`), an always-expanded list of every workspace with its Instagram accounts nested underneath (connection dot from `isIgTokenValid`; subscription text — days-left / covered-by-credit / pending-activation — computed directly from `GET /workspaces`'s per-instagram `subscriptionDaysLeft`/`hasReservedSubscription` and per-workspace `hasCreditCoverage`, **not** `PageCoverageBadge`, which only ever has the active workspace's data), tapping any workspace/account row switches context via `useWorkspaces().changeWorkspace`, and a footer "افزودن پیج" button to `/connect` plus logout. Split out from `WorkspaceDrawer.tsx` specifically so it's testable without vaul/Drawer portal mechanics. |
| `2026-08-03-workspaceDrawerRedesign.update.md` | `Front/knowledge/updates/2026-08-03-workspaceDrawerRedesign.update.md` | Summary of the mobile workspace drawer redesign: grouped per-workspace Instagram accounts, connection/subscription status, business-labeled bottom nav, add-page footer button. Paired Back branch `feat/workspace-drawer-redesign` (added `isIgTokenValid` to `GET /workspaces`). |
| `2026-08-05-workspaceDrawerRemainingDays.update.md` | `Front/knowledge/updates/2026-08-05-workspaceDrawerRemainingDays.update.md` | Follow-up fix: the drawer only showed subscription days-left for the active workspace (real-user-caught bug). `GET /workspaces` now computes it server-side per workspace; `WorkspaceDrawerContent.tsx` drops `PageCoverageBadge` entirely in favor of the new backend fields. Paired Back branch `fix/workspace-drawer-remaining-days`. |
| `2026-08-04-bef163AutomationListSinglePage.update.md` | `Front/knowledge/updates/2026-08-04-bef163AutomationListSinglePage.update.md` | BEF-163 follow-up: `AutomationCard.tsx` (automation list) now hides its per-card Instagram username badge when the workspace has only one connected page, mirroring `InstagramSelectField.tsx`'s `<= 1` guard from the 2026-08-03 bundle — that bundle only fixed the create form, not the list. |
| `2026-08-03-dashboardDevSpeed.update.md` | `Front/knowledge/updates/2026-08-03-dashboardDevSpeed.update.md` | Cuts `apps/dashboard` dev-mode cost. `@phosphor-icons/react` is imported from 119 sites and its barrel pulls ~3025 modules (1512 CSR + 1513 SSR); Turbopack does not tree-shake in dev, and Next 16's built-in `optimizePackageImports` list covers `lucide-react`/`recharts` but not Phosphor — fixed by adding `experimental.optimizePackageImports: ['@phosphor-icons/react', '@befroosh/ui']`. Sentry also ran at full power in dev (client Session Replay under HMR, `tracesSampleRate: 1`, server-side OpenTelemetry hooking every module load) — now gated behind `enabled: isProd` in all three configs, production behaviour unchanged. Stale 5.2 GB `.next` cleared. Also rewrites all 116 barrel imports to deep per-icon paths (263 lines, incl. the separate 1513-module `/dist/ssr` barrel) and removes the 2 `import * as` namespace imports that pulled the whole barrel regardless of config — both were dynamic icon-lookup-by-name, now passing the component instead. Drops 9 unused deps plus the `motion` wrapper (`motion/react` is just `export * from 'framer-motion'`). Statically verified: 150/150 specifiers resolve, 264/264 bindings exist; typecheck 211=211 (0 introduced). **Measured:** `/contacts` 13.1s → 1.69s (~7.8x), `/directs` ~30%, `/orders` ~13%; other routes within noise. Nearly the whole win came from deleting the 2 namespace imports, not the 116-file deep-import rewrite — a namespace import defeats both `optimizePackageImports` and deep imports. `apps/admin` given the same treatment. |
| `2026-08-03-guideHelperFixes.update.md` | `Front/knowledge/updates/2026-08-03-guideHelperFixes.update.md` | Fixes BEF-140's in-app guide (راهنما) dialog: `HelpMeDialog.tsx`'s close control moves from a bottom "بستن" button to a `DialogClose`+`X` icon in the dialog's top-left corner (RTL layout, matches the header's reserved `pl-6` space); `playsInline` added to the guide `<video>` to stop iOS Safari auto-fullscreen; the shared `AutomationBuilder` package's `TitleAndEnabled`/`ContentItem` gained `helpSlot`/`contentTypeHelpSlots` so the dashboard can inject a CMS-driven `HelpMeDialog` per content type and for the automated-message-name field; the "start request message" guide's overlap with the header delete button is fixed via `noAbsolute`; new `helpId`s are registered in the Admin guides page's dropdown. |
| `2026-08-08-secondInstagramSubscriptionGate.update.md` | `Front/knowledge/updates/2026-08-08-secondInstagramSubscriptionGate.update.md` | New `SetupInstagramDialog` on `/connect`, gated by `hasAvailableSubscriptionSlot` from `GET /users/me`, guides a user through username check → follower lookup → matched/manual plan → buy before connecting a 2nd+ Instagram account. Payment verify redirects to `/connect` (not `/settings/instagram`) for pooled purchases, with a one-time-read `pending_ig_username` cookie reminder. Also fixes a shallow-merge i18n bug: new `ERROR_CODES` translations must live in `messages/fa/ErrorCodes.json`, not the monolithic `messages/fa.json` (which is fully shadowed by the split file in `i18n/request.ts`'s merge). |
