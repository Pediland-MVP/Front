# Product kind dialog removal, unlimited stock, and two variation-grid bugs — 2026-09-07

Supersedes the "Add product" half of
[`2026-09-06-digitalProductsFinalMessage.update.md`](./2026-09-06-digitalProductsFinalMessage.update.md)
(the `ChooseProductKindDialog` it introduced is gone). The `kind` field, `ProductKindSection` and
the whole final-message feature described there are unchanged.

## Problem

Two things, both raised by the user.

1. **The physical/digital dialog was in the way.** «افزودن محصول» opened
   `ChooseProductKindDialog` before the editor mounted, and the answer only pre-set a control the
   editor already has (step ۱، `ProductKindSection`) and which stays editable afterwards anyway. It
   was a mandatory question for something the merchant could change one screen later.

2. **Steps ۶ and ۷ silently did nothing for a product with no attributes**, and the quantity field
   had no "unlimited" option at all.

   The second half is the load-bearing one. `BasePriceSection`/`BaseStockSection` wrote **only**
   `basePrice`/`baseCompare`/`baseStock`. Those are seeds: the sole reader is `useVariantSync`,
   which copies them into rows **it generates**, and generation only runs on an axis edit
   (`combosOf` returns `[]` with no axes). So for a product that never got a رنگ/سایز:

   - nothing was ever generated, so nothing ever read the seeds;
   - the product's real price and stock live on its one implicit variation
     (`buildEmptyProductForm`'s single blank row, rendered in the grid as «محصول بدون ویژگی»);
   - Save then failed zod's `priceRequired`/`stockRequired` on `variants.0.*` and jumped the
     merchant down to a grid row to retype what they had just typed at the top of the page.

   Meanwhile the grid row had an ∞ button and the card above it did not, so "unlimited" was
   reachable only from the table.

## Solution

**Dialog:** deleted. «افزودن محصول» pushes `/products/add` directly, `kind` starts `'physical'`,
and step ۱ is the only place it is ever chosen. The `?kind=` query param went with it — nothing
produced one any more, so reading it was dead code.

**Steps ۶ and ۷:** while the card is unlocked (no live axis) it now reads AND writes
`variants[0]` — the row that IS the product — instead of only the seed. `baseStock`/`basePrice`/
`baseCompare`/`baseInfinite` are still written alongside, because the implicit row carries no
option values and so never matches `donorOf`: the seeds are what the rows generated by the FIRST
axis inherit. Locked (real variations exist), the card is display-only over the seeds, as before.

**Unlimited:** a new `baseInfinite` form field plus a labelled switch on the quantity card,
matching the discount switch on the price card. It maps to `trackInventory: false` through the
existing `variants[].infinite` → payload path — no backend change. Turning it on clears the count
(a leftover would be sent as `initialStock` and rewrite the ledger of an untracked variant), the
same rule `VariantLeafRow` already followed.

`useVariantSync` seeds `infinite` onto **every** generated row, not just the first, because ∞ is a
tracking MODE rather than a quantity — the rule its own donor branch already applied.

### Why the discount switch lost its `useState`/`useEffect`

`BasePriceSection` kept `onSale` in local state with an effect re-syncing it from `baseCompare`,
plus a `lastLocal` ref so clearing the field mid-edit did not flip the switch off under the
cursor. Now that the card is bound to the row, the row's stored `hasDiscount` flag — which exists
for exactly this reason — answers the question directly, so all three were removed.

## Changes

| File | Change |
|---|---|
| `apps/dashboard/src/components/Commerce/ProductList/ChooseProductKindDialog.tsx` | **Deleted** |
| `apps/dashboard/src/components/Commerce/ProductList/ChooseProductKindDialog.test.tsx` | **Deleted** |
| `apps/dashboard/src/components/Commerce/ProductList/ProductListPage.tsx` | «افزودن محصول» pushes `/products/add` via `useRouter`; dialog import, `isChooseKindOpen` state and render removed |
| `apps/dashboard/src/components/Commerce/ProductList/ProductListPage.test.tsx` | Two dialog tests replaced by one asserting the direct push and that no dialog opens; dropped the now-unused `within` import |
| `apps/dashboard/src/components/Commerce/ProductEditor/ProductEditorPage.tsx` | `initialKind` is a constant `'physical'`; `useSearchParams` import and `?kind=` read removed |
| `apps/dashboard/src/components/Commerce/ProductEditor/ProductEditorPage.test.tsx` | Removed the `?kind=digital` seam test, the `mockUseSearchParams` hoisted mock and its per-test reset; `next/navigation` mock reduced to `useRouter` |
| `apps/dashboard/src/components/Commerce/ProductEditor/sections/BaseStockSection.tsx` | Unlimited switch; unlocked reads/writes `variants.0.stock`/`.infinite` and mirrors the seeds; ∞ disables and clears the count; hint swaps to `infiniteHint` |
| `apps/dashboard/src/components/Commerce/ProductEditor/sections/BaseStockSection.test.tsx` *(new)* | 12 tests: p2e digit entry, row-vs-seed write-through, null-vs-zero, the switch (start state, clearing, round trip, hint), and the locked/seed-display case |
| `apps/dashboard/src/components/Commerce/ProductEditor/sections/BasePriceSection.tsx` | Unlocked reads/writes `variants.0.price`/`.compare`/`.hasDiscount` and mirrors the seeds; `onSale` derives from the row's stored flag, dropping `useState`/`useEffect`/`lastLocal` |
| `apps/dashboard/src/components/Commerce/ProductEditor/sections/BasePriceSection.test.tsx` | Harness carries a real implicit row; probes assert row AND seed; new write-through and locked-shows-seed tests (16 total) |
| `apps/dashboard/src/components/Commerce/ProductEditor/sections/ProductKindSection.tsx` | Docstring: step ۱ is now the only place a kind is picked |
| `apps/dashboard/src/components/Commerce/ProductEditor/productEditor.schema.ts` | `baseInfinite: boolean` on `ProductFormValues` + `z.boolean()` + `buildEmptyProductForm`; seed docstring rewritten; `kind` docstring no longer names the dialog |
| `apps/dashboard/src/components/Commerce/ProductEditor/productEditor.mapping.ts` | `mapDetailToFormValues` returns `baseInfinite: false` (a loaded product reads ∞ off its row, not the seed) |
| `apps/dashboard/src/components/Commerce/ProductEditor/variant/useVariantSync.ts` | Generated rows inherit `infinite` from `baseInfinite` (every row, not just the first); the count seed is skipped entirely when the base is ∞ |
| `apps/dashboard/src/messages/fa.json` | Removed `Commerce.List.ChooseKind`; added `BaseStock.infinite`/`infiniteHint`/`infinitePlaceholder`; `BaseStock.hint` and `BasePrice.hint` reworded — they describe the product's own number now, not only a seed |
| test fixtures (`test/renderWithForm.tsx`, `AttributesSection`, `VariantsSection`, `useVariantSync`, `productEditor.mapping`, `PreviewDialog`) | `baseInfinite: false` added alongside `baseStock` |

No backend change: `trackInventory` and the `COMMERCE_KIND_LOCKED` guard already existed and are
untouched.

## Verification

- `pnpm exec tsc --noEmit` (dashboard): **zero errors in every touched file.** The app's ~165
  pre-existing `src/` errors (Badge props, `ImageWithFallback`, the repo-wide zod-3/zod-4
  `zodResolver` mismatch) are unchanged — none are in or caused by this work.
- `pnpm exec vitest run src/components/Commerce` — **45 files, 518 tests, all passing**, including
  the 12 new `BaseStockSection` tests and the rewritten `BasePriceSection` suite. The
  `CancelOrderDialog` `act(...)` stderr warning is pre-existing and unrelated.
- Not manually smoke-tested in a browser.

---

# Follow-up: two variation-grid bugs (same day, same branch)

Both reported by the user after clicking through the deployed test build. Neither was introduced
by the work above — both are pre-existing behaviour in the variations grid (step ۱۰).

## Problem A — "sometimes gives me an error without explanation"

Setting an offer (٪) on a variation and saving produced the fixed toast
«چند مورد کامل نیست. موردهای قرمز را درست کنید.» and nothing the merchant could act on.

Two separate causes, both needed for the symptom:

1. **The blocking error was painted as a soft warning.** `VariantNumberCell` tinted a `compare`
   issue `zero`, on the reasoning that its failure fires while the value is PRESENT rather than
   missing. But `globals.css` defines the tones by SEVERITY, and says so in its own comment:
   `empty` = red = "blocks Save outright", `zero` = amber = "a soft warning". `compareInvalid`
   blocks the save. So the toast said "fix the RED ones" while the only wrong cell was amber —
   the merchant was sent hunting for a colour that was not on screen.

2. **The reason was never shown anywhere.** Every issue in this schema carries a translated
   message (`compareInvalid` = «قیمت بدون تخفیف باید بیشتر از قیمت فروش باشد.»), but the grid
   renders no message line — a failing cell only ever changed colour — and `onInvalid` threw the
   message away, toasting a fixed string that names a colour and nothing else.

"Sometimes" is the giveaway: it fires only when the offer is at or below the sale price.

A third defect found while tracing, same family: `firstErrorPath`'s variants branch ended in a
bare `variants.<index>.stock` fallback, so a row failing on `saleStartsAt`/`saleEndsAt`
(pre-existing sale-window data — this design draws no control for either) put the cursor in a
stock cell with nothing wrong in it.

## Problem B — "it also didn't split the price when I'm typing it"

The grid's number cells are UNCONTROLLED (`register`) — that is what keeps 2000 rows responsive —
so nothing re-renders them while typing, and `onInputP2EHandler` strips every non-digit from the
DOM value on each keystroke, separators included. Nothing put them back until `onBlur`. The number
therefore grew as a bare digit string and only "split" once the merchant left the cell, while the
base price card beside it (controlled, `value={formatAmount(...)}`) split on every keystroke.

## Solution

| File | Change |
|---|---|
| `utils/editorNumber.util.ts` | New `formatAmountInPlace(element)`: does `onInputP2EHandler`'s job and writes the formatted value straight back to the input, restoring the caret **by digit count, not character index** — counting characters drifts by one per separator inserted and walks the caret backwards through the number as it grows |
| `variant/VariantNumberCell.tsx` | `onInput` uses `formatAmountInPlace`; a real zod issue now always tints `empty` (red), `compare` included, with the amber `tone` prop left to its real job — the live hint before any submit; the message rides on the cell's `title` so a failing cell says something besides its colour |
| `ProductEditorPage.tsx` | `firstErrorPath` → `firstError`, returning `{ path, message }`; `onInvalid` toasts the schema's own reason and falls back to `Errors.invalid` only when an issue carries no message; the variants branch returns a `null` path instead of a bogus `stock` one when the failing field has no cell |

No i18n change — every message this now surfaces already existed and was simply never displayed.

## Verification

- Both problems reproduced as **failing tests first**, then fixed:
  `expected '420000' to be '۴۲۰٬۰۰۰'` for the separators, and `data-bad` amber instead of red for
  the offer error.
- `vitest run src/components/Commerce` — **45 files, 530 tests** (was 518; +12). New: 7
  `formatAmountInPlace` unit tests covering caret placement mid-number, at the start and past a
  freshly inserted separator; 3 grid live-formatting tests; 1 tone test; 1 end-to-end
  `ProductEditorPage` test asserting the toast carries `compareInvalid` and NOT `Errors.invalid`,
  with the cell red.
- `tsc --noEmit` clean on every touched file (165 pre-existing `src/` errors unchanged);
  eslint 0 errors.
- Caret behaviour is covered by unit tests against a real `<input>`, but **has not been typed into
  by a human** — worth one pass in a browser.

