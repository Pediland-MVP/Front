networks:
  app_networks:
    external: true

services:
  front1:
    build:
      context: /home/cicd/Front
      dockerfile: Dockerfile
    container_name: front1
    networks:
      - app_networks
    ports:
      - '50001:3000'
    restart: always
